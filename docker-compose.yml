services:
  # PostgreSQL Database (pgvector for memory embeddings)
  postgres:
    image: pgvector/pgvector:pg16
    container_name: rtpi-postgres
    environment:
      POSTGRES_USER: rtpi
      POSTGRES_PASSWORD: rtpi
      POSTGRES_DB: rtpi_main
    ports:
      - "5434:5432"  # Changed from 5432 to avoid conflicts
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres-init:/docker-entrypoint-initdb.d:ro  # Creates sysreptor DB on first init
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rtpi"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rtpi-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: rtpi-redis
    ports:
      - "6381:6379"  # Changed from 6379 to avoid conflicts
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rtpi-network

  # RTPI Security Tools Container
  rtpi-tools:
    build:
      context: .
      dockerfile: Dockerfile.tools
    container_name: rtpi-tools
    network_mode: "host"
    volumes:
      - tool-results:/var/log/rtpi
      - tool-configs:/opt/tools/config
      - shared-data:/shared
    stdin_open: true
    tty: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "echo", "healthy"]
      interval: 30s
      timeout: 10s
      retries: 3

  # PowerShell Empire C2 Server
  empire-server:
    image: bcsecurity/empire:latest
    container_name: rtpi-empire
    ports:
      # REST API (1337) and Web UI (5001) proxied through empire-proxy
      - "8080-8100:8080-8100"  # Dynamic listener ports (direct, no proxy needed)
    environment:
      # Database connection (shared PostgreSQL)
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: rtpi_main
      POSTGRES_USER: rtpi
      POSTGRES_PASSWORD: rtpi
      # Empire configuration
      EMPIRE_USERNAME: empireadmin
      EMPIRE_PASSWORD: ${EMPIRE_PASSWORD:-Islamist-Repeal1-Unhealthy-Amulet-Survival}
      RESTPORT: 1337
      # Optional: Enable SocketIO for real-time events
      SOCKETIO_ENABLED: "true"
      SOCKETIO_PORT: 5002
    volumes:
      - empire-data:/opt/Empire/data
      - empire-downloads:/opt/Empire/downloads
      - shared-data:/shared
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "-o", "/dev/null", "http://0.0.0.0:1337/"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # Empire Reverse Proxy — normalizes paths (fixes Starkiller //token double-slash)
  empire-proxy:
    image: nginx:alpine
    container_name: rtpi-empire-proxy
    ports:
      - "1337:1337"     # Empire REST API (proxied)
      - "5001:5001"     # Empire Web UI (proxied)
    volumes:
      - ./docker/empire-proxy.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - empire-server
    networks:
      - rtpi-network
    restart: unless-stopped

  # ATT&CK Workbench MongoDB Database
  workbench-db:
    image: mongo:7
    container_name: rtpi-workbench-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: workbench
      MONGO_INITDB_ROOT_PASSWORD: ${WORKBENCH_DB_PASSWORD:-workbench123}
      MONGO_INITDB_DATABASE: attack-workbench
    ports:
      - "27017:27017"
    volumes:
      - workbench-db-data:/data/db
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "mongosh -u workbench -p \"$$MONGO_INITDB_ROOT_PASSWORD\" --authenticationDatabase admin --quiet --eval \"db.adminCommand('ping')\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ATT&CK Workbench REST API
  workbench-api:
    image: ghcr.io/center-for-threat-informed-defense/attack-workbench-rest-api:latest
    container_name: rtpi-workbench-api
    ports:
      - "3010:3010"
    environment:
      # MongoDB connection
      MONGODB_URL: mongodb://workbench:${WORKBENCH_DB_PASSWORD:-workbench123}@workbench-db:27017/attack-workbench?authSource=admin
      DATABASE_URL: mongodb://workbench:${WORKBENCH_DB_PASSWORD:-workbench123}@workbench-db:27017/attack-workbench?authSource=admin
      # API configuration
      PORT: 3010
      # CORS configuration - allow RTPI frontend
      CORS_ORIGIN: "http://localhost:5000,http://localhost:3000,http://localhost:3001,http://localhost:3020"
      # Session configuration
      SESSION_SECRET: ${WORKBENCH_SESSION_SECRET:-change-this-secret-key}
      # Authentication (optional)
      AUTHN_MECHANISM: anonymous
    depends_on:
      workbench-db:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:3010/api/collections > /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  # ATT&CK Workbench Frontend (Optional - for direct access)
  workbench-frontend:
    image: ghcr.io/center-for-threat-informed-defense/attack-workbench-frontend:latest
    container_name: rtpi-workbench-frontend
    ports:
      - "3020:80"
    environment:
      # API endpoint configuration
      WORKBENCH_REST_API_URL: http://workbench-api:3010/api
    volumes:
      # Override the upstream image's stock nginx config which lacks both
      # an SPA history fallback (so /matrices, /tactics, etc. 404) and an
      # /api/ proxy (so the Angular SPA can't reach workbench-api).
      # See configs/rtpi-workbench/default.conf for the corrected config.
      - ./configs/rtpi-workbench/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      workbench-api:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped

  # ============================================================================
  # Kasm Workspaces - Browser-Based Desktop Streaming Platform
  # All Kasm services are in the 'kasm' profile and must be explicitly enabled
  # Usage: docker compose --profile kasm up -d
  # ============================================================================

  # Kasm DB Certificate Init - Generates self-signed SSL certs for kasm-db
  kasm-db-init:
    image: alpine:latest
    container_name: rtpi-kasm-db-init
    profiles:
      - kasm  # Only start with Kasm profile
    volumes:
      - kasm-db-certs:/certs
      - ./scripts/kasm:/scripts:ro
    command: >
      sh -c "
        apk add --no-cache openssl &&
        /scripts/init-db-certs.sh
      "
    networks:
      - rtpi-network

  # Kasm Proxy Certificate Init - Generates self-signed nginx certs
  kasm-proxy-init:
    image: alpine:latest
    container_name: rtpi-kasm-proxy-init
    profiles:
      - kasm  # Only start with Kasm profile
    environment:
      KASM_SERVER_HOSTNAME: ${KASM_SERVER_HOSTNAME:-localhost}
    volumes:
      - kasm-certs:/certs
      - ./scripts/kasm:/scripts:ro
    command: >
      sh -c "
        apk add --no-cache openssl &&
        /scripts/init-proxy-certs.sh
      "
    networks:
      - rtpi-network

  # Kasm PostgreSQL Database (separate from main RTPI database)
  kasm-db:
    image: postgres:16-alpine
    container_name: rtpi-kasm-db
    profiles:
      - kasm  # Only start with Kasm profile
    # kasmweb/api 1.17.0 baked config requires SSL on the DB connection.
    # The kasm-db-certs volume is populated by kasm-db-init.
    command:
      - postgres
      - -c
      - ssl=on
      - -c
      - ssl_cert_file=/etc/ssl/certs/kasm/db_server.crt
      - -c
      - ssl_key_file=/etc/ssl/certs/kasm/db_server.key
    environment:
      POSTGRES_USER: kasmapp
      POSTGRES_PASSWORD: ${KASM_DB_PASSWORD:-kasm123secure}
      POSTGRES_DB: kasm
    ports:
      - "5433:5432"  # Different port to avoid conflict with main postgres
    volumes:
      - kasm-db-data:/var/lib/postgresql/data
      - kasm-db-certs:/etc/ssl/certs/kasm:ro
      # Loaded once on first init by the postgres image entrypoint.
      - ./scripts/kasm/init-extensions.sql:/docker-entrypoint-initdb.d/01-extensions.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kasmapp"]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      kasm-db-init:
        condition: service_completed_successfully
    networks:
      rtpi-network:
        aliases:
          - db  # kasmweb/api 1.17.0 baked config hardcodes hostname "db"
    restart: unless-stopped

  # Kasm Redis Cache (separate from main RTPI Redis)
  kasm-redis:
    image: redis:5-alpine
    container_name: rtpi-kasm-redis
    profiles:
      - kasm  # Only start with Kasm profile
    ports:
      - "6380:6379"  # Different port to avoid conflict with main redis
    volumes:
      - kasm-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      rtpi-network:
        aliases:
          - kasm_redis  # kasmweb/api 1.17.0 baked config uses underscore form
    restart: unless-stopped

  # Kasm API Server - REST API for workspace management
  kasm-api:
    image: kasmweb/api:1.17.0
    container_name: rtpi-kasm-api
    profiles:
      - kasm  # Only start with Kasm profile
    # Run as root so the entrypoint can sed-patch /opt/kasm/current/conf/...
    # The image's default user "kasm" doesn't own that path.
    user: "0:0"
    environment:
      # POSTGRES_* env vars are read by /usr/bin/startup.sh for DB initialization.
      # The runtime API server reads its connection settings from
      # /opt/kasm/current/conf/app/api/api.app.config.yaml — which the
      # entrypoint below patches from these same env vars on every start.
      POSTGRES_HOST: kasm-db
      POSTGRES_PORT: 5432
      POSTGRES_USER: kasmapp
      POSTGRES_PASSWORD: ${KASM_DB_PASSWORD:-kasm123secure}
      POSTGRES_DB: kasm
      DB_AUTO_INITIALIZE: "1"   # tells startup.sh to create schema if absent
      # API configuration
      API_SERVER_ID: ${KASM_API_SERVER_ID:-default}
      SERVER_HOSTNAME: ${KASM_SERVER_HOSTNAME:-localhost}
      SERVER_ZONE: default
      # SSL/TLS
      HTTPS_ENABLED: "true"
      SSL_CERT_FILE: /opt/kasm/current/certs/kasm_nginx.crt
      SSL_KEY_FILE: /opt/kasm/current/certs/kasm_nginx.key
    # Override the default entrypoint to patch the baked api.app.config.yaml
    # with the env-var values *before* startup.sh runs. The kasmweb/api:1.17.0
    # image hardcodes "password: changeme" / "redis_password: changeme" /
    # "host: db" in the YAML — these values win over the env vars at runtime.
    entrypoint:
      - /bin/sh
      - -c
      - |
        CFG=/opt/kasm/current/conf/app/api/api.app.config.yaml
        if [ -f "$$CFG" ]; then
          sed -i "s|^  password: .*|  password: $${POSTGRES_PASSWORD}|" "$$CFG"
          sed -i 's|^  redis_password: .*|  redis_password: ""|' "$$CFG"
          echo "[kasm-api] Patched $$CFG"
        fi
        exec /usr/bin/startup.sh
    # No host port mapping — kasm-proxy fronts the API on the internal network.
    # Exposing 443 here would collide with kasm-proxy's 8443:443 binding.
    volumes:
      - kasm-api-data:/opt/kasm/current
      - kasm-certs:/opt/kasm/current/certs
    depends_on:
      kasm-db:
        condition: service_healthy
      kasm-redis:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      # API serves plain HTTP on 8080; HTTPS is terminated by kasm-proxy.
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/__healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # Kasm Manager - Workspace lifecycle management
  kasm-manager:
    image: kasmweb/manager:1.17.0
    container_name: rtpi-kasm-manager
    profiles:
      - kasm  # Only start with Kasm profile
    # Run as root so the entrypoint can sed-patch the YAML in its own volume.
    user: "0:0"
    environment:
      # The kasmweb/manager image reads its DB/Redis config from
      # /opt/kasm/current/conf/app/api/api.app.config.yaml in its own volume.
      # We patch that YAML in the entrypoint below using these env vars.
      POSTGRES_PASSWORD: ${KASM_DB_PASSWORD:-kasm123secure}
      MANAGER_ID: ${KASM_MANAGER_ID:-default-manager}
      SERVER_HOSTNAME: ${KASM_SERVER_HOSTNAME:-localhost}
    entrypoint:
      - /bin/sh
      - -c
      - |
        CFG=/opt/kasm/current/conf/app/api/api.app.config.yaml
        if [ -f "$$CFG" ]; then
          sed -i "s|^  password: .*|  password: $${POSTGRES_PASSWORD}|" "$$CFG"
          sed -i 's|^  redis_password: .*|  redis_password: ""|' "$$CFG"
          echo "[kasm-manager] Patched $$CFG"
        fi
        exec python3 /src/api_server/manager_api_server.pyc --cfg "$$CFG"
    volumes:
      - kasm-manager-data:/opt/kasm/current
      - /var/run/docker.sock:/var/run/docker.sock  # Docker socket for workspace management
    depends_on:
      kasm-db:
        condition: service_healthy
      kasm-redis:
        condition: service_healthy
      kasm-api:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped

  # Kasm Proxy (Nginx) - Reverse proxy for workspace access
  kasm-proxy:
    image: nginx:alpine
    container_name: rtpi-kasm-proxy
    profiles:
      - kasm  # Only start with Kasm profile
    environment:
      - SERVER_HOSTNAME=${KASM_SERVER_HOSTNAME:-localhost}
      # Used by envsubst in default.conf.template — controls the redirect target.
      # Set to 443 in production behind real DNS on standard ports.
      - KASM_PUBLIC_HTTPS_PORT=${KASM_PUBLIC_HTTPS_PORT:-8443}
    ports:
      - "8443:443"  # HTTPS for workspace access
      - "8444:80"   # HTTP redirect to HTTPS (8080 reserved by rtpi-empire 8080-8100)
    volumes:
      # Kasm-aware nginx config template. nginx:alpine's entrypoint runs
      # envsubst over /etc/nginx/templates/*.template and writes the result
      # to /etc/nginx/conf.d/, so ${KASM_PUBLIC_HTTPS_PORT} is expanded once
      # at container start. Per-workspace configs are written by
      # KasmNginxManager into the containers.d/ subdir below.
      - ./docker/kasm-proxy/default.conf.template:/etc/nginx/templates/default.conf.template:ro
      - kasm-proxy-config:/etc/nginx/conf.d/containers.d
      - kasm-certs:/etc/nginx/certs:ro
    depends_on:
      kasm-proxy-init:
        condition: service_completed_successfully
      kasm-api:
        condition: service_healthy
      kasm-guac:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      # Probe HTTPS with -k (self-signed) instead of HTTP, since :80 is just a redirect.
      test: ["CMD", "wget", "--no-check-certificate", "--quiet", "--tries=1", "--spider", "https://localhost:443/api/__healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Kasm Guacamole - Remote desktop protocol gateway
  kasm-guac:
    image: kasmweb/kasm-guac:1.17.0
    container_name: rtpi-kasm-guac
    profiles:
      - kasm  # Only start with Kasm profile
    environment:
      # Database connection for session tracking
      POSTGRES_HOSTNAME: kasm-db
      POSTGRES_PORT: 5432
      POSTGRES_DATABASE: kasm
      POSTGRES_USER: kasmapp
      POSTGRES_PASSWORD: ${KASM_DB_PASSWORD:-kasm123secure}
      # Guacamole configuration
      GUACD_HOSTNAME: localhost
      GUACD_PORT: 4822
    ports:
      - "4822:4822"  # Guacamole daemon port
    volumes:
      - kasm-guac-data:/opt/kasm/current
    depends_on:
      kasm-db:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped

  # Kasm Agent - Workspace provisioning and monitoring
  kasm-agent:
    image: kasmweb/agent:1.17.0
    # The agent uses the Docker socket to inspect itself by container name
    # "kasm_agent" (hardcoded in the image). Keep this name verbatim — renaming
    # to rtpi-kasm-agent breaks self-registration.
    container_name: kasm_agent
    profiles:
      - kasm  # Only start with Kasm profile
    environment:
      # API connection
      API_HOSTNAME: kasm-api
      API_PORT: 443
      # Agent configuration
      AGENT_ID: ${KASM_AGENT_ID:-default-agent}
      SERVER_HOSTNAME: ${KASM_SERVER_HOSTNAME:-localhost}
      PUBLIC_HOSTNAME: ${KASM_PUBLIC_HOSTNAME:-localhost}
    volumes:
      - kasm-agent-data:/opt/kasm/current
      - /var/run/docker.sock:/var/run/docker.sock  # Docker socket for container management
    depends_on:
      kasm-api:
        condition: service_healthy
      kasm-manager:
        condition: service_started
    networks:
      - rtpi-network
    restart: unless-stopped

  # Kasm Share - File sharing service for workspaces
  kasm-share:
    image: kasmweb/share:1.17.0
    container_name: rtpi-kasm-share
    profiles:
      - kasm  # Only start with Kasm profile
    # Run as root so the entrypoint can sed-patch the YAML in the image layer.
    user: "0:0"
    environment:
      # Patched into api.app.config.yaml by the entrypoint below. The kasmweb/share
      # image hardcodes "password: changeme" in the YAML; without this patch the
      # share service hits "password authentication failed for user kasmapp"
      # against kasm-db.
      POSTGRES_PASSWORD: ${KASM_DB_PASSWORD:-kasm123secure}
      # Redis connection for share tracking
      REDIS_HOSTNAME: kasm-redis
      REDIS_PORT: 6379
      # Share service configuration
      SHARE_ID: ${KASM_SHARE_ID:-default-share}
    # Override the default entrypoint to patch the baked api.app.config.yaml
    # before launching share_server. Same pattern used by kasm-api and
    # kasm-manager — kasm-redis has no auth, so blank redis_password.
    entrypoint:
      - /bin/sh
      - -c
      - |
        CFG=/opt/kasm/current/conf/app/api/api.app.config.yaml
        if [ -f "$$CFG" ]; then
          sed -i "s|^  password: .*|  password: $${POSTGRES_PASSWORD}|" "$$CFG"
          sed -i 's|^  redis_password: .*|  redis_password: ""|' "$$CFG"
          echo "[kasm-share] Patched $$CFG"
        fi
        exec python3 /src/api_server/share_server.pyc --cfg "$$CFG"
    ports:
      - "8182:8182"  # File sharing port
    volumes:
      - kasm-share-data:/opt/kasm/current/shares
    depends_on:
      kasm-db:
        condition: service_healthy
      kasm-redis:
        condition: service_healthy
    networks:
      - rtpi-network
    restart: unless-stopped

  # Certbot - Automated SSL/TLS certificate management
  # Supports both HTTP-01 and DNS-01 (Cloudflare) challenges
  certbot:
    image: certbot/dns-cloudflare:latest
    container_name: rtpi-certbot
    environment:
      - CERTBOT_EMAIL=${CERTBOT_EMAIL:-admin@example.com}
      - CERTBOT_DOMAIN=${KASM_SERVER_HOSTNAME:-localhost}
      # Cloudflare API token for DNS-01 challenge (#KW-12)
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-}
      # Container name to signal for an nginx reload after a renewal copy.
      # Defaults to rtpi-kasm-proxy in this stack.
      - KASM_NGINX_CONTAINER=${KASM_NGINX_CONTAINER:-rtpi-kasm-proxy}
    volumes:
      - certbot-conf:/etc/letsencrypt
      - certbot-www:/var/www/certbot
      - kasm-certs:/etc/nginx/certs
      - ./scripts/ssl:/scripts/ssl:ro  # SSL automation scripts
      # Read-only docker socket so the renewal loop can `docker exec` an
      # nginx reload after copying new certs into kasm-certs.
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # The certbot/dns-cloudflare image sets ENTRYPOINT ["certbot"], which would
    # cause our shell loop below to be passed as args to certbot. Override with
    # /bin/sh so `command:` is interpreted as a script. Use a literal block
    # scalar (|) — the previous folded (>) form collapsed multi-line if/fi
    # constructs into a single unparseable line.
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        # Install docker-cli once so we can signal the kasm-proxy nginx for a reload.
        apk add --no-cache docker-cli >/dev/null 2>&1 || true

        # Create Cloudflare credentials if API token is set
        if [ -n "$$CLOUDFLARE_API_TOKEN" ]; then
          echo "dns_cloudflare_api_token = $$CLOUDFLARE_API_TOKEN" > /etc/letsencrypt/cloudflare.ini
          chmod 600 /etc/letsencrypt/cloudflare.ini
        fi

        # Certificate renewal loop (#KW-13)
        trap exit TERM
        while :; do
          # Renew certificates expiring within 30 days
          certbot renew --quiet

          cert_changed=0

          # Copy certificates to nginx directory
          if [ -d /etc/letsencrypt/live ]; then
            for domain in /etc/letsencrypt/live/*; do
              if [ -d "$$domain" ]; then
                domain_name=$$(basename "$$domain")
                cp -L "$$domain/fullchain.pem" "/etc/nginx/certs/$${domain_name}.crt" 2>/dev/null || true
                cp -L "$$domain/privkey.pem" "/etc/nginx/certs/$${domain_name}.key" 2>/dev/null || true

                # When this is the configured kasm hostname, also publish the
                # cert under the canonical filenames the kasm-proxy nginx
                # config loads (kasm_nginx.crt / kasm_nginx.key).
                if [ "$${domain_name}" = "$$CERTBOT_DOMAIN" ]; then
                  if ! cmp -s "$${domain}/fullchain.pem" /etc/nginx/certs/kasm_nginx.crt 2>/dev/null; then
                    cp -L "$${domain}/fullchain.pem" /etc/nginx/certs/kasm_nginx.crt
                    cp -L "$${domain}/privkey.pem"   /etc/nginx/certs/kasm_nginx.key
                    cert_changed=1
                  fi
                fi
              fi
            done
          fi

          # Reload kasm-proxy nginx so it picks up the new cert without dropping connections.
          if [ "$$cert_changed" = "1" ]; then
            docker exec "$$KASM_NGINX_CONTAINER" nginx -s reload 2>&1 || \
              echo "[certbot] warning: failed to reload $$KASM_NGINX_CONTAINER nginx (ok if not running yet)"
          fi

          # Renewal runs every 12 hours
          sleep 12h & wait $$!
        done
    networks:
      - rtpi-network
    restart: unless-stopped
    profiles:
      - certbot  # Optional profile - only start if SSL automation is needed

  # ===========================================================================
  # Local LLM Inference Services
  # ===========================================================================
  #
  # RKLLama (RK3588 NPU-accelerated inference) — RECOMMENDED for Orange Pi 5 Plus
  #
  # RKLLama runs as a native systemd service on the host (not in Docker) for
  # direct NPU device access. It implements the Ollama API on port 11434, so
  # RTPI connects via OLLAMA_HOST=http://localhost:11434 with zero changes.
  #
  # Setup:
  #   git clone https://github.com/notpunchnox/rkllama && cd rkllama
  #   pip install "packaging>=24.2" && pip install --no-build-isolation .
  #   rkllama_client pull qwen2.5:3b
  #   rkllama_client pull tinyllama:1.1b
  #   sudo cp scripts/rkllama.service /etc/systemd/system/
  #   sudo systemctl enable --now rkllama
  #
  # .env config:
  #   RKLLM_MODE=true
  #   PREFER_LOCAL_AI=true
  #   OLLAMA_HOST=http://localhost:11434
  #
  # The Ollama Docker services below are retained for non-RK3588 deployments.
  # They use Docker profiles and will NOT start unless explicitly activated.
  # ===========================================================================

  # Ollama (GPU-accelerated inference — for NVIDIA GPU deployments only)
  ollama:
    image: ollama/ollama:latest
    container_name: rtpi-ollama
    profiles:
      - gpu  # Only start if GPU is available (not used on RK3588)
    ports:
      - "11434:11434"
    volumes:
      - ollama-models:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
      - OLLAMA_ORIGINS=*
      - OLLAMA_NUM_PARALLEL=2
      - OLLAMA_MAX_LOADED_MODELS=2
      - OLLAMA_KEEP_ALIVE=30m  # Auto-unload after 30 min inactivity
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Ollama CPU Fallback (for systems without GPU or NPU)
  ollama-cpu:
    image: ollama/ollama:latest
    container_name: rtpi-ollama-cpu
    profiles:
      - cpu  # Only start if no GPU/NPU available (not used on RK3588)
    ports:
      - "11434:11434"
    volumes:
      - ollama-models:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
      - OLLAMA_ORIGINS=*
      - OLLAMA_NUM_PARALLEL=1  # Limit parallelism on CPU
      - OLLAMA_MAX_LOADED_MODELS=1
      - OLLAMA_KEEP_ALIVE=30m  # Auto-unload after 30 min inactivity
      - OLLAMA_NUM_THREAD=8  # CPU threads to use
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Open WebUI (LLM chat interface — connects to RKLLama on host)
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: rtpi-open-webui
    ports:
      - "3000:8080"
    volumes:
      - open-webui-data:/app/backend/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - WEBUI_AUTH=false
      - WEBUI_NAME=RTPI Local LLM (RKLLama NPU)
    networks:
      - rtpi-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # ===========================================================================
  # OffSec Agent Containers (v2.2 Enhancement)
  # Specialized security tool containers with MCP server integration
  # ===========================================================================

  # OffSec Base Image (built first, used by all agents)
  offsec-base:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.base
    image: rtpi/offsec-base:latest
    profiles:
      - build-only  # Only used for building, not running

  # Maldev Agent - Binary Analysis, Reverse Engineering, ROP Development
  # MITRE Tactics: Defense Evasion, Execution, Persistence
  offsec-maldev:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.maldev-tools
    image: rtpi/maldev-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-maldev-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=maldev
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
    volumes:
      - maldev-tools:/opt/tools
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # Azure-AD Agent - Azure & Active Directory Attack Research
  # MITRE Tactics: Credential Access, Persistence, Lateral Movement
  offsec-azure-ad:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.azure-ad-tools
    image: rtpi/azure-ad-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-azure-ad-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=azure-ad
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
    volumes:
      - azure-ad-tools:/opt/tools
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # Burp Suite Agent - Web Application Security Testing
  # MITRE Tactics: Reconnaissance, Initial Access
  offsec-burp:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.burp-tools
    image: rtpi/burp-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-burp-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=burp-suite
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
      - BURP_MCP_ENABLED=true
      - MCP_PORT=9876
      - ACTIVATION_MODE=dormant
    ports:
      - "9876:9876"   # Burp MCP Server
      - "8283:8082"   # Burp Proxy (when active) — avoids 8182 conflict with kasm-share
    volumes:
      - burp-tools:/opt/tools
      - /mnt/nvme/tmp/burp-setup:/opt/burp-setup
      - burp-projects:/opt/burp-projects
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f burp-entrypoint || true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # Empire C2 Agent - Command & Control, Post-Exploitation
  # MITRE Tactics: Command and Control, Lateral Movement, Persistence
  offsec-empire:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.empire-tools
    image: rtpi/empire-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-empire-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=empire-c2
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
    volumes:
      - empire-tools:/opt/tools
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # C3 C2 Framework - ReversecLabs
  offsec-c3:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.c3-tools
    image: rtpi/c3-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-c3-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=c3-c2
      - TOOLS_PATH=/opt/tools
    volumes:
      - c3-tools:/opt/tools
      - c2-setup:/opt/c2-setup
      - shared-data:/shared
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true

  # Sliver C2 Framework - BishopFox
  offsec-sliver:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.sliver-tools
    image: rtpi/sliver-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-sliver-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=sliver-c2
      - TOOLS_PATH=/opt/tools
    volumes:
      - sliver-tools:/opt/tools
      - c2-setup:/opt/c2-setup
      - shared-data:/shared
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true

  # Loki C2 Framework - boku7
  offsec-loki:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.loki-tools
    image: rtpi/loki-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-loki-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=loki-c2
      - TOOLS_PATH=/opt/tools
    volumes:
      - loki-tools:/opt/tools
      - c2-setup:/opt/c2-setup
      - shared-data:/shared
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true

  # AdaptixC2 Framework - Adaptix-Framework
  offsec-adaptix:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.adaptix-tools
    image: rtpi/adaptix-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-adaptix-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=adaptix-c2
      - TOOLS_PATH=/opt/tools
    volumes:
      - adaptix-tools:/opt/tools
      - c2-setup:/opt/c2-setup
      - shared-data:/shared
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true

  # Fuzzing Agent - Web Fuzzing, Directory Discovery
  # MITRE Tactics: Discovery, Reconnaissance
  offsec-fuzzing:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.fuzzing-tools
    image: rtpi/fuzzing-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-fuzzing-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=fuzzing
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
      - R2_ENDPOINT=${R2_ENDPOINT}
      - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
      - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
      - R2_BUCKET_NAME=${R2_BUCKET_NAME:-wordlist-storag}
    volumes:
      - fuzzing-tools:/opt/tools
      - fuzzing-wordlists:/opt/wordlists
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # Framework Security Agent - Technology Stack Detection
  # MITRE Tactics: Reconnaissance, Initial Access
  offsec-framework:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.framework-tools
    image: rtpi/framework-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-framework-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=framework-security
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
    volumes:
      - framework-tools:/opt/tools
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # Research Agent - General R&D, Tool Testing, POC Creation
  # MITRE Tactics: All (General Research)
  offsec-research:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.research-tools
    image: rtpi/research-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-research-agent
    stdin_open: true
    ports:
      - "8888:8888"  # JupyterLab for research notebooks
    environment:
      - AGENT_TYPE=research
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
      - R2_ENDPOINT=${R2_ENDPOINT}
      - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
      - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
      - R2_BUCKET_NAME=${R2_BUCKET_NAME:-wordlist-storag}
    volumes:
      - research-tools:/opt/tools
      - research-notebooks:/home/rtpi-agent/research
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

# Application (Development)
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile.dev
#    container_name: rtpi-app
#    ports:
#      - "3000:3000"
#      - "5000:5000"
#    environment:
#      NODE_ENV: development
#      DATABASE_URL: postgresql://rtpi:rtpi@postgres:5432/rtpi_main
#      REDIS_URL: redis://redis:6379
#      PORT: 3000
#    volumes:
#      - .:/app
#      - /app/node_modules
#    depends_on:
#      postgres:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#    networks:
#      - rtpi-network

  # ===========================================================================
  # SysReptor - Penetration Testing Documentation & Reporting
  # Usage: docker compose --profile sysreptor up -d
  # Access: http://localhost:7777 (via Caddy proxy)
  # ===========================================================================

  sysreptor-app:
    image: syslifters/sysreptor:2025.37
    container_name: rtpi-sysreptor-app
    profiles:
      - sysreptor
    restart: unless-stopped
    ports:
      - "9005:8000"  # SysReptor API/app — exposed on all interfaces (operator-served from 0.0.0.0). 9000 is reserved by minio.
    volumes:
      - sysreptor-app-data:/app/data
      - ./configs/rtpi-sysreptor/app.env:/app/.env:ro
    depends_on:
      postgres:
        condition: service_healthy
      sysreptor-redis:
        condition: service_healthy
    command: /bin/bash /app/api/start.sh
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  sysreptor-caddy:
    image: caddy:2.8
    container_name: rtpi-sysreptor-caddy
    profiles:
      - sysreptor
    restart: unless-stopped
    ports:
      - "7777:7777"  # Primary SysReptor access via Caddy proxy
    volumes:
      - sysreptor-caddy-data:/data
      - ./configs/rtpi-sysreptor/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      - sysreptor-app
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  sysreptor-redis:
    image: bitnami/redis:latest
    container_name: rtpi-sysreptor-redis
    profiles:
      - sysreptor
    restart: unless-stopped
    environment:
      - REDIS_PASSWORD=${SYSREPTOR_REDIS_PASSWORD:-sysreptorredispassword}
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD", "sh", "-c", "redis-cli -a \"$$REDIS_PASSWORD\" ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # vLLM — Qwen3.5-9B serving (v2.9.1 Phase 6, RTPI_INFERENCE_PROVIDER=vllm)
  # OpenAI-compatible inference for agentic workloads on Blackwell-class GPUs.
  # Tool-call parsing happens server-side via --tool-call-parser qwen3_coder;
  # the orchestrator's mcp-invoker (Phase 5) handles MCP dispatch.
  # Usage: docker compose --profile vllm up -d
  # Set VLLM_BASE_URL=http://rtpi-vllm:8000 in app env.
  # NOTE: For Blackwell sm_120 (RTX 5060 Ti) the published nightly may need
  # a source rebuild — see docs/enhancements/2.9/references/Inference-Agent-Architecture.md.
  # ===========================================================================

  vllm-qwen:
    image: vllm/vllm-openai:nightly
    container_name: rtpi-vllm
    profiles:
      - vllm
    restart: unless-stopped
    ports:
      - "127.0.0.1:18000:8000"  # OpenAI-compatible API (localhost only)
    volumes:
      - vllm-hf-cache:/root/.cache/huggingface
    environment:
      - HF_HOME=/root/.cache/huggingface
      - VLLM_LOGGING_LEVEL=${VLLM_LOGGING_LEVEL:-INFO}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    # Command flags follow Inference-Agent-Architecture.md:
    # - reasoning-parser qwen3 enables thinking/non-thinking switching
    # - tool-call-parser qwen3_coder + enable-auto-tool-choice activates the
    #   XML-format tool calling Qwen3.5 was trained for
    # - language-model-only skips the vision encoder to save ~1GB VRAM
    # - speculative-config enables Multi-Token Prediction for ~3-5x decoding
    command:
      - "--model=${VLLM_MODEL:-Qwen/Qwen3.5-9B}"
      - "--port=8000"
      - "--tensor-parallel-size=1"
      - "--max-model-len=131072"
      - "--reasoning-parser=qwen3"
      - "--enable-auto-tool-choice"
      - "--tool-call-parser=qwen3_coder"
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # docker-socket-proxy (v2.9.1 Phase 10 — KASM hardening)
  # When FF_KASM_HARDENED=true, KASM containers and the orchestrator's nested-
  # container helper route Docker calls through this proxy instead of mounting
  # /var/run/docker.sock directly. The proxy enforces a verb allowlist so a
  # compromised KASM host can't, e.g., create networks or destroy volumes.
  # Allowed: containers (read+write+exec) + images (read) + info.
  # Denied:  networks, volumes, services, swarm, tasks, system, build.
  # Usage: docker compose --profile kasm up -d (already brings KASM stack)
  # ===========================================================================

  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:latest
    container_name: rtpi-docker-socket-proxy
    profiles:
      - kasm
    restart: unless-stopped
    ports:
      - "127.0.0.1:2375:2375"  # localhost only — KASM containers reach via rtpi-network
    environment:
      # Verb allowlist (1=allow, 0=deny). Matches Phase 10 acceptance test:
      # `docker -H tcp://… network create` must return 403.
      - CONTAINERS=1
      - EXEC=1
      - IMAGES=1
      - INFO=1
      - PING=1
      - VERSION=1
      - NETWORKS=0
      - VOLUMES=0
      - SERVICES=0
      - SWARM=0
      - TASKS=0
      - SYSTEM=0
      - BUILD=0
      - COMMIT=0
      - CONFIGS=0
      - DISTRIBUTION=0
      - PLUGINS=0
      - SECRETS=0
      - SESSION=0
      - POST=1   # required for /containers/:id/exec
      - DELETE=1 # required for nested-container teardown
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:2375/_ping || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # Docmost — Team documentation (v2.9.1 Phase 8)
  # Wiki-style docs platform; reports can be published here as pages.
  # Usage: docker compose --profile docmost up -d
  # Access: http://localhost:13000
  # First-run setup creates the initial workspace + admin user.
  # ===========================================================================

  docmost:
    image: docmost/docmost:latest
    container_name: rtpi-docmost
    profiles:
      - docmost
    restart: unless-stopped
    ports:
      - "127.0.0.1:13000:3000"
    environment:
      - APP_URL=${DOCMOST_APP_URL:-http://localhost:13000}
      - APP_SECRET=${DOCMOST_APP_SECRET:-change-this-to-a-random-32-byte-secret}
      - DATABASE_URL=postgres://docmost:${DOCMOST_DB_PASSWORD:-docmostpassword}@postgres:5432/docmost
      - REDIS_URL=redis://default:${DOCMOST_REDIS_PASSWORD:-docmostredispassword}@docmost-redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      docmost-redis:
        condition: service_healthy
    volumes:
      - docmost-data:/app/data/storage
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  docmost-redis:
    image: bitnami/redis:latest
    container_name: rtpi-docmost-redis
    profiles:
      - docmost
    restart: unless-stopped
    environment:
      - REDIS_PASSWORD=${DOCMOST_REDIS_PASSWORD:-docmostredispassword}
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD", "sh", "-c", "redis-cli -a \"$$REDIS_PASSWORD\" ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # Chromium Headless Shell (v2.9.1 Phase 3 — app-side PDF generation)
  # Small alpine-based Chrome devtools-protocol endpoint used by the
  # documentRenderer service. Bind 127.0.0.1 only — never exposed externally.
  # Usage: docker compose --profile pdf up -d
  # Set CHROMIUM_WS_ENDPOINT=ws://rtpi-chromium-shell:9222 in app env.
  # ===========================================================================

  chromium-shell:
    image: chromedp/headless-shell:latest
    container_name: rtpi-chromium-shell
    profiles:
      - pdf
    restart: unless-stopped
    # Defaults expose CDP on :9222 with no auth — keep on the rtpi-network only.
    ports:
      - "127.0.0.1:9222:9222"
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    # Hardening: read-only rootfs, no new privileges, drop capabilities.
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp:size=256m,exec
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9222/json/version"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # Portainer - Container Management UI
  # Usage: docker compose --profile management up -d
  # Access: https://localhost:9443
  # First login sets admin password
  # ===========================================================================

  portainer:
    image: portainer/portainer-ce:latest
    container_name: rtpi-portainer
    profiles:
      - management
    restart: unless-stopped
    ports:
      - "9443:9443"  # HTTPS UI
      - "8000:8000"  # Edge agent tunnel (optional)
    volumes:
      - portainer-data:/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    healthcheck:
      test: ["CMD", "curl", "-f", "-k", "https://localhost:9443/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # Kasm Containerized Workspaces (optional — adds pre-built desktop images)
  # Requires the kasm profile: docker compose --profile kasm up -d
  # VS Code: http://localhost:6901
  # Kali: http://localhost:6902
  # Default VNC password: password  (change via VNC_PW env var)
  # ===========================================================================

  kasm-vscode:
    image: kasmweb/vs-code:1.16.0
    container_name: rtpi-kasm-vscode
    profiles:
      - kasm
    platform: linux/amd64
    restart: unless-stopped
    ports:
      - "6901:6901"  # VS Code web interface
    volumes:
      - kasm-vscode-data:/home/kasm-user
    environment:
      - VNC_PW=${KASM_VNC_PASSWORD:-password}
      - KASM_SVC_AUDIO=0
      - KASM_SVC_UPLOADS=1
      - KASM_SVC_DOWNLOADS=1
      - DISABLE_SSL=true
      - KASM_SVC_FORCE_HTTPS=0
    shm_size: "512m"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - rtpi-network
    # The kasm desktop image always returns 401 (auth required) on every
    # endpoint, so `curl -f` (fail on >=400) reports unhealthy even when the
    # server is up. Treat any 2xx/3xx/4xx response as healthy.
    healthcheck:
      test: ["CMD-SHELL", "curl -sk -o /dev/null -w '%{http_code}' --max-time 5 https://localhost:6901/ | grep -qE '^[234]'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  kasm-kali:
    image: kasmweb/kali-rolling-desktop:1.16.0
    container_name: rtpi-kasm-kali
    profiles:
      - kasm
    platform: linux/amd64
    restart: unless-stopped
    ports:
      - "6902:6901"  # Kali web interface (mapped to different host port)
    volumes:
      - kasm-kali-data:/home/kasm-user
    environment:
      - VNC_PW=${KASM_VNC_PASSWORD:-password}
      - KASM_SVC_AUDIO=1
      - KASM_SVC_UPLOADS=1
      - KASM_SVC_DOWNLOADS=1
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - rtpi-network
    # See note on kasm-vscode — same 401-on-everything behavior.
    healthcheck:
      test: ["CMD-SHELL", "curl -sk -o /dev/null -w '%{http_code}' --max-time 5 https://localhost:6901/ | grep -qE '^[234]'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================================================
  # v2.5 — mem0 Memory Infrastructure
  # ============================================================================

  # pgvector for mem0 vector store (separate from main postgres)
  mem0-postgres:
    image: pgvector/pgvector:pg16
    container_name: rtpi-mem0-postgres
    environment:
      POSTGRES_DB: rtpi_memory
      POSTGRES_USER: rtpi
      POSTGRES_PASSWORD: "${MEM0_POSTGRES_PASSWORD:-rtpi_mem0}"
    ports:
      - "5435:5432"
    volumes:
      - mem0-pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rtpi -d rtpi_memory"]
      interval: 10s
      retries: 5
    networks:
      - rtpi-network

  # Neo4j for mem0 graph store
  neo4j:
    image: neo4j:5-community
    container_name: rtpi-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: "neo4j/${NEO4J_PASSWORD:-rtpi_neo4j}"
      NEO4J_PLUGINS: '["apoc"]'
    volumes:
      - neo4j-data:/data
      - neo4j-logs:/logs
    healthcheck:
      test: ["CMD", "neo4j", "status"]
      interval: 10s
      retries: 5
    networks:
      - rtpi-network

  # mem0 API Service
  mem0-api:
    build:
      context: ./services/mem0
      dockerfile: Dockerfile
    container_name: rtpi-mem0-api
    profiles:
      - mem0
    ports:
      - "8005:8000"  # Avoids conflict with portainer edge agent on 8000
    environment:
      MEM0_LLM_PROVIDER: ollama
      MEM0_LLM_MODEL: qwen2.5-coder:7b
      MEM0_LLM_BASE_URL: http://ollama:11434
      MEM0_EMBEDDER_PROVIDER: ollama
      MEM0_EMBEDDER_MODEL: nomic-embed-text
      MEM0_EMBEDDER_BASE_URL: http://ollama:11434
      MEM0_VECTOR_HOST: mem0-postgres
      MEM0_VECTOR_PORT: 5432
      MEM0_VECTOR_DBNAME: rtpi_memory
      MEM0_VECTOR_USER: rtpi
      MEM0_VECTOR_PASSWORD: "${MEM0_POSTGRES_PASSWORD:-rtpi_mem0}"
      MEM0_GRAPH_URL: bolt://neo4j:7687
      MEM0_GRAPH_USER: neo4j
      MEM0_GRAPH_PASSWORD: "${NEO4J_PASSWORD:-rtpi_neo4j}"
    depends_on:
      mem0-postgres:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      ollama:
        condition: service_started
        required: false
    networks:
      - rtpi-network

  # ============================================================================
  # v2.5 — LangGraph Orchestrator
  # ============================================================================

  rtpi-orchestrator:
    build:
      context: ./services/orchestrator
      dockerfile: Dockerfile
    container_name: rtpi-orchestrator
    ports:
      - "8185:8080"
    volumes:
      - ./skills:/app/skills:ro
      - tool-results:/app/findings
      - shared-data:/app/samples
      - /var/run/docker.sock:/var/run/docker.sock:ro  # Docker exec into tool containers
    environment:
      MEM0_API_URL: http://mem0-api:8000
      OLLAMA_BASE_URL: http://ollama:11434
      METASPLOIT_HOST: attackbox
      REDIS_URL: redis://redis:6379
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY:-}"
      # Container names for tool execution (agent-to-container mapping)
      CONTAINER_RECON_PRIMARY: rtpi-fuzzing-agent
      CONTAINER_RECON_FINGERPRINT: rtpi-framework-agent
      CONTAINER_RECON_NETWORK: rtpi-tools
      CONTAINER_WEB_VULN: rtpi-burp-agent
      CONTAINER_AD_VULN: rtpi-azure-ad-agent
      CONTAINER_CLOUD_VULN: rtpi-cloud-agent
      CONTAINER_EXPLOIT_CORE: rtpi-tools
      CONTAINER_EXPLOIT_BINARY: rtpi-maldev-agent
      CONTAINER_EXPLOIT_PAYLOAD: rtpi-empire-agent
      CONTAINER_POSTEX_PRIMARY: rtpi-empire-agent
      CONTAINER_POSTEX_SLIVER: rtpi-sliver-agent
      CONTAINER_POSTEX_C3: rtpi-c3-agent
      CONTAINER_POSTEX_LOKI: rtpi-loki-agent
      CONTAINER_POSTEX_ADAPTIX: rtpi-adaptix-agent
      CONTAINER_RE_PRIMARY: rtpi-maldev-agent
      CONTAINER_RE_GHIDRA: rtpi-ghidra
      CONTAINER_RE_FUZZING: rtpi-aflplusplus
      CONTAINER_CODEREVIEW: rtpi-framework-agent
      CONTAINER_VULNRESEARCH: rtpi-research-agent
      CONTAINER_LLM_SECURITY: rtpi-llm-sec-agent
    depends_on:
      mem0-api:
        condition: service_started
        required: false
      redis:
        condition: service_started
      ollama:
        condition: service_started
        required: false
    networks:
      - rtpi-network

  # ============================================================================
  # v2.5 — Cloud Security Tools (NEW — fills AWS/GCP gap)
  # ============================================================================

  offsec-cloud:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.cloud-tools
    image: rtpi/cloud-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-cloud-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=cloud-security
      - TOOLS_PATH=/opt/tools
      - DOCS_PATH=/docs
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
    volumes:
      - cloud-tools:/opt/tools
      - shared-data:/shared
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # ============================================================================
  # v2.5 — LLM Security Tools (activated from unmapped Dockerfile)
  # ============================================================================

  offsec-llm-sec:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.agent-tools-llm-sec-tools
    image: rtpi/llm-sec-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-llm-sec-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=llm-security
      - TOOLS_PATH=/opt/tools
    volumes:
      - shared-data:/shared
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 60s
      timeout: 10s
      retries: 3

  # ============================================================================
  # v2.5 — Web Injection Tools (activated, merged into web vuln pipeline)
  # ============================================================================

  offsec-web-injection:
    build:
      context: ./docker/offsec-agents
      dockerfile: Dockerfile.agent-tools-web-injection
    image: rtpi/web-injection-tools:latest
    profiles:
      - offsec-agents
    container_name: rtpi-web-injection-agent
    stdin_open: true
    environment:
      - AGENT_TYPE=web-injection
      - TOOLS_PATH=/opt/tools
    volumes:
      - shared-data:/shared
    networks:
      - rtpi-network
    restart: unless-stopped
    init: true

  # ============================================================================
  # v2.5 — RE Tool Containers (Phase C)
  # Ghidra & AFL++ are NOT redundant — maldev has radare2/angr/pwntools
  # but lacks headless Ghidra decompilation and coverage-guided fuzzing
  # ============================================================================

  ghidra-headless:
    build:
      context: ./services/ghidra
      dockerfile: Dockerfile
    container_name: rtpi-ghidra
    networks:
      - rtpi-network
    volumes:
      - shared-data:/samples
      - tool-results:/findings
    entrypoint: ["tail", "-f", "/dev/null"]

  aflplusplus:
    image: aflplusplus/aflplusplus:latest
    container_name: rtpi-aflplusplus
    networks:
      - rtpi-network
    volumes:
      - shared-data:/samples
      - tool-results:/findings
    entrypoint: ["tail", "-f", "/dev/null"]

  # ===========================================================================
  # Bootstrap — one-shot secret generator (profile: bootstrap)
  # Runs scripts/bootstrap-secrets.sh inside a minimal Alpine container.
  # Usage: docker compose --profile bootstrap run --rm rtpi-bootstrap
  # ===========================================================================
  rtpi-bootstrap:
    image: alpine:3.20
    container_name: rtpi-bootstrap
    profiles:
      - bootstrap
    volumes:
      - .:/workspace
      - ${HOME}/.rtpi:/root/.rtpi
    working_dir: /workspace
    environment:
      HOME: /root
    entrypoint:
      - sh
      - -c
      - "apk add --no-cache bash openssl util-linux coreutils >/dev/null && bash scripts/bootstrap-secrets.sh \"$@\""
      - --

  # ===========================================================================
  # VPN Manager (v2.9.2 Phase 1 — VPN Infrastructure Integration)
  # Long-running container that owns OpenVPN / WireGuard tunnel interfaces on
  # behalf of RTPI. The Phase 2 backend service writes uploaded .ovpn / .conf
  # files into the mounted vpn-configs / wg-configs volumes, then triggers
  # connect/disconnect via `docker exec rtpi-vpn-manager vpn-entrypoint.sh ...`.
  # Usage: docker compose --profile vpn up -d
  # Requires the host kernel to expose /dev/net/tun and the `wireguard` module.
  # ===========================================================================

  vpn-manager:
    build:
      context: ./docker/vpn-manager
      dockerfile: Dockerfile
    container_name: rtpi-vpn-manager
    profiles:
      - vpn
    restart: unless-stopped
    # Required to manage tun/wg interfaces and program iptables rules from
    # inside the container. SYS_MODULE is *not* added — the host loads the
    # wireguard kernel module; the container only uses the userspace tools.
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    volumes:
      - vpn-configs:/etc/openvpn/configs
      - wg-configs:/etc/wireguard/configs
      - vpn-logs:/var/log/vpn
    networks:
      - rtpi-network
    dns:
      - 127.0.0.11
    healthcheck:
      test: ["CMD", "/usr/local/bin/vpn-health-check.sh"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  rtpi-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  tool-results:
  tool-configs:
  shared-data:
  empire-data:
  empire-downloads:
  workbench-db-data:
  # Kasm Workspaces volumes
  kasm-db-data:
  kasm-db-certs:
  kasm-redis-data:
  kasm-api-data:
  kasm-manager-data:
  kasm-proxy-config:
  kasm-guac-data:
  kasm-agent-data:
  kasm-share-data:
  kasm-certs:
  certbot-conf:
  certbot-www:
  # LLM / AI volumes
  ollama-models:
  open-webui-data:
  # Phase 6: Hugging Face cache for vLLM-served Qwen weights
  vllm-hf-cache:
  # OffSec Agent volumes (v2.2)
  maldev-tools:
  azure-ad-tools:
  burp-tools:
  burp-setup:
  burp-projects:
  empire-tools:
  c3-tools:
  sliver-tools:
  loki-tools:
  adaptix-tools:
  c2-setup:
  fuzzing-tools:
  fuzzing-wordlists:
  framework-tools:
  research-tools:
  research-notebooks:
  # SysReptor volumes
  sysreptor-app-data:
  sysreptor-caddy-data:
  # Docmost storage volume (v2.9.1 Phase 8)
  docmost-data:
  # Portainer volume
  portainer-data:
  # Kasm containerized workspace volumes
  kasm-vscode-data:
  kasm-kali-data:
  # v2.5 — mem0 & orchestrator volumes
  mem0-pg-data:
  neo4j-data:
  neo4j-logs:
  skills-data:
  cloud-tools:
  # v2.9.2 Phase 1 — VPN manager config + log volumes
  vpn-configs:
  wg-configs:
  vpn-logs:
