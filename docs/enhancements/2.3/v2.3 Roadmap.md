# Meta-Instruction
Under each header are the things that need to be fixed or features that need to be created, as well as the conditions for success. This document serves as scaffolding for the complete plan which will be detailed by Claude at the time of implementation. All implementation will document every step completed and follow a rigorous checklist, to prevent loss of development situational awareness.

If an Agent does not yet exist, create it and develop develop its necessary capabilities and workflow application routes. .

Reporting Agents are not presented through the Operational UI, all Reporting Agents will be managed through the Administration page and deliver all output through the Operations Manager Agent.

---

# v2.3 Implementation Status Summary (as of 2026-03-04)

| Phase | Description | Status | Plan Doc |
|-------|-------------|--------|----------|
| Phase 1 | Memory System Foundation (Mem0) | **DONE** | v2.3.1 |
| Phase 2 | Agent System Architecture | **DONE** (UI pending) | v2.3.2 |
| Phase 3 | Operations Management Automation | **IN PROGRESS** | v2.3.3 |
| Phase 4 | UI/UX Reorganization | **IN PROGRESS** | v2.3.4 |
| Phase 5 | Framework Integration | **DONE** (SysReptor, 3D topology pending) | Roadmap |
| Phase 6 | Vulnerability Investigation Workflow | **PLANNED** | v2.3.6.1 |
| -- | RTPI Tools | **DONE** | Roadmap |
| -- | Ollama AI Integration | **DONE** | Roadmap |
| -- | Infrastructure Monitoring | **NOT STARTED** | Roadmap |
| -- | Administration | **NOT STARTED** | Roadmap |

### Key Implementation Files
- Memory: `server/config/mem0-config.ts`, `server/services/memory-service.ts`, `server/api/v1/memory.ts`
- Agents: `server/services/operations-manager-agent.ts`, `server/services/reporter-agent-service.ts`, `server/services/agent-message-bus.ts`
- Task Agents: `server/services/agents/{base-task,vulnerability,target,qa,technical-writer,surface-assessment,web-hacker,tool-connector}-agent.ts`
- Automation: `server/services/target-auto-creation-service.ts`, `server/services/operation-lifecycle-automation.ts`
- Scheduling: `server/services/ops-manager-scheduler.ts`, `server/services/reporters/{base,operations}-reporter.ts`

---

# Troubleshooting
- Review the images within the ts-img folder, "[issue-name]/image"
    - Utilize all agents and tools until the issue is completely repaired


# v2.3 Objectives (UI-UX = Zero-Wasted Actions) [DONE — Mem0 selected and implemented]
Research complete. Mem0 was selected as the memory framework (best overall: dual vector+graph, 91% lower latency, 46.8k GitHub stars, LangGraph integration). See `v2.3.1-memory-system-foundation.md` for full analysis.

Implementation: `server/config/mem0-config.ts`, `server/services/memory-service.ts`, `server/api/v1/memory.ts`
Schema: 4 tables in `shared/schema.ts` — memoryContexts, memoryEntries, memoryRelationships, memoryAccessLogs

Also consider the framework in the [2.4 Roadmap](/home/cmndcntrl/code/rtpi/docs/enhancements/2.4/v2.4 Roadmap.md) when making a determination.

[1](https://github.com/cmndcntrlcyber/mem0)

[2](https://github.com/topoteretes/cognee)

[3](https://github.com/MemoriLabs/Memori)

[4](https://github.com/NevaMind-AI/memU)

[5](https://github.com/letta-ai/agent-file)

# Status Reporting [DONE]
## Roles
- Operations Manager Agent [DONE] — `server/services/operations-manager-agent.ts`
  - Delegates tasks according to operational need
  - Agent Message Bus: `server/services/agent-message-bus.ts` + `server/api/v1/agent-messages.ts`
  - Scheduling: `server/services/ops-manager-scheduler.ts`
  - Operations Management API: `server/api/v1/operations-management.ts`
- (Specialty) Task Agent [DONE] — `server/services/agents/base-task-agent.ts`
  - Completes the Task
  - Implementations: vulnerability-agent, target-agent, qa-agent, technical-writer-agent, surface-assessment-agent, web-hacker-agent, tool-connector-agent
- (Specialty) Reporter Agent [DONE] — `server/services/reporter-agent-service.ts`
  - Reports the Task as completed to Operations Manager
  - Reporter API: `server/api/v1/reporters.ts`
  - Base reporter framework: `server/services/reporters/base-reporter.ts`, `server/services/reporters/operations-reporter.ts`
  - [REMAINING] Reporter Agents UI in Administration page

Also consider the framework in the [2.4 Roadmap](/home/cmndcntrl/code/rtpi/docs/enhancements/2.4/v2.4 Roadmap.md) when making a determination for how to develop these roles  
  
# New Directory Structure
## Group Side-Panel Objects Based on Features and Nature
[REQUIRED!!!]: ENSURE ALL ROUTES AND LINKS ARE FULLY OPERATIONAL AFTER EACH CHANGE. CHECK ALL LINKS AND ROUTES 3 TIMES: 

1. AFTER CHANGES ARE MADE 
2. AFTER A PARENT SECTION IS COMPLETED 
3. AFTER ALL PAGES HAVE BEEN REORGANIZED AND CONSOLIDATED.

All link relationships should be retained throughout development.  

NO BROKEN LINKS!!! 

All pages UI-UX should behave as they do now unless explicitly instructed otherwise

# Dashboard

## Operations [IN PROGRESS]
### Operations Manager (Incorporated into Operations Page) [IN PROGRESS]
- Integrate the "Operations Manager" functions and features within the Operations Page
    - [NOT STARTED] a floating 2x2" square chat window for user input (planned in v2.3.4 Step 4.2)
    - [IN PROGRESS] Create a .tsx file that triggers default "Surface Assessment" scans and ATT&CK Reconnaissance against the "in-Scope" targets of an operation
        - [DONE] Trigger activates when the operation is submitted, switched to "Active" and "Active is confirmed by human operator — `server/services/operation-lifecycle-automation.ts`
        - [DONE] Findings are stored in `Discovered Services (JSON)`
        - [IN PROGRESS] Replace `Notes/Metadata (JSON)` fields with `BBOT OUTPUT (JSON)`
            - [DONE] Target Reporter Agent will read the output of the BBOT Scan — `server/services/agents/target-agent.ts`
            - [IN PROGRESS] Operations Manager will send data and tasks to Relevant Offsec R&D Team Agents
        - Target Tags include name of Client/Customer, Name, & Type

### Targets [DONE]
- Operations Manager Agent [DONE]
    - [DONE] Creates target (name,type,value) at the time of operation activation — `server/services/target-auto-creation-service.ts`
    - [DONE] Dispatches Task for Default Surface Assessment
- Target Reporter Agent [DONE] — `server/services/agents/target-agent.ts`
    - [DONE] Send All New Target Data to Operations Manager

### Vulnerabilities [IN PROGRESS]
- Operations Manager Agent [DONE]
  - Assigns tasks to vulnerability-agent

- Vulnerabilities Reporter Agent [DONE] — `server/services/agents/vulnerability-agent.ts`

### Surface Assessment [IN PROGRESS]
- Operations Manager Agent [DONE]
    - [DONE] Trigger activates when the operation is submitted, switched to "Active" and "Active is confirmed by human operator — `server/services/operation-lifecycle-automation.ts`
- Surface Assessment Reporter Agent [DONE] — `server/services/agents/surface-assessment-agent.ts`
    - [IN PROGRESS] Takes json output of BBOT, Nuclei and NMAP — BBOT/Nuclei done, Nmap async pending

#### Overview Section [DONE]

[DONE] Replace the two elements that say "Most Vulnerable Assets" & "Recent Activity" and in that space create an element to upload a burpsuite pro .jar file — `client/src/components/surface-assessment/BurpSuiteUploadCard.tsx` + `server/api/v1/burp-builder.ts`


```
// Find the sections to replace by their headings
const headings = document.querySelectorAll('h3, h2, h1');
let mostVulnerableCard = null;
let recentActivityCard = null;

headings.forEach(heading => {
  const text = heading.textContent;
  if (text && text.includes('Most Vulnerable Assets')) {
    // Go up until we find the card container
    let parent = heading.parentElement;
    while (parent && !parent.className.includes('bg-card')) {
      parent = parent.parentElement;
    }
    mostVulnerableCard = parent;
  }
  if (text && text.includes('Recent Activity')) {
    // Go up until we find the card container
    let parent = heading.parentElement;
    while (parent && !parent.className.includes('bg-card')) {
      parent = parent.parentElement;
    }
    recentActivityCard = parent;
  }
});

// Get the parent container that holds both cards
const parentContainer = mostVulnerableCard ? mostVulnerableCard.parentElement : null;

// Create the new BurpSuite upload component
const burpUploadCard = document.createElement('div');
burpUploadCard.className = 'bg-card p-6 rounded-lg shadow-sm border border-border col-span-2';
burpUploadCard.innerHTML = `
  <div class="flex items-center justify-between mb-6">
    <div>
      <h3 class="text-lg font-semibold mb-2">BurpSuite Pro Integration</h3>
      <p class="text-sm text-muted-foreground">Upload BurpSuite Pro .jar file to embed into the application</p>
    </div>
  </div>
  
  <div class="space-y-4">
    <div class="border-2 border-dashed border-border rounded-lg p-8 text-center hover:border-primary transition-colors" id="burp-upload-zone">
      <svg class="mx-auto h-12 w-12 text-muted-foreground mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
      </svg>
      <div class="mb-4">
        <label for="burp-jar-input" class="cursor-pointer">
          <span class="text-primary font-medium hover:underline">Click to upload</span>
          <span class="text-muted-foreground"> or drag and drop</span>
        </label>
        <input type="file" id="burp-jar-input" accept=".jar" class="hidden" />
      </div>
      <p class="text-xs text-muted-foreground">JAR files only (BurpSuite Pro)</p>
    </div>
    
    <div id="burp-upload-status" class="hidden">
      <div class="flex items-center p-4 bg-secondary rounded-lg">
        <svg class="h-5 w-5 text-primary mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <div class="flex-1">
          <p class="text-sm font-medium" id="burp-filename">No file selected</p>
          <p class="text-xs text-muted-foreground" id="burp-filesize"></p>
        </div>
        <button class="ml-4 px-4 py-2 bg-primary text-primary-foreground rounded-md hover:bg-primary/90 transition-colors" id="burp-embed-btn">
          Embed BurpSuite
        </button>
      </div>
    </div>
    
    <div id="burp-progress" class="hidden">
      <div class="w-full bg-secondary rounded-full h-2">
        <div class="bg-primary h-2 rounded-full transition-all" style="width: 0%" id="burp-progress-bar"></div>
      </div>
      <p class="text-xs text-muted-foreground mt-2 text-center" id="burp-progress-text">Processing...</p>
    </div>
  </div>
`;

// Remove the old sections and insert the new component
if (mostVulnerableCard && recentActivityCard && parentContainer) {
  // Insert the new card before removing old ones
  mostVulnerableCard.parentElement.insertBefore(burpUploadCard, mostVulnerableCard);
  
  // Remove old sections
  mostVulnerableCard.remove();
  recentActivityCard.remove();
  
  // Add event listeners for file upload
  const fileInput = document.getElementById('burp-jar-input');
  const uploadZone = document.getElementById('burp-upload-zone');
  const statusDiv = document.getElementById('burp-upload-status');
  const filenameSpan = document.getElementById('burp-filename');
  const filesizeSpan = document.getElementById('burp-filesize');
  const embedBtn = document.getElementById('burp-embed-btn');
  const progressDiv = document.getElementById('burp-progress');
  const progressBar = document.getElementById('burp-progress-bar');
  const progressText = document.getElementById('burp-progress-text');
  
  let selectedFile = null;
  
  // File input change handler
  fileInput.addEventListener('change', (e) => {
    const file = e.target.files[0];
    if (file && file.name.endsWith('.jar')) {
      selectedFile = file;
      filenameSpan.textContent = file.name;
      filesizeSpan.textContent = `${(file.size / 1024 / 1024).toFixed(2)} MB`;
      statusDiv.classList.remove('hidden');
      uploadZone.classList.add('hidden');
    }
  });
  
  // Drag and drop functionality
  uploadZone.addEventListener('dragover', (e) => {
    e.preventDefault();
    uploadZone.style.borderColor = 'var(--primary)';
  });
  
  uploadZone.addEventListener('dragleave', (e) => {
    e.preventDefault();
    uploadZone.style.borderColor = '';
  });
  
  uploadZone.addEventListener('drop', (e) => {
    e.preventDefault();
    uploadZone.style.borderColor = '';
    const file = e.dataTransfer.files[0];
    if (file && file.name.endsWith('.jar')) {
      selectedFile = file;
      filenameSpan.textContent = file.name;
      filesizeSpan.textContent = `${(file.size / 1024 / 1024).toFixed(2)} MB`;
      statusDiv.classList.remove('hidden');
      uploadZone.classList.add('hidden');
    }
  });
  
  // Embed button click handler
  embedBtn.addEventListener('click', () => {
    if (selectedFile) {
      statusDiv.classList.add('hidden');
      progressDiv.classList.remove('hidden');
      
      // Simulate upload progress (replace with actual upload logic)
      let progress = 0;
      const interval = setInterval(() => {
        progress += 10;
        progressBar.style.width = progress + '%';
        progressText.textContent = `Embedding BurpSuite Pro... ${progress}%`;
        
        if (progress >= 100) {
          clearInterval(interval);
          progressText.textContent = 'BurpSuite Pro embedded successfully!';
          
          // TODO: Add your actual file upload/embedding logic here
          // Example: Send file to backend endpoint
          // const formData = new FormData();
          // formData.append('burpsuite', selectedFile);
          // fetch('/api/embed-burpsuite', { method: 'POST', body: formData });
          
          setTimeout(() => {
            // Reset to initial state
            progressDiv.classList.add('hidden');
            uploadZone.classList.remove('hidden');
            fileInput.value = '';
            selectedFile = null;
            progressBar.style.width = '0%';
          }, 2000);
        }
      }, 300);
    }
  });
}

console.log('BurpSuite upload component created successfully');
```

#### Vulnerabilities [PLANNED - See v2.3.6.1]
- **[New Workflow] BBOT Finding/Vulnerability Investigation & Validation** — See `v2.3.6.1-vulnerability-investigation-workflow.md`
  
  **Overview:**
  - All BBOT `[FINDING]` and `[VULN]` outputs saved with severity classification
  - Medium+ severity automatically triggers investigation workflow
  - LOW severity findings evaluated/deleted manually
  
  **Workflow:**
  1. BBOT scan completes → Parser extracts [FINDING]/[VULN] tags with severity
  2. Vulnerability Reporter Agent filters Medium+ findings
  3. Operations Manager receives report and delegates tasks:
     - BurpSuite Orchestrator Agent → Active investigation via MCP
     - R&D Team Agent → Generate Nuclei template (if missing)
  4. Validation results stored in vulnerability record
  5. Investigation status updated and visible in UI
  
  **Key Components:**
  - **BurpSuite Orchestrator Agent** (uses existing rtpi-burp-agent)
    - Dormant by default until activated
    - Dual-upload activation (JAR + license required)
    - Connects to Burp Pro MCP Server for investigations
    - Reports blockers to Operations Manager
  - **R&D Team Agent** (new)
    - CVE research using Tavily MCP
    - AI-powered Nuclei template generation
    - Template deployment and validation
    - Reports blockers to Operations Manager
  
  **Documentation:**
  - Main plan: `v2.3.6.1-vulnerability-investigation-workflow.md`
  - Activation workflow: `v2.3.6.2-burpsuite-activation-workflow.md`
  
  **Status:** Planning complete, ready for implementation (28-39 hours estimated)

#### Assets [DONE] — `client/src/components/surface-assessment/AssetsTab.tsx`
#### Services [DONE]
#### Topology [IN PROGRESS]

[DONE] 2D network topology view — `client/src/components/surface-assessment/NetworkTopologyView.tsx`
[NOT STARTED] 3D topology — The goal is to use data collected from JSON files throughout an operation to construct a 3D topology from Top (Public Facing Assets) to bottom (internal descending subnets)

This will utilize BBOT, Nuclei and Bloodhound data

  - The goal is to create a live network topology mapping for each operation


[Use this repo-fork](https://github.com/cmndcntrlcyber/3d-force-graph)
[And this repo-fork](https://github.com/cmndcntrlcyber/networkmaps)

#### Activity [DONE] — `client/src/components/surface-assessment/ActivityTab.tsx`
#### Scan Config [DONE] — `client/src/components/surface-assessment/ScanConfigTab.tsx`

- Operations Manager Agent [DONE]
- Reporter Agent [DONE]

### Reports [IN PROGRESS]
- [NOT STARTED] Remove current reporting capabilities
- [NOT STARTED] Deploy and embed [Sysreptor](https://docs.sysreptor.com/setup/configuration/)
  - [NOT STARTED] Incorporate [AI Agents](https://docs.sysreptor.com/setup/configuration/#ai-agent), [OLLAMA LLM Provider](https://docs.sysreptor.com/setup/configuration/#llm-provider),
  - [NOT STARTED] Equip the Technical Writer Agent with all the capabilities of utilising the sysreptor [CLI](https://docs.sysreptor.com/cli/getting-started/) and [python library](https://docs.sysreptor.com/python-library/)

- Operations Manager Agent [DONE]
- Technical Writer Agent [DONE] — `server/services/agents/technical-writer-agent.ts` (agent exists, SysReptor integration pending)
- Reporter Agent [DONE]

# AI [DONE]

- Operations Manager Agent [DONE]
- Reporter Agent [DONE]

## Ollama AI [DONE]
Implemented: `server/services/ollama-ai-client.ts`, `server/services/ollama-manager.ts`, `server/api/v1/ollama.ts`
Agent AI config with multi-provider support (ollama/openai/anthropic/auto): `shared/types/agent-config.ts`

- Operations Manager Agent [DONE]
- Reporter Agent [DONE]

# Offsec R&D Team [IN PROGRESS]

## ATT&CK Workflows [IN PROGRESS] — `client/src/components/offsec-team/TacticWorkflowsView.tsx`
## Research Projects [NOT STARTED]
## Experiments [NOT STARTED]
## Tool Lab [NOT STARTED]
## Knowledge Base [NOT STARTED]
- Link knowledge base to

# Frameworks [DONE] (core pages and parsers complete)
Frameworks page: `client/src/pages/Frameworks.tsx`
Framework mappings API: `server/api/v1/framework-mappings.ts`

__1. For each framework and its variants, create an agent__

__2. For each framework and their agents, create docker containers that contain within them all the scripts and tools necessary to evaluate enterprises against all vulnerabilities and hardening requirements.__

- [IN PROGRESS] Review the files in ./framework/ and develop an app page interface capable of executing all techniques delivered through the rust-nexus agent

- Update the plan files in rust-nexus-enhancement when critically necessary to facilitate secure agentic testing of LLM and Adversarial AI/ML capabilities.

## MITRE [IN PROGRESS]
- Operations Manager Agent [DONE]
- Reporter Agent [DONE]

### ATT&CK [IN PROGRESS]
- [DONE] Move Tactic Agent List to First Tab
- [DONE] Stix Upload Tile
    - Make Single-Click instead of Double for Upload
    - Automatically push STIX Data Upload to ATT&CK Workbench DB
- [DONE] WorkbenchTab — `client/src/components/attack/WorkbenchTab.tsx`

#### Planner [NOT STARTED]
https://github.com/cmndcntrlcyber/attck-planner
###### **Features**
- **Threat Actor Lookup:** Retrieve known MITRE ATT&CK techniques used by a specified adversary.
- **Impact Selection:** Choose a desired impact (e.g., Data Exfiltration, Credential Theft, System Disruption).
- **AI-Powered Plan Generation:** Leverage Research Agent to generate emulation plans.
- **Real-Time Threat Intelligence:** Integrated with Tavily MCP for up-to-date threat intelligence and context enrichment. Utilizes ATT&CK STIX Collection data to drive research.
- **User-Friendly Interface:** Built with Streamlit for quick and easy interaction.
- **Customizable API Parameters:** Modify model-specific settings to fine-tune responses.


#### Workbench [IN PROGRESS]
- [NOT STARTED] Split the space so that the "ATT&CK Workbench Connection" is on the left and "Bidirectional Sync" on the right
- [NOT STARTED] Embed the Workbench Frontend container within the remaining space below
##### Frontend
- Ensure it is enabled in the @./docker-compose.yml line 151 by default

#### Navigator [Acceptable]
#### Flow Builder [DONE]
- [DONE] Remove Flow Builder Label, Place Attack Flow in that element — `client/src/components/attack/AttackFlowBuilder.tsx`, `client/src/components/attack/AttackFlowDiagram.tsx`, `server/api/v1/attack-flows.ts`

#### Techniques [Acceptable]

#### Tactics [Acceptable]

#### Groups [Acceptable]

#### Software [Acceptable]

#### Mitigations [Acceptable]

### ATLAS [DONE]
Full component set (13 components): `client/src/components/atlas/`
Including: AtlasTacticsGrid, AtlasTechniquesTable, AtlasNavigator, AtlasWorkbenchTab, AtlasFlowBuilder, AtlasFlowDiagram, AtlasCoverageMatrix, AtlasMitigationsTable, AtlasCaseStudiesTable, AtlasPlannerTab, AtlasStixImportDialog, AtlasTechniqueDetailDialog, AtlasAttackMappingsTab

## OWASP [DONE]
### OWASP LLM Top 10 [DONE]
Page: `client/src/pages/OWASPLLMFramework.tsx`
Components: `client/src/components/owasp-llm/` (OWASPLLMAssessment, AttackVectorsTable, MitigationsTable, VulnerabilitiesTable)
Backend: `server/api/v1/owasp-llm.ts`, `server/services/owasp-llm-parser.ts`

## NIST [DONE]
### NIST AI RMF [DONE]
Page: `client/src/pages/NISTAIFramework.tsx`
Components: `client/src/components/nist-ai/` (NISTAIAssessment, NISTCategoriesTable, NISTFunctionsAccordion, NISTImplementationGuide)
Backend: `server/api/v1/nist-ai.ts`

## CIS [DONE]
Page: `client/src/pages/CISFramework.tsx`
Components: `client/src/components/cis/` (CISAssessment, CISControlsTable)
Backend: `server/api/v1/cis.ts`, `server/services/cis-parser.ts`

# RTPI Tools [DONE]

## Issue Resolution: Tool Discovery Failure (FIXED)

**Root Cause:** The Tool Connector Agent was calling `DockerExecutor.exec()` with incorrect parameters - passing the command string where the container name should be, causing all tool discovery to fail silently.

**Fix Applied:**
1. Fixed `DockerExecutor.exec()` calls to use correct signature: `exec(containerName, cmdArray, options)`
2. Added multi-container discovery support (polls 8 containers instead of just 1)
3. Added container tracking in tool registry database

## Architecture Overview

```
Tool Discovery System (IMPLEMENTED)
├── Tool Connector Agent (server/services/agents/tool-connector-agent.ts)
│   ├── Polls all containers every 5 minutes
│   ├── Runs `which <tool> && <tool> --version` in containers
│   └── Syncs discovered tools to toolRegistry database with container info
│
├── Multi-Container Executor (server/services/agents/multi-container-executor.ts)
│   ├── Routes tool execution to correct container automatically
│   ├── Caches tool registry lookups for performance
│   └── Provides streaming execution support
│
├── Primary Container: rtpi-tools
│   └── General tools: nmap, bbot, nuclei, metasploit, gobuster, hydra
│
└── OffSec Agent Containers (start by default with docker compose up -d)
    ├── rtpi-maldev-agent    → Reverse engineering, ROP, binary analysis (44 tools)
    ├── rtpi-azure-ad-agent  → Azure/EntraID attack tools (28 tools)
    ├── rtpi-burp-agent      → Web security, proxy tools (15+ tools)
    ├── rtpi-empire-agent    → C2 frameworks, payloads (8+ tools)
    ├── rtpi-fuzzing-agent   → FFUF, Gobuster, Feroxbuster (8+ tools)
    ├── rtpi-framework-agent → WhatWeb, WPScan, CMS scanners (10+ tools)
    └── rtpi-research-agent  → Comprehensive R&D toolset + JupyterLab
```

## Deployment Checklist

### Step 1: Build Tool Images
```bash
# Build all containers (primary tools + all OffSec agents)
docker compose build
```

### Step 2: Start Containers
```bash
# Start all containers (OffSec agents now start by default)
docker compose up -d
```

### Step 3: Verify Tool Installation
```bash
# Check rtpi-tools
docker exec rtpi-tools which nmap bbot nuclei

# Check OffSec agents
docker exec rtpi-fuzzing-agent which ffuf gobuster
docker exec rtpi-maldev-agent which radare2 objdump
```

### Step 4: Trigger Tool Discovery
```bash
curl -X POST http://localhost:3001/api/v1/agents/tool-connector/poll
```

### Step 5: Verify Discovery Status
```bash
curl http://localhost:3001/api/v1/agents/tool-connector/status
# Expected: { "discoveredToolsCount": 20+, "lastPoll": "..." }
```

## API Endpoints (NEW)

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/v1/tools/execute` | POST | Execute any registered tool by name (auto-routes to container) |
| `/api/v1/tools/containers/status` | GET | Get running status of all tool containers |
| `/api/v1/tools/by-container` | GET | Get tools grouped by container |
| `/api/v1/tools/lookup/:toolName` | GET | Lookup tool info and container location |

### Example: Execute nmap
```bash
curl -X POST http://localhost:3001/api/v1/tools/execute \
  -H "Content-Type: application/json" \
  -d '{"tool": "nmap", "args": ["-V"]}'
```

## Tool Containers Reference

| Container | Dockerfile | Tools | Purpose |
|-----------|-----------|-------|---------|
| rtpi-tools | Dockerfile.tools | nmap, bbot, nuclei, metasploit, gobuster, hydra | General reconnaissance & exploitation |
| rtpi-maldev-agent | docker/offsec-agents/Dockerfile.maldev-tools | radare2, ghidra, pwntools, ROPgadget | Binary analysis & exploit development |
| rtpi-azure-ad-agent | docker/offsec-agents/Dockerfile.azure-ad-tools | AADInternals, ROADtools, AzureHound | Azure/AD attack research |
| rtpi-burp-agent | docker/offsec-agents/Dockerfile.burp-tools | Burp extensions, crawlers, proxies | Web application security |
| rtpi-empire-agent | docker/offsec-agents/Dockerfile.empire-tools | Empire, Covenant, Sliver, Havoc | C2 framework management |
| rtpi-fuzzing-agent | docker/offsec-agents/Dockerfile.fuzzing-tools | ffuf, gobuster, feroxbuster, dirsearch | Web fuzzing & directory discovery |
| rtpi-framework-agent | docker/offsec-agents/Dockerfile.framework-tools | WhatWeb, Wappalyzer, WPScan | Tech detection & CMS scanning |
| rtpi-research-agent | docker/offsec-agents/Dockerfile.research-tools | All categories + JupyterLab | General R&D & POC creation |

## Files Modified

| File | Changes |
|------|---------|
| `server/services/agents/tool-connector-agent.ts` | Fixed exec() calls, added multi-container polling |
| `server/services/agents/multi-container-executor.ts` | NEW: Routes tool execution to correct container |
| `shared/schema.ts` | Added containerName, containerUser columns to toolRegistry |
| `server/api/v1/tools.ts` | Added multi-container execution endpoints |

## Tool Registry Reporter Agent Workflow

1. **GitHub Repository Input:** User provides repository URL
2. **Staging Container:** Clone to `~/` in staging container
3. **Auto-Deployment:** Tool-Registry Reporter Agent reads README, follows install instructions, debugs until operational
4. **Documentation:** Generates troubleshooting docs, saves to `docs/troubleshooting/`
5. **Migration:** Tool-Validator Agent verifies and migrates to production container

## Tools Migration Pipeline

```
GitHub Repo → Staging Container → Reporter Agent → Troubleshooting Docs → Validator Agent → Production Container → Tool Registry
``` 

# Infrastructure [NOT STARTED]

## Infrastructure Monitoring

#### Rust-Nexus Teamserver

#### Rust-Nexus (Client) (Docker Container)

#### Agentic Implants (Rust-Nexus Agents)

      - Feature: Create Delete Agent Bundles


# Administration [NOT STARTED]

## User Management

## Profile

## Settings


---

# Cross-Document Consistency Notes (2026-03-04)

## v2.3.05 Roadmap Analysis
- **Outdated**: States only 3 agents exist (ToolConnector, SurfaceAssessment, WebHacker). Now 10+ agents are implemented including Operations Manager, Message Bus, Task Agents (vulnerability, target, qa, technical-writer), and Reporter framework.

## v2.3.1 Memory System Foundation
- **Status updated**: Plan describes architecture/schema. All 4 memory tables now exist in `shared/schema.ts` (lines 2483-2540). Mem0 config, memory service, and API endpoints are implemented.

## v2.3.2 Agent System Architecture
- **Largely complete**: Operations Manager Agent, Agent Message Bus, Reporter Agent Service, Scheduled Reports, and 8 Task Agent implementations all exist. Remaining: Q&A page UI, Reporter Agents Administration UI.

## v2.3.3 Operations Management Automation
- **Partially complete**: Target auto-creation and operation lifecycle automation services exist and are modified. BBOT and Nuclei executors updated. Remaining: full event-driven pipeline, Nmap async executor, Shodan-CVE-to-Nuclei workflow.

## v2.3.4 UI/UX Reorganization
- **Partially complete**: BurpSuite upload, Surface Assessment tab components, Dashboard mods, Network Topology (2D). Remaining: floating chat window (Step 4.2), multi-agent loop blocks (Step 4.5), sidebar reorg (Step 4.3).

## v2.3.6 Vulnerability Investigation Workflow
- **Fully planned**: Complete 6-phase implementation plan (Phase 0-5) with detailed specifications.
- **Status**: Planning complete, ready for implementation (28-39 hours estimated).
- **Key components**:
  - v2.3.6.1: Main workflow (BBOT parsing, agent coordination, validation)
  - v2.3.6.2: BurpSuite activation system (dual-upload, dormant state management)
- **Dependencies**:
  - BurpSuite Orchestrator Agent (new agent service required)
  - R&D Team Agent (new agent service required)
  - Enhanced BBOT parser for [FINDING]/[VULN] extraction
  - Migration 0025 for investigation tracking columns
- **Integration points**:
  - Operations Manager delegates investigation tasks
  - Vulnerability Reporter Agent filters Medium+ severity
  - Burp MCP Server in rtpi-burp-agent container
  - Tavily MCP for CVE research

## Remaining high-priority items across all phases:
1. Operations Manager floating chat window (v2.3.4 Step 4.2)
2. Multi-agent loop blocks (v2.3.4 Step 4.5) — planned, not started
3. SysReptor integration for reports
4. 3D force-graph network topology
5. ATT&CK Planner embedding
6. Nmap async executor
7. Vulnerability Investigation Workflow (v2.3.6.1) — PLANNED, awaiting implementation
8. Infrastructure monitoring (Rust-Nexus)
9. Administration pages (User Management, Profile, Settings)



