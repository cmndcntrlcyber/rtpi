# v2.5 Implementation Status

**Last Updated:** 2026-04-08
**Overall Status:** Scaffolding Complete
**Priority:** High — Unified skills platform with persistent memory

---

## Overview

v2.5 transforms RTPI from a containerized pen-testing toolkit into a full-spectrum, memory-persistent cybersecurity AI platform. It integrates 6 skill/tool repositories (900+ capabilities), a persistent mem0 memory backend with pgvector + Neo4j, and a LangGraph-based workflow orchestrator.

See: [v2.5-roadmap.md](./v2.5-roadmap.md) for the full technical specification.

---

## Phase A: Foundation (Weeks 1-4)

### Infrastructure
- [x] mem0 Python service scaffolding (`services/mem0/`)
  - [x] Dockerfile + requirements.txt
  - [x] FastAPI app with full REST API (add, search, get_all, delete)
  - [x] Pydantic models for request/response types
  - [x] Config with security-tuned fact extraction prompt
  - [x] Attack path and critical vuln graph query endpoints
- [x] LangGraph orchestrator service scaffolding (`services/orchestrator/`)
  - [x] Dockerfile + requirements.txt
  - [x] State definitions (RTFIState, Finding, Phase types)
  - [x] Graph builder with full node topology and conditional routing
  - [x] FastAPI app with engagement management API
  - [x] Human-in-the-loop approval endpoints
- [x] Docker Compose additions
  - [x] mem0-postgres (pgvector/pgvector:pg16)
  - [x] neo4j (neo4j:5-community with APOC)
  - [x] mem0-api service
  - [x] rtpi-orchestrator service
  - [x] nuclei, subfinder, bloodhound containers (Phase B)
  - [x] ghidra-headless, aflplusplus containers (Phase C)
  - [x] New volumes (mem0-pg-data, neo4j-data, neo4j-logs, skills-data)

### Agent Nodes (all with TODO implementation stubs)
- [x] Planning agent — queries mem0 for prior context, loads skills
- [x] Recon agent — network, web, OSINT, cloud discovery fan-out
- [x] Vuln assessment agent — parallel vuln scanning across domains
- [x] Human approval gate — interrupt point before exploitation
- [x] Exploitation agent — Metasploit/Caldera integration point
- [x] Post-exploitation agent — persistence, privesc, lateral movement
- [x] Reverse engineering agent — binary, malware, protocol, firmware
- [x] Reporting agent — report generation from mem0 graph

### Routing Logic
- [x] Severity router (critical/high → approval, else → reporting)
- [x] Approval router (approved → exploit, declined → reporting)
- [x] Binary check router (RE needed → rev eng, else → post-ex)

### TypeScript Integration
- [x] Shared types (`shared/types/`)
  - [x] mem0-types.ts — Memory CRUD, filters, attack paths
  - [x] langgraph-types.ts — Engagement lifecycle, findings, approvals
  - [x] skill-types.ts — Skill metadata, search, discovery
- [x] Server clients (`server/services/`)
  - [x] mem0-api-client.ts — Full HTTP client for mem0 service
  - [x] langgraph-client.ts — Full HTTP client for orchestrator
  - [x] skill-discovery-service.ts — Cached skill search wrapper
- [x] API routes (`server/api/v1/`)
  - [x] skills.ts — Skill search and retrieval
  - [x] orchestrator.ts — Engagement management, approval, advancement

### Skills Directory
- [x] Directory structure (`skills/`)
  - [x] anthropic-cybersecurity-skills/ (placeholder for import)
  - [x] payloadsallthethings/ (placeholder for transform)
  - [x] claude-advanced-plugins/ (7 subdirectories per category)
  - [x] rtpi-custom/ (4 initial skill files)
- [x] Skill discovery service (Python, in orchestrator)
  - [x] Dual-format indexing (agentskills.io + ClaudeAdvancedPlugins)
  - [x] Keyword + domain + MITRE technique search
  - [x] Source detection and domain inference

### RE Tool Dockerfiles
- [x] Ghidra headless (Eclipse Temurin + Ghidra 11.2.1)
- [x] Frida server (Python + frida-tools)
- [x] QEMU user-mode (Ubuntu + qemu-user-static + binwalk)

### Agent-to-Container Mapping (consolidated)
- [x] Tool registry (`services/orchestrator/app/tool_registry.py`)
  - [x] 13 agent roles mapped to existing containers
  - [x] 50+ tool definitions with command templates, containers, timeouts, parse formats
  - [x] Removed redundant standalone nuclei/subfinder/bloodhound (already in offsec agents)
- [x] Container executor (`services/orchestrator/app/container_executor.py`)
  - [x] Docker socket exec into running tool containers
  - [x] Async execution with timeout enforcement
  - [x] Parallel fan-out with configurable concurrency limit
  - [x] Structured output parsing (JSON, JSONL, XML, nmap-xml)
  - [x] Container health checking per agent role
- [x] Orchestrator API endpoints for tool execution
  - [x] POST /tools/execute — single tool execution
  - [x] POST /tools/execute-batch — parallel batch execution
  - [x] GET /tools/registry — full registry listing
  - [x] GET /tools/containers/health — all containers
  - [x] GET /tools/containers/{agent}/health — per-agent
- [x] Docker Compose consolidation
  - [x] Removed standalone nuclei, subfinder, bloodhound (redundant)
  - [x] Added Docker socket mount to orchestrator
  - [x] Added container name env vars to orchestrator
  - [x] Activated offsec-llm-sec (was unmapped Dockerfile)
  - [x] Activated offsec-web-injection (was unmapped Dockerfile)
- [x] Cloud security tools container (`Dockerfile.cloud-tools`)
  - [x] AWS: aws-cli, ScoutSuite, Prowler, Pacu, CloudFox, enumerate-iam, cloudsplaining, parliament, pmapper, steampipe, cartography, s3scanner, trufflehog
  - [x] GCP: gcloud, ScoutSuite, Prowler, steampipe, cartography
  - [x] Multi: steampipe, cartography, trufflehog
- [x] Shared types (`shared/types/tool-container-types.ts`)
- [x] Updated TS clients with tool execution + health endpoints
- [x] Updated Express API routes with tool execution + registry + health

### Agent Node Container Wiring
- [x] Recon agent → rtpi-fuzzing-agent + rtpi-framework-agent + rtpi-tools
  - 3-phase execution: discovery → HTTP probing → deep crawl
- [x] Vuln assessment agent → rtpi-burp-agent + rtpi-fuzzing-agent + rtpi-azure-ad-agent + rtpi-framework-agent
  - Sub-agent routing based on recon findings (web/AD/cloud/code)
- [x] Exploitation agent → rtpi-tools + rtpi-maldev-agent + rtpi-empire-agent
  - ExploitDB search + credential brute-force pipeline
- [x] Post-exploitation agent → rtpi-empire-agent + C2 fleet (sliver, c3, loki, adaptix)
  - Credential extraction + lateral movement probing
- [x] Reverse engineering agent → rtpi-maldev-agent + rtpi-ghidra + rtpi-aflplusplus
  - 2-phase: triage (yara/binwalk/r2) → deep analysis (ghidra headless)

---

## Phase B: Offensive Skills Expansion (Weeks 5-8) — NOT STARTED

- [ ] Deploy mem0 stack and validate end-to-end memory flow
- [ ] Import Anthropic-Cybersecurity-Skills into `/skills/`
- [ ] Install ClaudeAdvancedPlugins (40+ system prompts)
- [ ] Transform top 50 PayloadsAllTheThings categories to agentskills.io format
- [ ] Implement recon agent tool execution (Nmap, Subfinder, OSINT)
- [ ] Implement web vuln agent (Nuclei + PayloadsATT payloads)
- [ ] Implement AD assessment agent (BloodHound + pentest-ad)
- [ ] Implement social engineering agent (EvilGoPhish + OSINT)
- [ ] Wire human-in-the-loop checkpoints with WebSocket notifications
- [ ] Configure Neo4j graph memory with security entity extraction

## Phase C: RE & Architecture (Weeks 9-12) — NOT STARTED

- [ ] Build and test Ghidra headless container
- [ ] Build and test QEMU cross-arch emulation container
- [ ] Build and test AFL++ fuzzing container
- [ ] Implement RE pipeline agent (all 5 reverse plugins)
- [ ] Implement code review agent (secure-code-review plugin)
- [ ] Implement threat modeling agent
- [ ] Implement vulnerability research agent (fuzzing pipelines)
- [ ] Implement DevSecOps pipeline agent
- [ ] Build automated report generation from mem0 engagement graph

## Phase D: Platform & Dashboard (Weeks 13-16) — NOT STARTED

- [ ] React dashboard scaffolding for engagement management
- [ ] 3D network topology visualization (Three.js)
- [ ] Attack path graph visualization from Neo4j
- [ ] REST API for SIEM/SOAR integration
- [ ] pgvector query optimization
- [ ] Redis caching layer for scan data
- [ ] Autonomous workflow with operator approval gates
- [ ] Threat intel correlation via graph memory
- [ ] Token tracking + cost optimization
- [ ] Hallucination Guard confidence scoring integration

---

## Dependency Map

```
mem0-postgres + neo4j + ollama
        ↓
    mem0-api
        ↓
  rtpi-orchestrator ← skills/ volume
        ↓
  agent nodes → tool containers (nuclei, subfinder, bloodhound, ghidra, etc.)
        ↓
  Express backend (TS clients) → React frontend
```

## Environment Variables (new for v2.5)

```bash
# mem0 infrastructure
MEM0_POSTGRES_PASSWORD=rtpi_mem0
NEO4J_PASSWORD=rtpi_neo4j

# Orchestrator
MEM0_API_URL=http://mem0-api:8000
ORCHESTRATOR_URL=http://rtpi-orchestrator:8080

# Optional external LLM APIs
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
CALDERA_API_KEY=
```
