# v2.9 — Implementation Status

**Last Updated:** 2026-04-28
**Overall Status:** 🟡 IN PROGRESS (27%)
**Release Scope:** v2.9.1 (platform completion) + v2.9.2 (VPN infrastructure — unscheduled)
**Source plans:**
- [v2.9.1-platform-completion-requirements.md](v2.9.1-platform-completion-requirements.md)
- [v2.9.2-vpn-infrastructure-integration.md](v2.9.2-vpn-infrastructure-integration.md) (renamed from 2.8/, restored to scope)

---

## Release Summary

| Sub-release | Status | Phases | % Complete | Notes |
|-------------|--------|--------|------------|-------|
| v2.9.1 — Platform Completion | 🟡 In Progress | 11 (P0–P10) | 27% | Phases 0–2 complete (3/11) |
| v2.9.2 — VPN Infrastructure | 🟦 Unscheduled | 4 | 0% | Plan exists at v2.9.2-vpn-infrastructure-integration.md (was renamed from 2.8/); not yet scheduled |

**Status legend:** 📋 Not Started · 🟡 In Progress · ⚠️ Blocked · ✅ Complete · 🟦 Unscheduled · 🔴 Failed/Reverted

---

## v2.9.1 — Platform Completion

**Status:** 🟡 In Progress · **Completion:** 27% · **Phases complete:** 3/11

### Phase Progress Overview

| # | Phase | Status | % | Lane | Effort | Flag | Owner |
|---|-------|--------|---|------|--------|------|-------|
| 0 | Feature flag scaffold + `/settings/features` | ✅ Complete | 100% | infra | S | n/a | claude |
| 1 | Sidebar reorder + `/cti` page stub | ✅ Complete | 100% | UI | S | none | claude |
| 2 | Sysreptor health endpoint + Reports banner | ✅ Complete | 100% | reports | M | none | claude |
| 3 | App-side PDF (puppeteer-core + chromium-shell sidecar) | 📋 Not Started | 0% | reports | M | `FF_PDF_NATIVE` | Unassigned |
| 4 | Framework→tools/agents bindings UI | 📋 Not Started | 0% | UI | M | none | Unassigned |
| 5 | Container-app connection repair | 📋 Not Started | 0% | container | M | none | Unassigned |
| 6 | Inference provider registry + vLLM/Qwen-Agent | 📋 Not Started | 0% | agent infra | XL | `RTPI_INFERENCE_PROVIDER=vllm` | Unassigned |
| 7 | CTI feeds + vectorization + STIX auto-import | 📋 Not Started | 0% | knowledge | XL | `RTPI_CTI_ENABLED` | Unassigned |
| 8 | Docmost integration | 📋 Not Started | 0% | reports | M | `RTPI_DOCMOST_ENABLED` | Unassigned |
| 9 | Per-framework Deploy button + collective bundle | 📋 Not Started | 0% | container | L | `FF_FRAMEWORK_DEPLOY` | Unassigned |
| 10 | KASM hardening + nested containers | 📋 Not Started | 0% | container | L | `FF_KASM_HARDENED` | Unassigned |

---

### Phase 0 — Feature Flag Scaffold

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** S · **Flag:** n/a · **Completed:** 2026-04-28

**Work items:**
- [x] `shared/feature-flags.ts` — central flag map (`FF_PDF_NATIVE`, `FF_VLLM_AGENT`, `FF_DOCMOST`, `FF_FRAMEWORK_DEPLOY`, `FF_CTI_VECTOR`, `FF_KASM_HARDENED`); `readFeatureFlags(env)` parser; `FEATURE_FLAG_KEYS` export
- [x] `client/src/lib/feature-flags.ts` — typed `useFeatureFlag(key)` and `useFeatureFlags()` hooks; promise-deduped fetch + subscriber notification
- [x] `server/api/v1/settings.ts` — `GET /features` mounted before `ensureRole("admin")` (any authenticated user can read)
- [x] `.env.example` — six flags documented with phase reference

**Acceptance test (P0):**
- [x] `readFeatureFlags()` smoke test: defaults all `false`; `true`/`1`/`yes`/`on` parse truthy (case+whitespace tolerant); `false`/`0` parse falsy. End-to-end `curl` verification deferred until next dev-server run by user.

**Notes:**
- `useAccessibility.ts` typecheck errors are pre-existing (JSX in `.ts` file); not introduced by Phase 0.
- Endpoint deliberately positioned before admin guard since the client must read flags to gate UI elements.

---

### Phase 1 — Sidebar Reorder + CTI Page Stub

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** S · **Flag:** none · **Completed:** 2026-04-28

**Work items:**
- [x] Extract `navGroups` + `adminNavItems` → `client/src/config/nav-groups.ts` (seam S10). 111 lines; types kept identical.
- [x] Reorder Intelligence to `[OffSec Team R&D, CTI, Frameworks, Reports]` (was `[Frameworks, Reports, OffSec Team R&D]`). Added `Radar` icon for CTI.
- [x] Refactored `client/src/components/layout/Sidebar.tsx` — drops 21 unused `lucide-react` icon imports, types, and the inline `navGroups` array (320 → 215 lines).
- [x] New `client/src/pages/CTI.tsx` — Tabs shell with 4 tabs (Feeds, Indicators, Reports, Search) and Phase-7 placeholder copy in each empty state.
- [x] Registered `/cti` route in `client/src/App.tsx` and imported the `CTI` component.

**Acceptance test (P1):**
- [x] Typecheck (`npx tsc --noEmit -p .`) passes for all Phase 1 files; only pre-existing `useAccessibility.ts` errors remain (not introduced by Phase 1).
- [ ] Live `/cti` 200 + a11y verification deferred until next dev-server run by user.

**Notes:**
- Sidebar's collapsed-groups localStorage key is unchanged; existing user state preserved.
- `Microscope` icon kept for OffSec R&D for visual continuity; `Radar` chosen for CTI.

---

### Phase 2 — Sysreptor Health Endpoint + Reports Banner

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** M · **Flag:** none · **Completed:** 2026-04-28

**Work items:**
- [x] `server/services/sysreptor-client.ts` — replaced `checkHealth()` with rich `HealthResult` shape; added `extractErrnoCode()` (walks `Error.cause` + `AggregateError.errors` chain); added `tryGetVersion()` best-effort version probe; new `HealthReason` union and `classifyFetchError()` distinguishing six failure modes (`not_configured` / `profile_not_enabled` / `service_unreachable` / `timeout` / `auth_error` / `service_error`).
- [x] `server/api/v1/sysreptor.ts` — `GET /sysreptor/health` with 5s in-memory cache; returns 200 even when service is down (body carries the diagnostic). Legacy `GET /sysreptor/status` updated to read the same cache for back-compat.
- [x] `client/src/pages/Reports.tsx` — top-of-page `<SysReptorHealthBanner />` (green pill with version + Open link when up, red banner with reason title + actionable suggestion + Retry when down).
- [x] `client/src/components/reports/SysReptorProjectsList.tsx` — switched to `useSysReptorHealth`; removed redundant inline banners (page-level banner now owns that messaging).
- [x] `docker-compose.yml` — verified `sysreptor-app` already has `restart: unless-stopped`, `depends_on postgres+sysreptor-redis (service_healthy)`, and a `/health` healthcheck. **No changes needed.**

**APIs:**
- [x] `GET /api/v1/sysreptor/health`

**Acceptance test (P2):**
- [x] Smoke test of `checkHealth()` confirmed correct classification for: missing token (`not_configured`), docker DNS failure (`profile_not_enabled` with `docker compose --profile sysreptor up -d` suggestion), connection refused (`service_unreachable`).
- [x] `npx tsc --noEmit -p .` clean for all Phase 2 files.
- [ ] Live `docker compose stop sysreptor-app` → UI red banner; restart → green pill. Pending dev-server run by user.

**Notes:**
- 5s server-side health cache prevents hammering Sysreptor when multiple components mount the hook; UI still polls every 30s for fresh state.
- Banner has its own `defaultSuggestion()` fallback keyed on `reason`, so even if the server-side classifier misses a `suggestion` field, the UI still renders actionable copy.
- Sysreptor's `version` field comes from `/api/v1/utils/settings/` (best-effort; UI tolerates missing).

---

### Phase 3 — App-side PDF Generation

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** M · **Flag:** `FF_PDF_NATIVE`

**Work items:**
- [ ] `package.json` — add `puppeteer-core` (pinned)
- [ ] `docker-compose.yml` — add `chromium-headless-shell` sidecar on private network
- [ ] New `server/services/reports/document-renderer.ts` (seam S8) — browser pool, disk cache, cleanup
- [ ] Refactor `server/services/pdf-report-generator.ts` — template assembly only; delegate to `documentRenderer`
- [ ] `server/api/v1/reports.ts` — async PDF job endpoints
- [ ] `client/src/pages/Reports.tsx` — "Generate PDF" button gated on flag; progress polling

**APIs:**
- [ ] `POST /api/v1/reports/:id/pdf` → `{jobId}`
- [ ] `GET /api/v1/reports/jobs/:jobId`
- [ ] `GET /api/v1/reports/jobs/:jobId/download`

**Schema:**
- [ ] Migration `0032_add_pdf_jobs.sql` — `pdf_jobs(id, report_id, status, file_path, error, created_at)`

**Acceptance test (P3):**
- [ ] PDF renders <30s for 50-page report; status survives app restart; file cleanup after 24h; renders in headless Linux

---

### Phase 4 — Framework→Tools/Agents Bindings UI

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** M · **Flag:** none

**Work items:**
- [ ] New `server/services/frameworks/framework-binding-service.ts` (seam S4) — CRUD + reverse lookup
- [ ] New `client/src/components/frameworks/FrameworkBindingsPanel.tsx`
- [ ] Embed panel in `OWASPLLMFramework.tsx`, `NISTAIFramework.tsx`, `CISFramework.tsx`

**APIs:**
- [ ] `GET /api/v1/frameworks/:type/:externalId/bindings`
- [ ] `POST /api/v1/frameworks/bindings` [admin]
- [ ] `DELETE /api/v1/frameworks/bindings/:id` [admin]
- [ ] `GET /api/v1/tools/:id/frameworks`
- [ ] `GET /api/v1/agents/:id/frameworks`

**Schema:**
- [ ] Migration `0033_extend_framework_bindings.sql` — `framework_bindings` table with unique index on `(framework_type, framework_element_external_id, binding_kind, target_id)`

**Acceptance test (P4):**
- [ ] Bind tool to OWASP LLM01; reload → still bound; reverse lookup returns binding; works for NIST AI RMF + CIS v8

---

### Phase 5 — Container-App Connection Repair

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** M · **Flag:** none

**Work items:**
- [ ] New `server/services/runtime/container-runtime.ts` (seam S2) — `inspect → preflight → exec → classify-error`
- [ ] New `server/services/runtime/error-classifier.ts` — taxonomy: `ContainerNotFound`, `NotRunning`, `Timeout`, `SocketUnreachable`, `ExecFailed`
- [ ] `server/services/tool-executor.ts:101-107` — call `containerRuntime.assertReady()` before exec
- [ ] `server/services/tool-executor.ts:193` — replace string-match with classifier
- [ ] `server/services/agents/multi-container-executor.ts` — structured error response `{code, retryable, remediation}`
- [ ] New `server/services/agents/mcp-invoker.ts` (seam S3) — JSON-RPC 2.0; tool-list cache 60s; liveness probe 30s
- [ ] `server/api/v1/agent-mcp.ts:54-61` — replace TODO stub with `mcpInvoker.callTool()`
- [ ] `server/services/skill-import-pipeline.ts:82-443` — every terminal branch writes `githubToolInstallations`
- [ ] `server/api/v1/health-checks.ts` — `GET /health-checks/containers`
- [ ] Frontend tool result components surface structured errors

**APIs:**
- [ ] `GET /api/v1/health-checks/containers`
- [ ] `POST /api/v1/agents/:agentId/mcp-call` (real)
- [ ] `GET /api/v1/agents/:agentId/mcp-tools`
- [ ] `GET /api/v1/mcp-servers/:id/probe`

**Schema:**
- [ ] Column adds: `mcp_servers.{liveness_path, last_probe_at, last_probe_ok}`, `tool_registry.{health_check_cmd, last_health_at, last_health_ok}`

**Acceptance test (P5):**
- [ ] Stop tool container → UI shows `{code:"not_running", retryable, remediation}` (not stack trace); preflight <100ms; mcp-call returns real output

---

### Phase 6 — Inference Provider Registry + vLLM/Qwen-Agent

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** XL (~6–8 days) · **Flag:** `RTPI_INFERENCE_PROVIDER=vllm`

**Work items:**
- [ ] `docker-compose.yml` — add `vllm-qwen` service (`vllm/vllm-openai:nightly`, GPU reservation, `--profile vllm`)
- [ ] Build `rtpi-vllm` Blackwell sm_120 OCI artifact, pin SHA, document build
- [ ] New `services/qwen-agent/` — Python sidecar Dockerfile + `agent.py` (Qwen-Agent + MCP)
- [ ] New `server/services/inference/inference-provider-registry.ts` (seam S1) — capability-aware
- [ ] New `server/services/inference/vllm-client.ts` — OpenAI-compatible at `http://vllm:8000/v1`
- [ ] Refactor `ollama-ai-client.ts` and `ai-clients.ts` into provider implementations
- [ ] `client/src/components/ollama/AIProviderSettings.tsx` — add vLLM provider card with health probe
- [ ] `server/api/v1/settings.ts` — persist `VLLM_BASE_URL`, `VLLM_MODEL` to `.env`

**APIs:**
- [ ] `GET /api/v1/inference/providers`
- [ ] `POST /api/v1/inference/providers/:id/probe` [admin]
- [ ] `PATCH /api/v1/inference/providers/:id` [admin]

**Schema:**
- [ ] Column adds: `agents.inference_provider_id`, `agents.tool_choice_strategy`

**Acceptance test (P6):**
- [ ] With `--profile vllm` + flag on → agent chat returns Qwen3 XML tool call; stop vLLM → falls back to Ollama; provider probe passes; Blackwell build runs in CI

---

### Phase 7 — CTI Feeds + Vectorization + STIX Auto-import

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** XL · **Flag:** `RTPI_CTI_ENABLED`

**Work items (CTI part):**
- [ ] New `server/services/knowledge/cti-feed-ingestor.ts` (seam S6) — node-cron, dedup by content_hash
- [ ] New `server/services/knowledge/taxii-client.ts` — TAXII 2.1 collection poller
- [ ] New `server/services/knowledge/embedder.ts` — calls active provider's `embed()`; writes to `memoryEntries.embedding_vec`
- [ ] New `server/api/v1/cti.ts` — feeds CRUD, items list, manual refresh
- [ ] New `server/api/v1/knowledge.ts` — semantic search
- [ ] `client/src/pages/CTI.tsx` — wire Feeds tab + Search tab
- [ ] New `client/src/lib/query.ts` (seam S9) — react-query for new surfaces

**Work items (STIX part):**
- [ ] New `server/services/knowledge/stix-import-job.ts` (seam S7) — wraps existing parsers, scheduled
- [ ] Cron entries: ATLAS daily, ATT&CK weekly
- [ ] New `server/api/v1/stix.ts` — manual upload + manual refresh + run history

**APIs:**
- [ ] `GET/POST/PATCH /api/v1/cti/sources`
- [ ] `POST /api/v1/cti/sources/:id/refresh` [operator]
- [ ] `GET /api/v1/cti/items?source=&q=&limit=&offset=`
- [ ] `GET /api/v1/cti/runs?source=`
- [ ] `POST /api/v1/knowledge/search`
- [ ] `GET /api/v1/knowledge/stats`
- [ ] `POST /api/v1/stix/import` [admin]
- [ ] `POST /api/v1/stix/atlas/refresh` [admin]
- [ ] `GET /api/v1/stix/runs`

**Schema:**
- [ ] Migration `0034_cti.sql` — `cti_sources`, `cti_ingestion_runs`, `cti_items` (HNSW index on `memory_entries.embedding_vec`)
- [ ] Migration `0036_stix_import_audit.sql` — `stix_import_runs`

**Acceptance test (P7):**
- [ ] Add ATT&CK TAXII feed → indicators within 5min; `/knowledge/search?q="phishing"` returns ranked items; ATLAS cron logs nightly run; `EXPLAIN` shows HNSW scan

---

### Phase 8 — Docmost Integration

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** M · **Flag:** `RTPI_DOCMOST_ENABLED`

**Work items:**
- [ ] `docker-compose.yml` — add `docmost` + `docmost-redis` services under `--profile docmost`, persistent volume
- [ ] New `server/services/integrations/docmost-client.ts` — REST client (workspace, page, token)
- [ ] New `server/api/v1/docmost.ts` — health + page create
- [ ] `client/src/pages/Reports.tsx` — "Open in Docmost" + "Publish to Docmost" actions
- [ ] Optional new `client/src/pages/Docs.tsx` — iframe embed

**APIs:**
- [ ] `GET /api/v1/docmost/health`
- [ ] `POST /api/v1/docmost/pages` [operator]

**Schema:** none (state in Docmost)

**Acceptance test (P8):**
- [ ] `--profile docmost up` → health ok; "Publish to Docmost" creates a reachable page

---

### Phase 9 — Per-framework Deploy Button + Collective Bundle

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** L (~4 days) · **Flag:** `FF_FRAMEWORK_DEPLOY`

**Work items:**
- [ ] `docker-compose.yml` — move C2 services behind per-framework profiles (`sliver`, `adaptix`, `c3`, `loki`, `empire`)
- [ ] New `server/services/deployments/framework-deployment-orchestrator.ts` (seam S5)
- [ ] New `server/services/deployments/compose-driver.ts` — adapter using ContainerRuntime (S2)
- [ ] New `server/config/framework-stacks.json` — bundle definitions
- [ ] New `server/api/v1/framework-deploy.ts` — lifecycle endpoints
- [ ] `client/src/pages/Frameworks.tsx` — Deploy/Teardown buttons + collective "Deploy All C2"
- [ ] New `client/src/pages/Deployments.tsx` (under Infrastructure)
- [ ] Realtime events via `/api/v1/ws/agents` (`{kind:"deployment_event"}`)

**APIs:**
- [ ] `GET /api/v1/deployments`
- [ ] `POST /api/v1/deployments/:name/up` [admin]
- [ ] `POST /api/v1/deployments/:name/down` [admin]
- [ ] `POST /api/v1/deployments/bundles/:name/up` [admin]
- [ ] `GET /api/v1/deployments/:name/events?since=`
- [ ] `GET /api/v1/deployments/:name/health`

**Schema:**
- [ ] Migration `0035_deployments.sql` — `deployments`, `deployment_events`, `deployment_dependencies`

**Acceptance test (P9):**
- [ ] Deploy Sliver → only Sliver starts; "Deploy all C2" bundle <60s; teardown stops + records; admin-only RBAC

---

### Phase 10 — KASM Hardening + Nested Containers

**Status:** 📋 Not Started · **% Complete:** 0% · **Owner:** Unassigned · **Effort:** L (~4 days) · **Flag:** `FF_KASM_HARDENED`

**Work items:**
- [ ] `server/services/kasm-workspace-manager.ts` — replace unconditional `rejectUnauthorized:false` with cert pinning via `KASM_CA_PATH`; circuit-breaker on axios (5 failures → open 60s)
- [ ] `docker-compose.yml` — switch `kasm-manager` raw socket mount to `docker-socket-proxy` with verb allowlist (`containers`, `exec`, `images:read`)
- [ ] Nested container helper in `kasm-workspace-manager.ts` — kasm-agent invokes child via socket-proxy
- [ ] New `rtpi-ca-init` one-shot init container (mirror of `kasm-db-init`) emitting CA into shared volume
- [ ] `client/src/pages/Infrastructure.tsx` — KASM panel with workspace list, "Spawn Nested Workspace", real status, self-signed warning

**APIs:**
- [ ] `POST /api/v1/kasm-workspaces/:id/nested` [operator]
- [ ] `GET /api/v1/kasm-workspaces/:id/children`

**Schema:** none

**Acceptance test (P10):**
- [ ] Spawn KASM workspace → reachable; nested appears as child; disallowed Docker verb via socket-proxy → 403; cert pinning honored when `KASM_CA_PATH` set

---

## Acceptance Test Matrix

Cross-phase view of verification status. Update this as phases land.

| ID | Test | Phase | Status | Last Run | Notes |
|----|------|-------|--------|----------|-------|
| P0 | `/settings/features` returns flag map | 0 | 🟡 Verified (unit) | 2026-04-28 | Module-level smoke test passes; live `curl` verification pending dev-server run |
| P1 | Sidebar order + `/cti` 200 + a11y | 1 | 🟡 Verified (typecheck) | 2026-04-28 | Typecheck passes; live `/cti` + a11y verification pending dev-server run |
| P2 | Sysreptor stop/start → banner reflects | 2 | 🟡 Verified (smoke + typecheck) | 2026-04-28 | Three failure modes classified correctly in unit smoke test; live stop/start verification pending dev-server run |
| P3 | PDF render + persistence + cleanup | 3 | ⏸ Pending | — | — |
| P4 | Bind tool to OWASP/NIST/CIS persists | 4 | ⏸ Pending | — | — |
| P5 | Structured error on stopped container | 5 | ⏸ Pending | — | — |
| P6 | vLLM tool call + Ollama fallback | 6 | ⏸ Pending | — | — |
| P7 | TAXII feed + semantic search + ATLAS cron | 7 | ⏸ Pending | — | — |
| P8 | Docmost publish + health | 8 | ⏸ Pending | — | — |
| P9 | Sliver deploy + bundle deploy + RBAC | 9 | ⏸ Pending | — | — |
| P10 | KASM nested + cert pinning + verb deny | 10 | ⏸ Pending | — | — |

**Status legend:** ⏸ Pending · 🟡 Running · ✅ Pass · 🔴 Fail · 🟦 Skipped

---

## Risk Register Status

Mirrors the v2.9.1 plan's R1–R5. Update status as risks are mitigated, realized, or retired.

| ID | Risk | Phase | Status | Mitigation Status | Notes |
|----|------|-------|--------|-------------------|-------|
| R1 | vLLM Blackwell sm_120 build fragility | 6 | 🟦 Open | Planned: pinned-SHA OCI artifact | — |
| R2 | Puppeteer Linux deps / image bloat | 3 | 🟦 Open | Planned: chromium-shell sidecar | — |
| R3 | Docker socket exposure (deploy + KASM) | 9, 10 | 🟦 Open | Planned: docker-socket-proxy + RBAC + audit | — |
| R4 | CTI ingestion volume + HNSW rebuild | 7 | 🟦 Open | Planned: rate limit, TTL, partition, threshold | — |
| R5 | Sysreptor profile vs crash ambiguity | 2 | ✅ Mitigated | `{up, profileEnabled, version, reason, suggestion}` shape ships in commit (Phase 2). Six-state classifier distinguishes profile_not_enabled / service_unreachable / timeout / auth_error / service_error | 2026-04-28 |

**Risk status legend:** 🟦 Open · 🟡 Mitigating · ✅ Mitigated · 🔴 Realized · ⚪ Retired

---

## Decision Log

Append-only log of scope, architecture, or process decisions made during implementation. Most recent on top.

| Date | Decision | Rationale | Phase(s) Affected |
|------|----------|-----------|-------------------|
| 2026-04-28 | Sysreptor `/health` returns 200 even when service is down (body carries the diagnostic) | Avoids spurious 500s in UI; lets the client treat health as a structured value rather than an error | 2 |
| 2026-04-28 | 5s server-side cache on health probe | Multiple UI components mount `useSysReptorHealth`; cache prevents hammering Sysreptor on every render | 2 |
| 2026-04-28 | Six-state `HealthReason` union with per-state suggestion text (server side) + matching client-side fallback in banner | Distinguishing `profile_not_enabled` from `service_unreachable` from `auth_error` lets the UI emit actionable remediation — exactly the v2.9.1 plan's R5 mitigation | 2 |
| 2026-04-28 | Picked `Radar` (lucide) icon for CTI sidebar entry | Available without new dep; visually distinct from `Shield` (Frameworks) and `Microscope` (R&D) | 1 |
| 2026-04-28 | CTI tabs are stubs with explicit "Wired by Phase 7" copy in empty states | Sets clear contract — Phase 1 ships the route, Phase 7 fills the data | 1, 7 |
| 2026-04-28 | Phase 0 features endpoint mounted before admin role guard | Client must read flag map to gate UI — would 403 if admin-only | 0 |
| 2026-04-28 | Six flags scaffolded (incl. `FF_VLLM_AGENT`, `FF_CTI_VECTOR`); `RTPI_INFERENCE_PROVIDER` and `RTPI_DOCMOST_ENABLED` left as separate env vars (not in feature map) | Flag map is for boolean UI gates; provider selection and service toggles are config, not flags | 0, 6, 7, 8 |
| 2026-04-28 | Ship vLLM full (not stubbed) in v2.9.1 | User direction; XL effort accepted | 6 |
| 2026-04-28 | All four optional phases (PDF, Docmost, Deploy, KASM) in v2.9.1 scope | User direction; no deferral to v2.9.2+ | 3, 8, 9, 10 |
| 2026-04-28 | Preserve v2.9.1 doc outline + image refs; expand inline | User direction; minimum disruption to existing screenshot narrative | all |
| 2026-04-28 | Equal depth across all phases in plan doc | User direction | all |

---

## Blockers / Open Questions

Running list of items blocking progress or needing clarification. Remove when resolved (move resolution to Decision Log if architectural).

| Date Raised | Item | Phase | Owner | Status |
|-------------|------|-------|-------|--------|
| 2026-04-28 | ~~v2.9.2 VPN plan deleted~~ — resolved: file was renamed from `2.8/` to `2.9/` (git detected as rename in commit `c156222`), plan still exists | v2.9.2 | claude | ✅ Resolved |
| 2026-04-28 | Owner assignments — solo or multi-person? Currently all "Unassigned" | all | Unassigned | 🟦 Open |
| 2026-04-28 | Hugging Face availability of Qwen3.5-9B Q6_K at deploy time (Q5_K_M fallback documented) | 6 | Unassigned | 🟦 Open |
| 2026-04-28 | Docmost auth: SSO integration vs token-only — affects scope | 8 | Unassigned | 🟦 Open |

**Status legend:** 🟦 Open · 🟡 Investigating · ✅ Resolved · 🔴 Escalated

---

## v2.9.2 — VPN Infrastructure (Unscheduled)

**Status:** 🟦 Unscheduled · **Plan doc:** deleted (working tree status `D`)

The original `v2.9.2-vpn-infrastructure-integration.md` was deleted. No work items tracked here until the plan is re-created. The four phases referenced in the v2.8 status doc were:

| Phase | Component | Status |
|-------|-----------|--------|
| 1 | VPN Manager Container | 🟦 Unscheduled |
| 2 | Backend VPN Service | 🟦 Unscheduled |
| 3 | Frontend VPN Management | 🟦 Unscheduled |
| 4 | Container Routing & Testing | 🟦 Unscheduled |

**Action:** decide whether to restore the v2.9.2 plan, fold VPN work into v2.9.1, or defer to v2.10.

---

## Update Cadence

- **Per-PR**: bump phase % and tick work-item checkboxes when a PR lands.
- **Per-week**: review Risk Register status, prune resolved Blockers, update overall %.
- **At phase completion**: flip phase status to ✅, run acceptance test, record result in Acceptance Test Matrix.
- **At release sign-off**: all P0–P10 must be ✅ Pass; all R1–R5 must be ✅ Mitigated or ⚪ Retired.
