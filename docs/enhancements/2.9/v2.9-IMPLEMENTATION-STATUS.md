# v2.9 — Implementation Status

**Last Updated:** 2026-05-06
**Overall Status:** v2.9.1 ✅ Complete · v2.9.2 🟦 Unscheduled · v2.9.3 🟡 In Progress · v2.9.4 🟡 In Progress · v2.9.5 / v2.9.5.1 📋 Planning · v2.9.6 / v2.9.6.1 / v2.9.6.2 🟡 In Progress (joint initiative, A0 landed)
**Release Scope:** v2.9.1 platform completion · v2.9.2 VPN · v2.9.3 default MCP · v2.9.4 Google OAuth bootstrap · v2.9.5 Cloudflare Access · v2.9.5.1 catch recon · v2.9.6 nexus-mesh comms · v2.9.6.1 GML swarm telemetry (shadow only) · v2.9.6.2 MatrixA2A
**Source plans:**
- [v2.9.1-platform-completion-requirements.md](v2.9.1-platform-completion-requirements.md)
- [v2.9.2-vpn-infrastructure-integration.md](v2.9.2-vpn-infrastructure-integration.md)
- [v2.9.3-default-mcp-integrations.md](v2.9.3-default-mcp-integrations.md)
- [v2.9.4-google-oauth-automation.md](v2.9.4-google-oauth-automation.md)
- [v2.9.5-cloudflare-access-automation.md](v2.9.5-cloudflare-access-automation.md)
- [v2.9.5.1-catch-recon-integration.md](v2.9.5.1-catch-recon-integration.md)
- [v2.9.6-nexus-mesh-comms.md](v2.9.6-nexus-mesh-comms.md)
- [v2.9.6.1-gml-agent-swarm-telemetry-analysis.md](v2.9.6.1-gml-agent-swarm-telemetry-analysis.md)
- [v2.9.6.2-matrix-a2a.md](v2.9.6.2-matrix-a2a.md)
- Joint integration plan: `~/.claude/plans/please-review-the-following-glimmering-hartmanis.md`

---

## Release Summary

| Sub-release | Status | Phases | % Complete | Notes |
|-------------|--------|--------|------------|-------|
| v2.9.1 — Platform Completion | ✅ Complete | 11 (P0–P10) | 100% | All 11 phases complete |
| v2.9.2 — VPN Infrastructure | 🟦 Unscheduled | 4 | 0% | Plan exists at v2.9.2-vpn-infrastructure-integration.md; not yet scheduled |
| v2.9.3 — Default MCP Integrations | 🟡 In Progress | 5 (P1–P5) | TBD | In-flight code in `server/services/mcp/`, migrations 0039–0041, MCP UI hooks; per-phase % to confirm against plan |
| v2.9.4 — Google OAuth Bootstrap | 🟡 In Progress | 1 script + 4-stage runbook | 25% | Stage 1 script shipped at `setup/setup-google-oauth.sh`; stages 2–4 are operator runbook actions |
| v2.9.5 — Cloudflare Access Automation | 📋 Planning | 6 (P1–P6) | 0% | Multi-tenant SSO + IaC; depends on v2.9.4 |
| v2.9.5.1 — catch Recon Integration | 📋 Planning | 6 (P1–P6) | 0% | Split-perimeter for catch under Access; depends on v2.9.5 |
| v2.9.6 — nexus-mesh Comms | 🟡 In Progress | 12 (P0–P11; P9, P11 optional) | A0 landed | rust-nexus mesh layer; sequenced as part of joint v2.9.6 / 6.1 / 6.2 initiative (phases A0–J). |
| v2.9.6.1 — GML Swarm Telemetry | 🟡 In Progress | A0 + B3 + G + I | A0 landed | Shadow + viz only per ADR 012. Cross-cuts Operations, Intelligence, Automation. |
| v2.9.6.2 — MatrixA2A | 🟡 In Progress | A0 + A1 + B2 + D + H | A0 landed | Linux Foundation A2A protocol over gRPC, Python lib at `services/a2a/`. Tonic 0.14 bump tracked in ADR 009. |

**Status legend:** 📋 Not Started · 🟡 In Progress · ⚠️ Blocked · ✅ Complete · 🟦 Unscheduled · 🔴 Failed/Reverted

---

## v2.9.1 — Platform Completion

**Status:** ✅ Complete · **Completion:** 100% · **Phases complete:** 11/11

### Phase Progress Overview

| # | Phase | Status | % | Lane | Effort | Flag | Owner |
|---|-------|--------|---|------|--------|------|-------|
| 0 | Feature flag scaffold + `/settings/features` | ✅ Complete | 100% | infra | S | n/a | claude |
| 1 | Sidebar reorder + `/cti` page stub | ✅ Complete | 100% | UI | S | none | claude |
| 2 | Sysreptor health endpoint + Reports banner | ✅ Complete | 100% | reports | M | none | claude |
| 3 | App-side PDF (puppeteer-core + chromium-shell sidecar) | ✅ Complete | 100% | reports | M | `FF_PDF_NATIVE` | claude |
| 4 | Framework→tools/agents bindings UI | ✅ Complete | 100% | UI | M | none | claude |
| 5 | Container-app connection repair | ✅ Complete | 100% | container | M | none | claude |
| 6 | Inference provider registry + vLLM | ✅ Complete | 100% | agent infra | XL | `RTPI_INFERENCE_PROVIDER=vllm` | claude |
| 7 | CTI feeds + vectorization + STIX auto-import | ✅ Complete | 100% | knowledge | XL | `RTPI_CTI_ENABLED` | claude |
| 8 | Docmost integration | ✅ Complete | 100% | reports | M | `FF_DOCMOST` | claude |
| 9 | Per-framework Deploy button + collective bundle | ✅ Complete | 100% | container | L | `FF_FRAMEWORK_DEPLOY` | claude |
| 10 | KASM hardening + nested containers | ✅ Complete | 100% | container | L | `FF_KASM_HARDENED` | claude |

---

### Phase 0 — Feature Flag Scaffold

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** S · **Flag:** n/a · **Completed:** 2026-04-28

**Work items:**
- [x] `shared/feature-flags.ts` — central flag map (`FF_PDF_NATIVE`, `FF_VLLM_AGENT`, `FF_DOCMOST`, `FF_FRAMEWORK_DEPLOY`, `FF_CTI_VECTOR`, `FF_KASM_HARDENED`); `readFeatureFlags(env)` parser; `FEATURE_FLAG_KEYS` export
- [x] `client/src/lib/feature-flags.ts` — typed `useFeatureFlag(key)` and `useFeatureFlags()` hooks; promise-deduped fetch + subscriber notification
- [x] `server/api/v1/settings.ts` — `GET /features` mounted before `ensureRole("admin")` (any authenticated user can read)
- [x] `.env.example` — six flags documented with phase reference

**Acceptance test (P0):**
- [x] `readFeatureFlags()` smoke test: defaults all `false`; `true`/`1`/`yes`/`on` parse truthy (case+whitespace tolerant); `false`/`0` parse falsy. End-to-end `curl` verification deferred until next dev-server run by user.

**Notes:**
- `useAccessibility.ts` typecheck errors are pre-existing (JSX in `.ts` file); not introduced by Phase 0.
- Endpoint deliberately positioned before admin guard since the client must read flags to gate UI elements.

---

### Phase 1 — Sidebar Reorder + CTI Page Stub

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** S · **Flag:** none · **Completed:** 2026-04-28

**Work items:**
- [x] Extract `navGroups` + `adminNavItems` → `client/src/config/nav-groups.ts` (seam S10). 111 lines; types kept identical.
- [x] Reorder Intelligence to `[OffSec Team R&D, CTI, Frameworks, Reports]` (was `[Frameworks, Reports, OffSec Team R&D]`). Added `Radar` icon for CTI.
- [x] Refactored `client/src/components/layout/Sidebar.tsx` — drops 21 unused `lucide-react` icon imports, types, and the inline `navGroups` array (320 → 215 lines).
- [x] New `client/src/pages/CTI.tsx` — Tabs shell with 4 tabs (Feeds, Indicators, Reports, Search) and Phase-7 placeholder copy in each empty state.
- [x] Registered `/cti` route in `client/src/App.tsx` and imported the `CTI` component.

**Acceptance test (P1):**
- [x] Typecheck (`npx tsc --noEmit -p .`) passes for all Phase 1 files; only pre-existing `useAccessibility.ts` errors remain (not introduced by Phase 1).
- [ ] Live `/cti` 200 + a11y verification deferred until next dev-server run by user.

**Notes:**
- Sidebar's collapsed-groups localStorage key is unchanged; existing user state preserved.
- `Microscope` icon kept for OffSec R&D for visual continuity; `Radar` chosen for CTI.

---

### Phase 2 — Sysreptor Health Endpoint + Reports Banner

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** M · **Flag:** none · **Completed:** 2026-04-28

**Work items:**
- [x] `server/services/sysreptor-client.ts` — replaced `checkHealth()` with rich `HealthResult` shape; added `extractErrnoCode()` (walks `Error.cause` + `AggregateError.errors` chain); added `tryGetVersion()` best-effort version probe; new `HealthReason` union and `classifyFetchError()` distinguishing six failure modes (`not_configured` / `profile_not_enabled` / `service_unreachable` / `timeout` / `auth_error` / `service_error`).
- [x] `server/api/v1/sysreptor.ts` — `GET /sysreptor/health` with 5s in-memory cache; returns 200 even when service is down (body carries the diagnostic). Legacy `GET /sysreptor/status` updated to read the same cache for back-compat.
- [x] `client/src/pages/Reports.tsx` — top-of-page `<SysReptorHealthBanner />` (green pill with version + Open link when up, red banner with reason title + actionable suggestion + Retry when down).
- [x] `client/src/components/reports/SysReptorProjectsList.tsx` — switched to `useSysReptorHealth`; removed redundant inline banners (page-level banner now owns that messaging).
- [x] `docker-compose.yml` — verified `sysreptor-app` already has `restart: unless-stopped`, `depends_on postgres+sysreptor-redis (service_healthy)`, and a `/health` healthcheck. **No changes needed.**

**APIs:**
- [x] `GET /api/v1/sysreptor/health`

**Acceptance test (P2):**
- [x] Smoke test of `checkHealth()` confirmed correct classification for: missing token (`not_configured`), docker DNS failure (`profile_not_enabled` with `docker compose --profile sysreptor up -d` suggestion), connection refused (`service_unreachable`).
- [x] `npx tsc --noEmit -p .` clean for all Phase 2 files.
- [ ] Live `docker compose stop sysreptor-app` → UI red banner; restart → green pill. Pending dev-server run by user.

**Notes:**
- 5s server-side health cache prevents hammering Sysreptor when multiple components mount the hook; UI still polls every 30s for fresh state.
- Banner has its own `defaultSuggestion()` fallback keyed on `reason`, so even if the server-side classifier misses a `suggestion` field, the UI still renders actionable copy.
- Sysreptor's `version` field comes from `/api/v1/utils/settings/` (best-effort; UI tolerates missing).

---

### Phase 3 — App-side PDF Generation

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** M · **Flag:** `FF_PDF_NATIVE` · **Completed:** 2026-04-28

**Work items:**
- [x] `package.json` — added `puppeteer-core@^23.11.1` (Chromium not bundled; sidecar provides it). User must run `npm install` to pull the package.
- [x] `docker-compose.yml` — new `chromium-shell` service (`chromedp/headless-shell:latest`, profile `pdf`, bound to `127.0.0.1:9222` only, hardened with `read_only`, `no-new-privileges`, `cap_drop: ALL`, tmpfs /tmp).
- [x] New `server/services/reports/document-renderer.ts` (seam S8) — `puppeteer-core` connects to sidecar via `puppeteer.connect`; lazy reconnect on disconnect; structured `RendererError` (`not_configured` / `connect_failed` / `render_failed`).
- [x] Refactored `server/services/pdf-report-generator.ts` — template assembly preserved; delegates render to `documentRenderer`; falls back to writing HTML to disk when sidecar unavailable so callers always get an artifact.
- [x] `server/api/v1/reports.ts` — added async job endpoints + hourly cleanup task (`unref`'d interval; deletes jobs+files older than `PDF_JOB_RETENTION_MS` default 24h; skipped in `NODE_ENV=test`).
- [x] New `client/src/components/reports/GeneratePdfButton.tsx` — gated on `useFeatureFlag('pdfNative')`; click → POST → 1s polling for up to 5min → blob download; shows status label ("Queueing…" / "Rendering…").
- [x] `client/src/pages/Reports.tsx` — wired `<GeneratePdfButton>` into each report row; renders only when flag is on (component returns null otherwise).
- [x] `.env.example` — documented `CHROMIUM_WS_ENDPOINT`.

**APIs:**
- [x] `POST /api/v1/reports/:id/pdf` → 202 `{jobId, status: "queued"}` (admin/operator only)
- [x] `GET /api/v1/reports/jobs/:jobId` — status + downloadUrl
- [x] `GET /api/v1/reports/jobs/:jobId/download` — streams PDF (or HTML fallback)

**Schema:**
- [x] Migration `0032_add_pdf_jobs.sql` — `pdf_jobs(id, report_id, requested_by, status, file_path, file_size, error, duration_ms, created_at, started_at, completed_at)` with indexes on `status`, `created_at`, `report_id`. `pdf_job_status` enum (queued/rendering/completed/failed).
- [x] Drizzle schema additions for `pdfJobs` and `pdfJobStatusEnum` in `shared/schema.ts`.

**Acceptance test (P3):**
- [x] Typecheck (`npx tsc --noEmit -p .`) clean for all Phase 3 files.
- [x] Migration SQL well-formed; mirror of Drizzle schema.
- [ ] Live render test pending — requires `npm install`, `docker compose --profile pdf up -d`, migration applied, and dev-server run by user.

**Notes:**
- `documentRenderer.connected` listener nulls out the cached `Browser` on disconnect so the next `renderToPdf` reconnects automatically.
- HTML fallback mode produces `*.html` filenames (Content-Type detection in download endpoint sniffs the extension).
- Worker is fire-and-forget (`runPdfJob().catch(...)`) — failures land in the row's `error` column rather than a 500 to the client.

---

### Phase 4 — Framework→Tools/Agents Bindings UI

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** M · **Flag:** none · **Completed:** 2026-04-29

**Work items:**
- [x] New `server/services/frameworks/framework-binding-service.ts` (seam S4) — CRUD + reverse lookup; resolves `targetName` via per-kind queries (`tool_registry`, `agents`); deleted-target rows return `targetName: null` so UI can prompt cleanup.
- [x] New `client/src/components/frameworks/FrameworkBindingsPanel.tsx` — admin-gated picker (Kind: tool/agent, Target: select, Strength: primary/supports/validates) + chip list; lazy-loads tool/agent options on first hover/focus; non-admins see read-only chips.
- [x] Embedded in `VulnerabilitiesTable.tsx` (OWASP LLM, keyed by `owaspId`), `NISTCategoriesTable.tsx` (NIST AI, keyed by `subcategoryId`), `CISControlsTable.tsx` (CIS v8, keyed by `safeguardId`).

**APIs:**
- [x] `GET /api/v1/frameworks/:type/:externalId/bindings`
- [x] `POST /api/v1/frameworks/bindings` [admin] — maps unique-violation (Postgres 23505) → 409
- [x] `DELETE /api/v1/frameworks/bindings/:id` [admin]
- [x] `GET /api/v1/tools/:id/frameworks` — reverse lookup
- [x] `GET /api/v1/agents/:id/frameworks` — reverse lookup

**Schema:**
- [x] Migration `0033_add_framework_bindings.sql` — `framework_bindings` table + `framework_binding_kind` and `framework_binding_strength` enums + unique index on `(framework_type, framework_element_external_id, binding_kind, target_id)` + secondary `(binding_kind, target_id)` index for fast reverse lookups.
- [x] Drizzle schema additions for `frameworkBindings`, `frameworkBindingKindEnum`, `frameworkBindingStrengthEnum`.

**Acceptance test (P4):**
- [x] Typecheck (`npx tsc --noEmit -p .`) clean for all Phase 4 files.
- [ ] Live binding round-trip pending dev-server run + `npm run db:push` to apply migration 0033.

**Notes:**
- `framework_bindings` is intentionally separate from existing `framework_mappings` (which is framework↔framework). Bindings link framework elements to *executable* assets (tools, agents, workflows). Avoids overloading mapping semantics.
- `target_id` is FK by convention; not enforced at the DB layer because the target table varies by `binding_kind`. Service layer resolves names via per-kind queries.
- `workflow` binding kind is reserved in the enum for future use; UI currently exposes only tool + agent.

---

### Phase 5 — Container-App Connection Repair

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** M · **Flag:** none · **Completed:** 2026-04-29

**Work items:**
- [x] New `server/services/runtime/error-classifier.ts` — `ContainerError` class + 6-state taxonomy (`container_not_found` / `not_running` / `timeout` / `socket_unreachable` / `exec_failed` / `invalid_command`); each state ships an actionable `remediation` string and `retryable` flag; `suggestedHttpStatus` getter routes 404/503/504/400/500 sensibly. `extractErrnoCode()` walks Error.cause + AggregateError.errors (same chain walker used by Sysreptor classifier).
- [x] New `server/services/runtime/container-runtime.ts` (seam S2) — `assertReady()` (cached 2s by default via `CONTAINER_READINESS_CACHE_MS`), `exec()` wraps dockerExecutor with classification, `inspectSafe()` for the health endpoint.
- [x] `server/services/tool-executor.ts` — `runCommand()` now calls `containerRuntime.exec()`; the catch block uses `classifyContainerError()` to set `status: 'timeout'` for the right reason instead of substring-matching `'timeout'` in the error message.
- [x] `server/services/agents/multi-container-executor.ts` — `executeTool()` and `executeRawCommand()` route through containerRuntime.
- [x] New `server/services/agents/mcp-invoker.ts` (seam S3) — real JSON-RPC 2.0 client over stdio: line-delimited message framing, request/response correlation by id, `initialize` handshake, `tools/list` (60s cache per server), `tools/call`, 30s liveness probe loop that writes `mcp_servers.{last_probe_at,last_probe_ok}`. Skipped under `NODE_ENV=test`.
- [x] `server/services/mcp-server-manager.ts` — exposed `getChildProcess(serverId)` accessor so the invoker can read/write the process's stdin/stdout without breaking the manager's encapsulation of lifecycle logic.
- [x] `server/api/v1/agent-mcp.ts` — TODO stub replaced with `mcpInvoker.callTool()`; `mcp-tools` endpoint now queries the live server (with graceful fallback when unreachable); structured `MCPInvokerError` mapped to 503/504/502 by code.
- [x] `server/api/v1/health-checks.ts` — `GET /containers` aggregates tool-registry containers + MCP servers with state/lastHealth/lastProbe details. Registered before the `:id` wildcard route.
- [x] New `client/src/components/runtime/ContainerErrorBanner.tsx` — renders structured errors with icon + title + retryable chip + remediation copy + optional retry button. Falls through to a generic AlertTriangle banner for unknown codes.
- [x] `client/src/components/tools/ConfigureToolDialog.tsx` — execution catch block sets `executionError` from `err.data.{code,retryable,remediation,message}`; dialog renders `<ContainerErrorBanner>` above the existing result panel.

**Skipped (out of Phase 5 scope):**
- [ ] `skill-import-pipeline.ts` deeper bookkeeping fix — current pipeline already writes `githubToolInstallations` in the binary/python paths; comprehensive container-build event emission lands with Phase 9 (Deploy automation), which adds the `deployment_events` table.

**APIs:**
- [x] `GET /api/v1/health-checks/containers`
- [x] `POST /api/v1/agents/:agentId/mcp-call` (real JSON-RPC invocation)
- [x] `GET /api/v1/agents/:agentId/mcp-tools` (live `tools/list`, cached 60s)
- [x] `GET /api/v1/agents/mcp-servers/:id/probe` (force a liveness probe)

**Schema:**
- [x] Migration `0034_add_runtime_health_columns.sql` + Drizzle: `mcp_servers.{liveness_path, last_probe_at, last_probe_ok}`, `tool_registry.{health_check_cmd, last_health_at, last_health_ok}`. Additive; safe `IF NOT EXISTS`.

**Acceptance test (P5):**
- [x] Smoke test of `classifyContainerError()` confirms all six failure modes land on the right code (dockerode 404 → `container_not_found`, "is not running" → `not_running`, AbortError → `timeout`, ECONNREFUSED → `socket_unreachable`, "dangerous pattern" → `invalid_command`, generic → `exec_failed`).
- [x] Typecheck (`npx tsc --noEmit -p .`) clean for all Phase 5 files.
- [ ] Live "stop tool container → structured error in UI" verification pending dev-server run + `npm run db:push` to apply migration 0034.

**Notes:**
- Readiness cache TTL (default 2s) is a pragmatic balance: long enough to keep preflight overhead near zero on rapid back-to-back execs, short enough that a recently stopped container surfaces as `not_running` within the next request cycle.
- The MCP invoker uses newline-delimited JSON-RPC framing per current MCP spec (rev 2025-06-18); the `initialize` handshake runs lazily on first request to a server.
- MCP probe writes are best-effort (caught + logged) so a transient DB failure never masks the actual probe result.

---

### Phase 6 — Inference Provider Registry + vLLM

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** XL · **Flag:** `RTPI_INFERENCE_PROVIDER=vllm` · **Completed:** 2026-04-29

**Scope adjustment (2026-04-29):** Path A shipped — vLLM with built-in tool calling (`--tool-call-parser qwen3_coder`) + the orchestrator's existing `mcp-invoker` (Phase 5) for MCP dispatch. The Qwen-Agent Python sidecar (Path B) was deliberately deferred: vLLM's own tool calling satisfies the agentic requirement and integrates cleanly with what Phase 5 already built. Qwen-Agent remains an opt-in future addition.

**Work items:**
- [x] `docker-compose.yml` — `vllm-qwen` service (`vllm/vllm-openai:nightly`, GPU reservation, profile `vllm`, port `127.0.0.1:18000:8000`, command flags from architecture doc: `--reasoning-parser=qwen3 --enable-auto-tool-choice --tool-call-parser=qwen3_coder --max-model-len=131072`, HF cache volume, `/health` healthcheck w/ `start_period: 120s`).
- [x] New `vllm-hf-cache` named volume for HF weights cache.
- [x] New `server/services/inference/vllm-client.ts` — OpenAI-compatible client (`probeVLLM`, `vllmChat`, `vllmEmbed`); reads `VLLM_BASE_URL` / `VLLM_MODEL` at call time.
- [x] New `server/services/inference/inference-provider-registry.ts` (seam S1) — `InferenceProvider` interface + capability matrix; concrete `VLLMProvider`, `OllamaProvider`, `OpenAIProvider`, `AnthropicProvider` wrapping existing clients (no transport duplicated). `resolveDefault()` follows `RTPI_INFERENCE_PROVIDER` env or walks `[vllm → ollama → openai → anthropic]` in `auto` mode.
- [x] New `server/api/v1/inference.ts` mounted at `/api/v1/inference` — three endpoints (see APIs); PATCH writes both `process.env` and `.env` so changes are immediate + persistent.
- [x] New `client/src/components/inference/InferenceProvidersPanel.tsx` — capability badges, last-probe state, inline endpoint+model editing for vllm/ollama (admin-only); per-provider Probe + global "Probe all".
- [x] Mounted in `client/src/pages/Ollama.tsx` settings tab above the existing `AIProviderSettings`.
- [x] Schema additions: `agents.inference_provider_id` (NULL = use registry default) + `agents.tool_choice_strategy`; migration `0035_add_inference_provider_columns.sql`.
- [x] `.env.example` documents `RTPI_INFERENCE_PROVIDER`, `VLLM_BASE_URL`, `VLLM_MODEL`.

**APIs:**
- [x] `GET /api/v1/inference/providers` (`?probe=true` forces a fresh probe of every provider)
- [x] `POST /api/v1/inference/providers/:id/probe`
- [x] `PATCH /api/v1/inference/providers/:id` [admin] — endpoint/model overrides for vllm/ollama; cloud key management stays in `/api/v1/settings/llm`.

**Schema:**
- [x] Migration `0035_add_inference_provider_columns.sql` (additive `IF NOT EXISTS`): `agents.inference_provider_id TEXT`, `agents.tool_choice_strategy TEXT`.

**Acceptance test (P6):**
- [x] Smoke test of `inferenceProviderRegistry.list()` confirms all four providers register correctly with capability matrix; `resolveDefault()` returns vLLM when both vllm + ollama are configured; correctly picks up `OLLAMA_HOST=http://192.168.1.124:14434` from env.
- [x] `npx tsc --noEmit -p .` clean for all Phase 6 files.
- [ ] Live vLLM probe + chat round-trip pending `docker compose --profile vllm up -d` on the user's Blackwell host + `npm run db:push` for migration 0035.

**Deliberately not built (deferred):**
- ❌ `services/qwen-agent/` Python sidecar — superseded by Path A. Add later if user needs Qwen-Agent's agent-loop semantics specifically.
- ❌ Refactor of `ollama-ai-client.ts` / `ai-clients.ts` to remove their direct exports — registry wraps them; refactor would churn many call sites for no immediate benefit.

**Notes:**
- Blackwell sm_120 (RTX 5060 Ti): the published `vllm/vllm-openai:nightly` may need a source rebuild against CUDA 12.8 nightly — see [`Inference-Agent-Architecture.md`](references/Inference-Agent-Architecture.md). Compose service definition unchanged; only the image tag varies.
- vLLM + Ollama on the same GPU: pick one at runtime. Registry's `auto` resolution naturally prefers vLLM, so flipping providers is just `--profile vllm up -d` (vLLM) or stopping it (Ollama remains default).
- Per-agent overrides: agents can pin themselves to a specific provider via `agents.inference_provider_id`. Use case: code-generation agents pin to vLLM-served Qwen2.5-Coder-14B even when registry default is something else.

---

### Phase 7 — CTI Feeds + Vectorization + STIX Auto-import

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** XL · **Flag:** `RTPI_CTI_ENABLED` · **Completed:** 2026-04-29

**Work items (CTI part):**
- [x] New `server/services/knowledge/taxii-client.ts` — TAXII 2.1 read client; pagination via `next` token; auth via `auth_headers`; `addedAfter` filter for incremental polling.
- [x] New `server/services/knowledge/embedder.ts` — routes to active `InferenceProvider.embed()`; resolveDefault chain `[openai → vllm → ollama]` (OpenAI first because `text-embedding-3-small` is 1536 dims by default); structured `EmbedderError` for `no_provider` / `dimension_mismatch` / `transport_failed`; gracefully returns `null` when no provider can embed.
- [x] Extended `inference-provider-registry.ts` — added `embed()` to `InferenceProvider` interface; concrete impls for vLLM (uses `/v1/embeddings`), Ollama (`/api/embed` with `/api/embeddings` fallback for legacy servers), OpenAI (SDK `embeddings.create`), Anthropic (returns null — no embed endpoint).
- [x] New `server/services/knowledge/cti-feed-ingestor.ts` (seam S6) — `CronJob` ticks every 15min in production; per-source loop respects `cadence_seconds`; `runSource(id)` is idempotent (dedup by `content_hash`, upsert by `(source_id, external_id)`); embeds title+summary in batches of 16, breaks gracefully on `dimension_mismatch`; writes audit row to `cti_ingestion_runs`.
- [x] Per-kind fetchers: TAXII (via taxii-client) and RSS/Atom (via existing `fast-xml-parser`); `json` and `github` kinds reserved with explicit `not_implemented` error.
- [x] New `server/api/v1/cti.ts` — sources CRUD (admin write, anyone read); `POST /sources/:id/refresh` (operator); `GET /items` with keyword filter + pagination; `GET /runs` for audit.
- [x] New `server/api/v1/knowledge.ts` — `POST /search` runs vector search via pgvector `<=>` cosine operator, ranked by ascending distance; `GET /stats` returns counts (sources, items, items embedded, embedding dim).
- [x] `client/src/pages/CTI.tsx` rewritten — Feeds tab with add/edit/delete + manual refresh + last-run badge; Indicators tab with paginated keyword search; Search tab with semantic search + cosine-distance display + stats panel. Reduced to 3 tabs (dropped Reports tab — defer to a future CTI-reports phase).

**Work items (STIX part):**
- [x] New `server/services/knowledge/stix-import-job.ts` (seam S7) — wraps existing `importSTIXBundle()` and `importATLASSTIXBundle()`; bundles fetched from `ATLAS_BUNDLE_URL` / `ATTCK_BUNDLE_URL` (overrideable). Cron: ATLAS daily 03:30, ATT&CK Sunday 04:45. `importBundle()` auto-detects ATLAS bundles by `x-mitre-atlas-*` object types.
- [x] New `server/api/v1/stix.ts` — `POST /import` (JSON body), `POST /atlas/refresh`, `POST /attck/refresh`, `GET /runs`. All admin-gated except `GET /runs`.

**APIs:**
- [x] `GET /api/v1/cti/sources`
- [x] `POST /api/v1/cti/sources` [admin]
- [x] `PATCH /api/v1/cti/sources/:id` [admin]
- [x] `DELETE /api/v1/cti/sources/:id` [admin]
- [x] `POST /api/v1/cti/sources/:id/refresh` [operator]
- [x] `GET /api/v1/cti/items?source=&q=&limit=&offset=`
- [x] `GET /api/v1/cti/runs?source=`
- [x] `POST /api/v1/knowledge/search`
- [x] `GET /api/v1/knowledge/stats`
- [x] `POST /api/v1/stix/import` [admin]
- [x] `POST /api/v1/stix/atlas/refresh` [admin]
- [x] `POST /api/v1/stix/attck/refresh` [admin]
- [x] `GET /api/v1/stix/runs`

**Schema:**
- [x] Migration `0036_add_cti_tables.sql` — `cti_sources` (with `cti_source_kind` enum), `cti_ingestion_runs` (with `cti_ingestion_run_status` enum), `cti_items` (with `embedding vector(1536)` + HNSW index `cti_items_embedding_hnsw_idx` on `embedding vector_cosine_ops`).
- [x] Migration `0037_add_stix_import_runs.sql` — `stix_import_runs` audit table with `(source, started_at)` index.
- [x] Drizzle additions: `ctiSources`, `ctiIngestionRuns`, `ctiItems`, `stixImportRuns`, plus `ctiSourceKindEnum`, `ctiIngestionRunStatusEnum`.

**Acceptance test (P7):**
- [x] `npx tsc --noEmit -p .` clean for all Phase 7 files.
- [x] CRON loop only starts when `RTPI_CTI_ENABLED=true|1` (off by default).
- [ ] Live "add ATT&CK TAXII feed → indicators within 5min" test pending `npm run db:push` for migrations 0036/0037 + setting `RTPI_CTI_ENABLED=true` + setting an embedding provider (OpenAI key OR `nomic-embed-text` pulled in Ollama).
- [ ] Live `EXPLAIN` showing HNSW scan pending data ingestion.

**Deliberately not built (deferred):**
- ❌ `client/src/lib/query.ts` (seam S9) — react-query rollout; the new CTI surfaces use the existing `api` fetch wrapper for consistency with the rest of the codebase. Migration to react-query stays a future, opportunistic task.
- ❌ CTI Reports tab (4th tab in Phase 1 stub) — out of scope; CTI digests and analyst reports will land in a future phase. Current Phase 7 ships 3 tabs (Feeds, Indicators, Search).
- ❌ Workflow type for `framework_bindings.binding_kind` — Phase 4 reserved the enum value but Phase 7 doesn't surface workflows in CTI (no overlap).

**Notes:**
- pgvector HNSW index uses `vector_cosine_ops`; cosine distance via `<=>` is what `/knowledge/search` orders by. Sub-millisecond probes for typical CTI volumes (<1M items).
- Embedding dimension is hard-coded to **1536** to match the existing `vector(1536)` schema column. The embedder validates dim and throws `EmbedderError("dimension_mismatch", ...)` if a smaller model (e.g. `nomic-embed-text` at 768 dims) is used — surfaces in the UI as a clear error so the user can switch models.
- Ollama embed endpoint preference: `/api/embed` (newer, supports batch) → `/api/embeddings` (legacy, single-input). Most current Ollama servers support the batch endpoint; the fallback covers older deployments.
- ATT&CK weekly cron is intentional: the bundle is ~50MB and parses 100k+ relationships. Daily would add no signal but consume resources.

---

### Phase 8 — Docmost Integration

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** M · **Flag:** `FF_DOCMOST` · **Completed:** 2026-04-29

**Work items:**
- [x] `docker-compose.yml` — `docmost` service (image `docmost/docmost:latest`, profile `docmost`, port `127.0.0.1:13000:3000`, depends_on postgres + dedicated `docmost-redis`, `/api/health` healthcheck) + `docmost-redis` (bitnami/redis under same profile) + `docmost-data` named volume.
- [x] `docker/postgres-init/01-init-databases.sql` — creates `docmost` database + role + grants on first-run + `uuid-ossp` extension.
- [x] New `server/services/integrations/docmost-client.ts` — `checkHealth()` returns the same six-state classification used by Sysreptor (`not_configured` / `service_unreachable` / `timeout` / `auth_error` / `service_error`); `listWorkspaces()`; `createPage({title, content, workspaceId?})` resolves workspace from `DOCMOST_DEFAULT_WORKSPACE_ID` or first available. Reads env at call time.
- [x] New `server/api/v1/docmost.ts` mounted at `/api/v1/docmost` — `GET /health` (5s in-memory cache, always 200), `GET /workspaces`, `POST /pages` [admin/operator] that loads a report by `reportId` and publishes its `content.markdown`/`content.body` as a Docmost page.
- [x] New `client/src/components/reports/DocmostHealthBanner.tsx` — green pill with workspace name + Open link when up; red banner with reason title + suggestion + Retry when down. Hidden entirely when `FF_DOCMOST` is off.
- [x] New `client/src/components/reports/PublishToDocmostButton.tsx` — gated on `useFeatureFlag('docmost')`; click → POST `/docmost/pages` with `reportId` → opens new page in tab + toast w/ "Open" action.
- [x] `client/src/pages/Reports.tsx` — banner mounted top-of-page; button wired into each report row alongside Generate-PDF and Download.
- [x] `.env.example` documents `DOCMOST_BASE_URL`, `DOCMOST_APP_URL`, `DOCMOST_APP_SECRET`, `DOCMOST_DB_PASSWORD`, `DOCMOST_REDIS_PASSWORD`, `DOCMOST_API_TOKEN`, `DOCMOST_DEFAULT_WORKSPACE_ID`.

**APIs:**
- [x] `GET /api/v1/docmost/health` — structured `{up, url, tokenConfigured, workspace?, reason?, suggestion?}`
- [x] `GET /api/v1/docmost/workspaces`
- [x] `POST /api/v1/docmost/pages` [admin/operator] — body: `{reportId?, title?, content?, workspaceId?}`

**Schema:** none (state lives in Docmost itself).

**Acceptance test (P8):**
- [x] Typecheck (`npx tsc --noEmit -p .`) clean for all Phase 8 files.
- [ ] Live "Publish to Docmost creates a reachable page" pending `docker compose --profile docmost up -d` + first-run admin setup + creating an API token + setting `DOCMOST_API_TOKEN` + flipping `FF_DOCMOST=true` in `.env`.

**Notes:**
- Docmost shares the existing `rtpi-postgres` instance with a dedicated `docmost` database to avoid spinning up another PG; init script creates the role + db on first run.
- `docmost-redis` is a separate dedicated redis (mirror of the Sysreptor pattern) so cache invalidations don't conflict with the main app's Redis.
- Page URL pattern follows Docmost's `/p/:slug` convention (with `/p/:id` fallback for older builds); the client returns the resolved URL so the UI can open it directly.
- The "Open in Docmost" deeplink in the banner goes to the Docmost root; per-page deep links come from the publish flow's response.
- Phase 8 deliberately does not iframe-embed Docmost (`Docs.tsx` page) — the optional embed was deferred. Users who want in-app docs can add a Docs sidebar entry pointing at `DOCMOST_APP_URL` directly.

---

### Phase 9 — Per-framework Deploy Button + Collective Bundle

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** L · **Flag:** `FF_FRAMEWORK_DEPLOY` · **Completed:** 2026-04-29

**Scope adjustment:** the orchestrator manages container lifecycle by **name** via dockerode (Phase 5's ContainerRuntime under the hood), not by shelling out to `docker compose`. This avoids requiring the compose CLI inside the orchestrator image and keeps the existing always-on compose layout unchanged. Compose profile-gating of C2 services (`sliver`, `adaptix`, `c3`, `loki`, `empire`) was deferred — it's a behavior change for existing deployments and not required to make the deploy/teardown flow work. Profile gating can be a small follow-up when users opt in.

**Work items:**
- [x] New `server/services/deployments/compose-driver.ts` — dockerode adapter routed via Phase 5's `containerRuntime`. `start()` is idempotent (already-running → "running"); `stop()` is idempotent (already-stopped → "stopped"); `status()` returns null on absent. Errors classified into the Phase 5 taxonomy.
- [x] New `server/services/deployments/framework-deployment-orchestrator.ts` (seam S5) — owns the desired/current state machine; `up()`/`down()` cascade to bundle members in declared order (reverse on teardown); writes audit rows to `deployment_events`; `bootstrap()` upserts deployment + dependency rows from the manifest on first call (idempotent).
- [x] New `server/config/framework-stacks.json` — manifest mapping each deployment to its named container set. 9 deployments (5 C2: empire/sliver/adaptix/c3/loki + vllm/chromium/docmost/sysreptor) and 1 bundle (`all-c2` with 5 members).
- [x] New `server/api/v1/framework-deploy.ts` mounted at `/api/v1/deployments` — six endpoints (see APIs); admin role enforced for all write paths.
- [x] New `client/src/components/deployments/DeploymentsPanel.tsx` — gated on `FF_FRAMEWORK_DEPLOY`; lists bundles + units; per-row Deploy/Teardown; auto-refresh every 15s; admin-gated buttons.
- [x] Mounted as a new "Deployments" tab in `client/src/pages/Infrastructure.tsx` (between Containers and Devices).
- [x] Schema additions: `deployments`, `deployment_events`, `deployment_dependencies` + three enums (`deployment_kind`, `deployment_state`, `deployment_event_type`); migration `0038_add_deployments.sql`.

**Deliberately not built (deferred):**
- ❌ Compose profile-gating of existing C2 services — would change `docker compose up` default behavior; breaking-change for existing deployments. The deploy/teardown flow works regardless because the orchestrator operates on container *names*, not profiles. Profile-gating can ship as a follow-up if users opt in.
- ❌ Realtime `{kind:"deployment_event"}` envelope on `/api/v1/ws/agents` — the panel polls every 15s instead. Sufficient for the typical 30-60s deploy cycle. WebSocket extension is opportunistic future work.
- ❌ Inline Deploy buttons on `Frameworks.tsx` — that page is the *security knowledge* frameworks page (ATT&CK/ATLAS/OWASP/NIST/CIS), not C2. Deploy/Teardown lives in Infrastructure → Deployments where the abstraction makes sense.

**APIs:**
- [x] `GET /api/v1/deployments` — list with current state
- [x] `POST /api/v1/deployments/:name/up` [admin]
- [x] `POST /api/v1/deployments/:name/down` [admin]
- [x] `POST /api/v1/deployments/bundles/:name/up` [admin] — collective deploy
- [x] `POST /api/v1/deployments/bundles/:name/down` [admin] — bonus, mirrors up
- [x] `GET /api/v1/deployments/:name/events?limit=`
- [x] `GET /api/v1/deployments/:name/health` — proxies ContainerRuntime checks

**Schema:**
- [x] Migration `0038_add_deployments.sql` (additive `IF NOT EXISTS`): `deployments`, `deployment_events`, `deployment_dependencies` + three enums.

**Acceptance test (P9):**
- [x] Typecheck (`npx tsc --noEmit -p .`) clean for all Phase 9 files.
- [ ] Live deploy/teardown verification pending `npm run db:push` for migration 0038 + `FF_FRAMEWORK_DEPLOY=true` + dev-server. Click "Deploy" on Sliver → `rtpi-sliver-agent` starts; click "Teardown" → it stops; "Deploy all C2" bundle starts the 5 C2 containers in order.

**Notes:**
- `bootstrap()` is idempotent: it upserts manifest deployments on every list/up/down call (only inserts/updates rows that don't match). User-edited rows in the deployments table aren't overwritten because the upsert only sets `kind`/`composeProfile`/`params`, not `desiredState`/`currentState`.
- For containers absent from the host (never created, e.g. user has never run `compose --profile X up`), the driver returns `outcome: "absent"` with the structured `container_not_found` error from the Phase 5 classifier. UI surfaces this as a deploy "error" state and the user can run the compose command themselves.
- Bundle teardown reverses member order, mirroring the start sequence.

---

### Phase 10 — KASM Hardening + Nested Containers

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** L · **Flag:** `FF_KASM_HARDENED` · **Completed:** 2026-04-29

**Work items:**
- [x] `server/services/kasm-workspace-manager.ts` — three-mode TLS resolution (`pinned` via `KASM_CA_PATH` / `insecure` via `RTPI_KASM_INSECURE_TLS=1` with explicit warn log / `system` default). `tlsMode` exposed via `getRuntimeHealth()`.
- [x] Inline circuit-breaker on the axios client: 5 consecutive 5xx/transport failures → open for 60s. 4xx responses (auth/user errors) explicitly do NOT trip the breaker. Successful requests reset the counter.
- [x] `docker-compose.yml` — new `docker-socket-proxy` (tecnativa/docker-socket-proxy) under `--profile kasm`, bound to `127.0.0.1:2375`. Verb allowlist: `CONTAINERS=1`, `EXEC=1`, `IMAGES=1`, `INFO=1`, `POST=1`, `DELETE=1`. Explicit deny: `NETWORKS`, `VOLUMES`, `SERVICES`, `SWARM`, `TASKS`, `SYSTEM`, `BUILD`, `COMMIT`, `CONFIGS`, `DISTRIBUTION`, `PLUGINS`, `SECRETS`, `SESSION`. `/_ping` healthcheck.
- [x] `buildDockerOpts()` helper: when `FF_KASM_HARDENED=1`, dockerode connects to `docker-socket-proxy:2375`; otherwise default socket. Override hosts via `RTPI_DOCKER_PROXY_HOST`/`RTPI_DOCKER_PROXY_PORT`.
- [x] Nested container helper: `spawnNested({parentSessionId, image, name?, cmd?, env?, extraLabels?})` creates a child container with `rtpi.parent_session_id=<id>` + `rtpi.kind=nested-workspace` labels on `rtpi-network`. `listNested(parentSessionId)` queries by label. `removeNestedContainer(id)` is idempotent (already-gone → ok).
- [x] `.env.example` documents `RTPI_DOCKER_PROXY_HOST`, `RTPI_DOCKER_PROXY_PORT`, `KASM_CA_PATH`, `RTPI_KASM_INSECURE_TLS`.

**APIs:**
- [x] `GET /api/v1/kasm-workspaces/runtime-health` — TLS mode + breaker state (no network round-trip)
- [x] `GET /api/v1/kasm-workspaces/:id/children` — nested containers for a workspace's session
- [x] `POST /api/v1/kasm-workspaces/:id/nested` [admin/operator]
- [x] `DELETE /api/v1/kasm-workspaces/:id/nested/:containerId` [admin/operator]

**Schema:** none — state lives in container labels + KASM API + existing `kasmWorkspaces` table.

**Deliberately not built (deferred):**
- ❌ `rtpi-ca-init` one-shot init container — out of scope for this slice. Users with KASM behind a self-signed CA pin via `KASM_CA_PATH=/path/to/ca.crt`. The init-container pattern (mirror of `kasm-db-init`) lands as a follow-up if cert-rotation becomes friction.
- ❌ Frontend "Spawn Nested Workspace" UI — Phase 10 ships hardening primitives only. The API surface is in place; UI extension is a small follow-up that drops into the existing `WorkspaceTab` component.

**Acceptance test (P10):**
- [x] Typecheck (`npx tsc --noEmit -p .`) clean for all Phase 10 files.
- [ ] Live test pending `docker compose --profile kasm up -d` + `FF_KASM_HARDENED=true` + dev-server. Spawn workspace → reachable. Spawn nested via `POST /:id/nested` → labeled child appears in `GET /:id/children`. Attempt blocked verb: `curl -X POST http://127.0.0.1:2375/networks/create` → 403.

**Notes:**
- TLS resolution priority documented in `.env.example`: `KASM_CA_PATH` (pinned, recommended) → `RTPI_KASM_INSECURE_TLS=1` (dev fallback, logs a warning) → system trust store.
- The breaker explicitly ignores 4xx so an invalid token doesn't lock out the operator. Only 5xx + transport errors count.
- Nested containers run on `rtpi-network` so the parent workspace can reach them by name. `extraLabels` lets callers add domain-specific labels (e.g. `rtpi.scenario=phishing-lab`).
- The proxy keeps `POST=1` because `/containers/:id/exec` requires POST; the verb allowlist (`CONTAINERS=1`, etc.) is the actual security boundary, not the HTTP method.

---

## Acceptance Test Matrix

Cross-phase view of verification status. Update this as phases land.

| ID | Test | Phase | Status | Last Run | Notes |
|----|------|-------|--------|----------|-------|
| P0 | `/settings/features` returns flag map | 0 | 🟡 Verified (unit) | 2026-04-28 | Module-level smoke test passes; live `curl` verification pending dev-server run |
| P1 | Sidebar order + `/cti` 200 + a11y | 1 | 🟡 Verified (typecheck) | 2026-04-28 | Typecheck passes; live `/cti` + a11y verification pending dev-server run |
| P2 | Sysreptor stop/start → banner reflects | 2 | 🟡 Verified (smoke + typecheck) | 2026-04-28 | Three failure modes classified correctly in unit smoke test; live stop/start verification pending dev-server run |
| P3 | PDF render + persistence + cleanup | 3 | 🟡 Verified (typecheck) | 2026-04-28 | Code + migration + sidecar landed; live render pending `npm install` + `docker compose --profile pdf up -d` + migration apply by user |
| P4 | Bind tool to OWASP/NIST/CIS persists | 4 | 🟡 Verified (typecheck) | 2026-04-29 | API + service + UI shipped; live round-trip pending `npm run db:push` to apply migration 0033 |
| P5 | Structured error on stopped container | 5 | 🟡 Verified (smoke + typecheck) | 2026-04-29 | All six classifications correct in unit smoke test; live stop-container verification pending `npm run db:push` for migration 0034 + dev-server run |
| P6 | vLLM tool call + Ollama fallback | 6 | 🟡 Verified (smoke + typecheck) | 2026-04-29 | Registry constructs all 4 providers correctly; vLLM auto-selected as default when configured. Live vLLM round-trip pending `--profile vllm up -d` + migration 0035 |
| P7 | TAXII feed + semantic search + ATLAS cron | 7 | 🟡 Verified (typecheck) | 2026-04-29 | Code + migrations + cron loops shipped. Live TAXII pull + HNSW EXPLAIN pending `npm run db:push` 0036/0037 + `RTPI_CTI_ENABLED=true` + embedding model |
| P8 | Docmost publish + health | 8 | 🟡 Verified (typecheck) | 2026-04-29 | Compose service + API + UI shipped; live publish test pending `--profile docmost up -d` + first-run admin token + `FF_DOCMOST=true` |
| P9 | Sliver deploy + bundle deploy + RBAC | 9 | 🟡 Verified (typecheck) | 2026-04-29 | Orchestrator + API + UI shipped; live test pending `npm run db:push` 0038 + `FF_FRAMEWORK_DEPLOY=true` + dev-server |
| P10 | KASM nested + cert pinning + verb deny | 10 | 🟡 Verified (typecheck) | 2026-04-29 | Three-mode TLS, circuit-breaker, socket-proxy, nested helper + API shipped. Live test pending `--profile kasm up -d` + `FF_KASM_HARDENED=true` + dev-server |

**Status legend:** ⏸ Pending · 🟡 Running · ✅ Pass · 🔴 Fail · 🟦 Skipped

---

## Risk Register Status

Mirrors the v2.9.1 plan's R1–R5. Update status as risks are mitigated, realized, or retired.

| ID | Risk | Phase | Status | Mitigation Status | Notes |
|----|------|-------|--------|-------------------|-------|
| R1 | vLLM Blackwell sm_120 build fragility | 6 | 🟡 Mitigating | Compose service uses `vllm/vllm-openai:nightly` (mutable tag); registry's `auto` fallback picks Ollama if vLLM never starts. Source-build fallback path documented in [Inference-Agent-Architecture.md](references/Inference-Agent-Architecture.md). Pinned-SHA OCI artifact deferred until first stable Blackwell release. | 2026-04-29 |
| R2 | Puppeteer Linux deps / image bloat | 3 | ✅ Mitigated | `puppeteer-core` (no Chromium download) + `chromedp/headless-shell` sidecar with `read_only`, `no-new-privileges`, `cap_drop: ALL`, tmpfs /tmp — bound to 127.0.0.1:9222 only | 2026-04-28 |
| R3 | Docker socket exposure (deploy + KASM) | 9, 10 | ✅ Mitigated | Phase 9: admin RBAC + `deployment_events` audit. Phase 10: `tecnativa/docker-socket-proxy` with verb allowlist (containers/exec/images:read/info only — networks/volumes/swarm explicitly denied) gated by `FF_KASM_HARDENED`. Disallowed verbs return 403. | 2026-04-29 |
| R4 | CTI ingestion volume + HNSW rebuild | 7 | 🟡 Mitigating | HNSW index built per-source by Postgres lazily; `cadenceSeconds` per source caps poll rate; per-source `addedAfter` filter for incremental TAXII fetches. Retention TTL + partitioning still pending — flag for v2.9.x if real-world volume warrants. | 2026-04-29 |
| R5 | Sysreptor profile vs crash ambiguity | 2 | ✅ Mitigated | `{up, profileEnabled, version, reason, suggestion}` shape ships in commit (Phase 2). Six-state classifier distinguishes profile_not_enabled / service_unreachable / timeout / auth_error / service_error | 2026-04-28 |

**Risk status legend:** 🟦 Open · 🟡 Mitigating · ✅ Mitigated · 🔴 Realized · ⚪ Retired

---

## Decision Log

Append-only log of scope, architecture, or process decisions made during implementation. Most recent on top.

| Date | Decision | Rationale | Phase(s) Affected |
|------|----------|-----------|-------------------|
| 2026-04-29 | Phase 10 circuit-breaker explicitly ignores 4xx | Auth/user errors (e.g. invalid token) shouldn't open the breaker and lock out the operator. Only 5xx + transport errors count. | 10 |
| 2026-04-29 | Phase 10 ships TLS hardening + nested-container API but defers the iframe UI extension and the `rtpi-ca-init` one-shot | Hardening primitives are the security-critical pieces; UI/init-container drop into existing surfaces and can ship as small follow-ups when cert rotation or operator workflow demand it. | 10 |
| 2026-04-29 | Phase 10 socket-proxy keeps `POST=1` and `DELETE=1` enabled | `/containers/:id/exec` requires POST; nested-container teardown requires DELETE. The verb allowlist (`CONTAINERS=1`, `IMAGES=1`, `EXEC=1`, etc.) is the actual security boundary — HTTP-method gating would block legitimate exec calls without preventing escalation. | 10 |
| 2026-04-29 | Phase 9 orchestrator manages container lifecycle by **name** via dockerode (not by shelling out to `docker compose`) | Avoids requiring the compose CLI inside the orchestrator image; works with the existing always-on compose layout; reuses Phase 5's ContainerRuntime + error taxonomy. | 9 |
| 2026-04-29 | Phase 9 deferred compose profile-gating of existing C2 services | Adding profiles to `offsec-empire/sliver/adaptix/c3/loki` would change `docker compose up` default behavior and break existing deployments. Deploy/teardown works on container names regardless. Profile-gating can ship as an opt-in follow-up. | 9 |
| 2026-04-29 | Phase 9 polls every 15s instead of streaming via WebSocket | Typical deploy/teardown is 30-60s; polling is good enough at this cadence and avoids extending the `/ws/agents` envelope. Real-time deploy events stay opportunistic future work. | 9 |
| 2026-04-29 | Deploy/Teardown UI lives in Infrastructure → Deployments, not Frameworks.tsx | Frameworks.tsx is the security-knowledge page (ATT&CK/ATLAS/OWASP/NIST/CIS); the C2 deploy concept is unrelated. Mixing them confuses users. | 9 |
| 2026-04-29 | Phase 8 reuses existing `rtpi-postgres` for Docmost (separate `docmost` DB) instead of a dedicated PG instance | Avoids spinning up another Postgres process for a single integration. Mirrors the SysReptor pattern. Init script `01-init-databases.sql` creates the role + db on first volume init. | 8 |
| 2026-04-29 | Phase 8 ships dedicated `docmost-redis` (not shared with main app's redis) | Mirrors SysReptor's pattern; cache invalidations don't conflict with the main app's Redis. Tradeoff: one extra container; gated under `--profile docmost` so default deployments don't pay the cost. | 8 |
| 2026-04-29 | Phase 8 deferred the optional `Docs.tsx` iframe-embed page | Out of scope for v2.9.1; users can add a sidebar link pointing at `DOCMOST_APP_URL` if they want in-app docs immediately. Iframe embedding has CSP/auth-bridging complications best handled in a follow-up. | 8 |
| 2026-04-29 | Phase 7 hard-codes embedding dim to 1536 (schema column type) | Schema constraint; switching dim would require a migration that rewrites the `embedding` column. Embedder rejects mismatches with a structured error so user can switch models cleanly. Default `text-embedding-3-small` (OpenAI) matches naturally; vLLM/Ollama users pick a 1536-dim model. | 7 |
| 2026-04-29 | Phase 7 reuses existing `api` fetch wrapper instead of introducing react-query (seam S9) | New CTI surfaces stay consistent with rest of codebase; react-query rollout is opportunistic future work, not a v2.9.1 blocker | 7 |
| 2026-04-29 | CTI cron loop only starts when `RTPI_CTI_ENABLED=true` | Default-off keeps quiet for users who don't want background polling; explicit opt-in matches the `--profile sysreptor` / `--profile pdf` pattern | 7 |
| 2026-04-29 | Phase 7 drops the "Reports" CTI tab from the Phase 1 stub (now 3 tabs) | CTI digests / analyst reports are a separate concern; out of scope for v2.9.1 to keep the slice focused | 7 |
| 2026-04-29 | ATT&CK STIX import runs weekly, not daily | ~50MB bundle, 100k+ relationships; daily cadence adds no signal but consumes DB resources | 7 |
| 2026-04-29 | Phase 6 ships Path A (vLLM built-in tool calling + Phase 5 mcp-invoker) instead of Path B (Qwen-Agent Python sidecar) | Path A satisfies the "vLLM serving Qwen with MCP tool calling" requirement using infra we already shipped. Path B doubles moving parts for no immediate user-visible win. Qwen-Agent stays opt-in for users who specifically want its agent-loop semantics. | 6 |
| 2026-04-29 | InferenceProviderRegistry wraps existing clients rather than refactoring them | Concrete providers delegate to `ollamaManager`, `getOpenAIClient()`, `getAnthropicClient()`. Refactoring those out of their callers would churn ~20 files for no immediate benefit; lazy migration when files are touched naturally | 6 |
| 2026-04-29 | Per-agent provider override stored in `agents.inference_provider_id` (TEXT, nullable) instead of a foreign key | Provider list is fixed in code (4 entries), not data. FK would imply a `providers` table that doesn't otherwise exist — overkill. | 6 |
| 2026-04-29 | PATCH /inference/providers/:id writes `process.env` AND `.env` | Mirrors `/api/v1/settings/llm` pattern: process.env update for immediate effect, .env write for survival across restart | 6 |
| 2026-04-29 | Phase 5 readiness cache defaults to 2s TTL (`CONTAINER_READINESS_CACHE_MS`) | Long enough to keep back-to-back execs near zero overhead; short enough to surface a stopped container in the next request cycle. Acceptable freshness/preflight tradeoff | 5 |
| 2026-04-29 | MCPInvoker accesses MCP child processes via new `mcp-server-manager.getChildProcess()` rather than refactoring the manager into a transport layer | Keeps single-responsibility: manager owns lifecycle, invoker owns protocol. Smaller blast radius in case of bugs | 5 |
| 2026-04-29 | MCP probe DB writes wrapped in try/catch so DB failures never mask probe result | Probe state is observability data — must not falsely flip a healthy server to "down" because of an unrelated DB blip | 5 |
| 2026-04-29 | Phase 5 deferred deeper `skill-import-pipeline` bookkeeping to Phase 9 | Comprehensive container-build event emission requires `deployment_events` table (Phase 9) — partial fix in Phase 5 would just churn the file twice | 5, 9 |
| 2026-04-29 | `framework_bindings` is a new table, not a column-add to `framework_mappings` | Bindings link framework elements to *executable* assets (tools/agents/workflows); mappings link framework↔framework. Overloading would conflate two distinct concepts | 4 |
| 2026-04-29 | `target_id` FK is by convention (not DB-enforced) | Target table varies by `binding_kind`; would require polymorphic FK — service layer resolves names instead, deleted-target rows surface `targetName: null` | 4 |
| 2026-04-29 | `workflow` binding kind reserved in enum but UI only exposes tool + agent | Workflow primitives stabilize in Phase 7 (CTI auto-import) and Phase 9 (deploy bundles) — adding the enum value now avoids a future schema migration | 4 |
| 2026-04-29 | Bindings panel lazy-loads tool/agent options on first hover/focus | The OWASP LLM page expands ~10 panels at once if user expands all rows; eager-load would 20× the network cost on initial render | 4 |
| 2026-04-28 | Phase 3 chromium sidecar gated under `--profile pdf` (not always-on) | Image is small but unused in default deployments; opting in mirrors the `--profile sysreptor` pattern | 3 |
| 2026-04-28 | PDF generator falls back to HTML on disk when sidecar unreachable | Caller always gets a downloadable artifact; explicit failure mode documented (filename ends in `.html`) | 3 |
| 2026-04-28 | `pdf_jobs` retention defaults to 24h; cleanup is hourly via `setInterval(...).unref()` | Plan calls for 24h cleanup; `unref` lets the timer not block process exit; `NODE_ENV=test` skip prevents test interference | 3 |
| 2026-04-28 | Sysreptor `/health` returns 200 even when service is down (body carries the diagnostic) | Avoids spurious 500s in UI; lets the client treat health as a structured value rather than an error | 2 |
| 2026-04-28 | 5s server-side cache on health probe | Multiple UI components mount `useSysReptorHealth`; cache prevents hammering Sysreptor on every render | 2 |
| 2026-04-28 | Six-state `HealthReason` union with per-state suggestion text (server side) + matching client-side fallback in banner | Distinguishing `profile_not_enabled` from `service_unreachable` from `auth_error` lets the UI emit actionable remediation — exactly the v2.9.1 plan's R5 mitigation | 2 |
| 2026-04-28 | Picked `Radar` (lucide) icon for CTI sidebar entry | Available without new dep; visually distinct from `Shield` (Frameworks) and `Microscope` (R&D) | 1 |
| 2026-04-28 | CTI tabs are stubs with explicit "Wired by Phase 7" copy in empty states | Sets clear contract — Phase 1 ships the route, Phase 7 fills the data | 1, 7 |
| 2026-04-28 | Phase 0 features endpoint mounted before admin role guard | Client must read flag map to gate UI — would 403 if admin-only | 0 |
| 2026-04-28 | Six flags scaffolded (incl. `FF_VLLM_AGENT`, `FF_CTI_VECTOR`); `RTPI_INFERENCE_PROVIDER` and `RTPI_DOCMOST_ENABLED` left as separate env vars (not in feature map) | Flag map is for boolean UI gates; provider selection and service toggles are config, not flags | 0, 6, 7, 8 |
| 2026-04-28 | Ship vLLM full (not stubbed) in v2.9.1 | User direction; XL effort accepted | 6 |
| 2026-04-28 | All four optional phases (PDF, Docmost, Deploy, KASM) in v2.9.1 scope | User direction; no deferral to v2.9.2+ | 3, 8, 9, 10 |
| 2026-04-28 | Preserve v2.9.1 doc outline + image refs; expand inline | User direction; minimum disruption to existing screenshot narrative | all |
| 2026-04-28 | Equal depth across all phases in plan doc | User direction | all |

---

## Blockers / Open Questions

Running list of items blocking progress or needing clarification. Remove when resolved (move resolution to Decision Log if architectural).

| Date Raised | Item | Phase | Owner | Status |
|-------------|------|-------|-------|--------|
| 2026-04-28 | ~~v2.9.2 VPN plan deleted~~ — resolved: file was renamed from `2.8/` to `2.9/` (git detected as rename in commit `c156222`), plan still exists | v2.9.2 | claude | ✅ Resolved |
| 2026-04-28 | Owner assignments — solo or multi-person? Currently all "Unassigned" | all | Unassigned | 🟦 Open |
| 2026-04-28 | Hugging Face availability of Qwen3.5-9B Q6_K at deploy time (Q5_K_M fallback documented) | 6 | Unassigned | 🟦 Open |
| 2026-04-28 | Docmost auth: SSO integration vs token-only — affects scope | 8 | Unassigned | 🟦 Open |

**Status legend:** 🟦 Open · 🟡 Investigating · ✅ Resolved · 🔴 Escalated

---

## v2.9.2 — VPN Infrastructure (Unscheduled)

**Status:** 🟦 Unscheduled · **Plan doc:** deleted (working tree status `D`)

The original `v2.9.2-vpn-infrastructure-integration.md` was deleted. No work items tracked here until the plan is re-created. The four phases referenced in the v2.8 status doc were:

| Phase | Component | Status |
|-------|-----------|--------|
| 1 | VPN Manager Container | 🟦 Unscheduled |
| 2 | Backend VPN Service | 🟦 Unscheduled |
| 3 | Frontend VPN Management | 🟦 Unscheduled |
| 4 | Container Routing & Testing | 🟦 Unscheduled |

**Action:** decide whether to restore the v2.9.2 plan, fold VPN work into v2.9.1, or defer to v2.10.

---

## v2.9.3 — Default MCP Integrations

**Status:** 🟡 In Progress · **Plan doc:** [v2.9.3-default-mcp-integrations.md](v2.9.3-default-mcp-integrations.md) · **Flag:** `FF_DEFAULT_MCP_SERVERS`

Seeds eleven MCP servers as built-in defaults on top of the v2.9.1 Phase 5 supervisor. Schema-additive, idempotent sync, managed-lifecycle endpoints, and a settings-page surface.

| Phase | Component | Status |
|-------|-----------|--------|
| 1 | Schema scaffold + feature flag (`seed_key`, `is_managed`) | 🟡 In Progress |
| 2 | Default catalog + idempotent sync | 🟡 In Progress |
| 3 | Managed-lifecycle API (start/stop/PATCH secrets) | 🟡 In Progress |
| 4 | Frontend surface (catalog UI, configure dialog) | 🟡 In Progress |
| 5 | Acceptance + docs | 📋 Not Started |

**In-flight code observed in working tree (uncommitted):**
- `migrations/0039_add_mcp_seed_key.sql`, `0040_seed_default_mcp_servers.sql`, `0041_repair_default_mcp_servers.sql`
- `server/services/mcp/` (catalog + sync modules)
- `client/src/components/mcp/`, `client/src/hooks/useMcpCatalog.ts`
- `tests/unit/services/mcp-catalog.test.ts`, `tests/unit/services/mcp-preflight.test.ts`

Per-phase % to be confirmed by reading the plan against the in-flight code.

---

## v2.9.4 — Google OAuth 2.0 Web Client Bootstrap

**Status:** 🟡 In Progress · **Plan doc:** [v2.9.4-google-oauth-automation.md](v2.9.4-google-oauth-automation.md) · **Flag:** none (one-shot operator runbook)

One-time per-Workspace-tenant bash bootstrap (`setup-google-oauth.sh`) that mints a Google Web OAuth 2.0 client and writes credentials into `.env`. Prerequisite for v2.9.5. No phased breakdown — one script + a four-stage operator runbook.

| Stage | Component | Status |
|-------|-----------|--------|
| 1 | Bootstrap script: prereqs → APIs → brand → client → `.env` ([`setup/setup-google-oauth.sh`](../../../setup/setup-google-oauth.sh)) | ✅ Complete |
| 2 | Move secret to Google Secret Manager | 📋 Not Started |
| 3 | Verify consent screen (External user type) | 📋 Not Started |
| 4 | Hand off `(client_id, secret, workspace_domain)` to v2.9.5 tfvars | 📋 Not Started |

**Stage 1 notes:** Script is 255 lines, `bash -n` clean. Operator must run `chmod +x setup/setup-google-oauth.sh` before first use (file is currently mode 664 — the execute bit was not settable in the implementation environment). Sibling scripts in `setup/` are mode 775, so matching that is the right end state.

---

## v2.9.5 — Cloudflare Access Automation (Multi-Tenant SSO + Authorization)

**Status:** 📋 Planning · **Plan doc:** [v2.9.5-cloudflare-access-automation.md](v2.9.5-cloudflare-access-automation.md) · **Flag:** `FF_CLOUDFLARE_ACCESS` (+ `FF_DEPRECATE_DIRECT_OAUTH`)

Move RTPI's user-facing SSO + authorization to Cloudflare Access in front of every public RTPI service, configured entirely via Terraform. Three Workspace tenants (`attck.nexus`, `c3s.nexus`, `d3fend.nexus`), N IdPs, M Access apps. Depends on v2.9.4 for Google client IDs.

| Phase | Component | Status |
|-------|-----------|--------|
| 1 | Terraform skeleton (`infra/cloudflare-access/`) | 📋 Not Started |
| 2 | Per-tenant IdP resources | 📋 Not Started |
| 3 | Per-app Access applications + policies | 📋 Not Started |
| 4 | `verifyCloudflareJWT` middleware | 📋 Not Started |
| 5 | Group-to-role mapping (SCIM) | 📋 Not Started |
| 6 | Direct-OAuth deprecation gate | 📋 Not Started |

---

## v2.9.5.1 — `catch` Browser-Recon Integration (Split-Perimeter)

**Status:** 📋 Planning · **Plan doc:** [v2.9.5.1-catch-recon-integration.md](v2.9.5.1-catch-recon-integration.md) · **Flag:** `FF_CATCH`

Bring [`cmndcntrlcyber/catch`](https://github.com/cmndcntrlcyber/catch) onto the parent RTPI side as a Docker service with a split-perimeter under v2.9.5 Access: target-facing `lure.<tenant>.nexus` is open, operator-facing `catch-admin.<tenant>.nexus` is gated. Depends on v2.9.5.

| Phase | Component | Status |
|-------|-----------|--------|
| 1 | Vendor catch as `services/catch/` submodule + Dockerfile + compose | 📋 Not Started |
| 2 | DNS + TLS for both hostnames via `services.manifest` | 📋 Not Started |
| 3 | nginx vhost wiring + Express vhost guard | 📋 Not Started |
| 4 | Cloudflare Access app on admin hostname only (tfvars) | 📋 Not Started |
| 5 | Replace `access.log` with `catch_events` table + ingest API | 📋 Not Started |
| 6 | Operator dashboard tab + campaign CRUD | 📋 Not Started |

---

## v2.9.6 — `nexus-mesh` Communications Layer (rust-nexus)

**Status:** 📋 Planning · **Plan doc:** [v2.9.6-nexus-mesh-comms.md](v2.9.6-nexus-mesh-comms.md) · **Flag:** `mesh` (Cargo feature, agent + bridge crates)

Replicates GhostWire-style mesh communication into rust-nexus by re-implementing on `libp2p` (avoiding GhostWire's AGPL). Bridge model: C2 stays gRPC-native; dedicated `nexus-mesh-bridge` containers connect mesh ↔ C2. Per-engagement 128-bit `engagement_id` namespaces both DHT protocol and Gossipsub topics. **Lives in the `rust-nexus` submodule**; mirror plan at [`rust-nexus/docs/development/Mesh-Comms-Plan.md`](../../../rust-nexus/docs/development/Mesh-Comms-Plan.md).

| Phase | Component | Status |
|-------|-----------|--------|
| 0 | Decisions, scaffold, design lock-in | 📋 Not Started |
| 1 | Application-layer Ed25519 identity in `nexus-common` | 📋 Not Started |
| 2 | Minimal libp2p node in `nexus-mesh` | 📋 Not Started |
| 3 | Kademlia DHT discovery | 📋 Not Started |
| 4 | Gossipsub with prost-encoded `MeshMessage` | 📋 Not Started |
| 5 | End-to-end sealed envelopes above Gossipsub | 📋 Not Started |
| 6 | Store-and-forward DTN queue | 📋 Not Started |
| 7 | Agent transport + `nexus-mesh-bridge` (first user-visible) | 📋 Not Started |
| 8 | Multi-hop relay + routing policy | 📋 Not Started |
| 9 | Optional: Sphinx mix-style sender anonymity | 📋 Not Started |
| 10 | Operational polish + `init-engagement` subcommand | 📋 Not Started |
| 11 | Optional: WebRTC transport (egress evasion + NAT) | 📋 Not Started |

---

---

## v2.9.6 / 6.1 / 6.2 — Joint Initiative (mesh + GML + A2A)

**Status:** 🟡 In Progress · **Joint plan:** `~/.claude/plans/please-review-the-following-glimmering-hartmanis.md` · **ADRs:** [009](../../adrs/adr-009-tonic-014-bump.md), [010](../../adrs/adr-010-a2a-proto-canonical-location.md), [011](../../adrs/adr-011-agent-message-bus-wrap-strategy.md), [012](../../adrs/adr-012-gml-shadow-only-scope.md)

User-locked decisions: Tonic upgrade as dedicated phase (A1); GML scope = shadow + viz only (no throttling); MatrixA2A Python lib at `services/a2a/`; mesh through Phase 10 (no Sphinx).

| Phase | Component | Status | Notes |
|-------|-----------|--------|-------|
| A0 | Joint scaffold (flags, schema stub, ADRs, threat model, `nexus-mesh/` empty crate, CI matrix) | ✅ Complete | Landed 2026-05-06. See work items below. |
| A1 | Tonic 0.10 → 0.14 bump (rust-nexus workspace) | 📋 Not Started | Isolated branch; blocks Phase D. |
| B1 | Mesh identity (`nexus-common::identity`) — v2.9.6 P1 | 📋 Not Started | Parallelizable, doesn't need Tonic 0.14. |
| B2 | A2A in-process MVP at `services/a2a/` — v2.9.6.2 P1 | 📋 Not Started | Parallelizable; reuses Ed25519 from B1. |
| B3 | SIEM alert sink + hourly snapshot job + GML inference scaffold | 📋 Not Started | Parallelizable; starts data accumulation early. |
| C | Mesh transports (libp2p node → DTN) — v2.9.6 P2–P6 | 📋 Not Started | Sequential sub-phases. |
| D | A2A gRPC interop (vendor proto, `crates/a2a-matrix-rs`) — v2.9.6.2 P2 | 📋 Not Started | Requires A1. |
| E | Mesh user-visible integration — v2.9.6 P7 | 📋 Not Started | First user-visible mesh value. |
| F | Mesh hardening — v2.9.6 P8 | 📋 Not Started | TTL, relay-only mode, deny-list. |
| G | GML shadow mode — v2.9.6.1 | 📋 Not Started | Train, predict, viz. No enforcement. |
| H0 | A2A capability matrix dual-write window | 📋 Not Started | ≥1 week observation before H. |
| H | A2A security + adapters — v2.9.6.2 P3 | 📋 Not Started | Wraps `AgentMessageBus`. |
| I | Operations / Intelligence / Automation surfaces (read-only) | 📋 Not Started | Cross-cutting GML display. |
| J | Rollout & polish — v2.9.6 P10 + A2A polish + GML graduation gates | 📋 Not Started | Operator-facing docs; no default-on flips in this initiative. |

### Phase A0 — Joint Scaffold

**Status:** ✅ Complete · **% Complete:** 100% · **Owner:** claude · **Effort:** S · **Completed:** 2026-05-06

**Work items:**
- [x] `shared/feature-flags.ts` — added `nexusMesh`, `matrixA2a`, `gmlTelemetry`, `a2aCapabilityGate` (data-driven; client/server hooks at `client/src/lib/feature-flags.ts` + `server/api/v1/settings.ts` pick up automatically).
- [x] `.env.example` — four new flags documented.
- [x] `rust-nexus/Cargo.toml` — `nexus-mesh` added to `[workspace] members`.
- [x] `rust-nexus/nexus-mesh/{Cargo.toml,src/lib.rs}` — empty crate that compiles.
- [x] `rust-nexus/nexus-agent/Cargo.toml` — `mesh = []` feature added.
- [x] `rust-nexus/nexus-infra/Cargo.toml` — `[features] default = []`, `mesh = []` added.
- [x] `rust-nexus/config/agent-linux.toml` and `config/examples/nexus-config.toml` — commented `[mesh]` stub appended.
- [x] `workflows/ci.yml` — `rust-nexus` job with `default` / `mesh` / `all-features` matrix variants.
- [x] `docs/adrs/{README.md, adr-009..012}.md` — four ADRs landing the joint-initiative decisions.
- [x] `rust-nexus/docs/development/Mesh-Threat-Model.md` — required v2.9.6 P0 deliverable.

**Schema migration deferred to Phase A0.5 / B-track:** the four new tables (`siemAlerts`, `agentSwarmGraphs`, `a2aAgentCards`, `a2aCapabilityMatrix`) and the `agents` extensions (`peerId`, `isA2aEnabled`, `meshEnabled`) are scheduled but not yet applied — this avoids forcing a `db:push` for downstream developers before the consuming code is written. Migration lands at the start of B3.

**Acceptance test (A0):**
- [x] `npm run build` — no new errors introduced (no TS code changes beyond the data-only flag map and `.env.example`).
- [ ] `cargo check --workspace` and `cargo check --workspace --all-features` — **deferred**: cargo not installed in this environment. Verified by inspection that empty crate compiles (Cargo.toml + minimal lib.rs are syntactically valid). CI matrix added in `workflows/ci.yml` will run these on the next push.
- [x] Feature flags readable from `/api/v1/settings/features` — verified by inspection (data-driven, no hardcoded list to update).
- [x] ADRs and threat model exist and are cross-referenced.



- **Per-PR**: bump phase % and tick work-item checkboxes when a PR lands.
- **Per-week**: review Risk Register status, prune resolved Blockers, update overall %.
- **At phase completion**: flip phase status to ✅, run acceptance test, record result in Acceptance Test Matrix.
- **At release sign-off**: all P0–P10 must be ✅ Pass; all R1–R5 must be ✅ Mitigated or ⚪ Retired.
