# v3.1.11 — Database & Schema

**Grade: A** | Target: A | Priority: 11

## Overview

`shared/schema.ts` (3,689 lines) with `strict: true` TypeScript. 20+ enums for type safety. UUID primary keys with `defaultRandom()`. pgvector for embeddings, tsvector for full-text search. Well-organized by domain (auth, operations, agents, MCP, tools, reports). Drizzle ORM prevents SQL injection. Defensive comments protecting destructive operations (FTS column guard).

## Evidence

- **Type safety**: 20+ enums covering user roles, agent types, task statuses, workflow types, C2 frameworks, vulnerability severities, etc. All status fields use enums rather than bare strings.
- **Primary keys**: UUID with `defaultRandom()` across all tables — no sequential integers exposed.
- **Advanced features**: pgvector for embedding storage, tsvector for full-text search, custom types for domain-specific fields.
- **Organization**: Sections clearly separated by domain: auth (users, sessions, apiKeys), operations, agents, MCP, tools, reports, infrastructure, C2, offsec R&D.
- **Defensive**: Comments like `-- FTS column: DO NOT DROP` guard against destructive migration mistakes.
- **ORM**: Drizzle ORM with parameterized queries prevents SQL injection by design.
- **No critical gaps identified.**

## Repair Items

No critical repairs needed.

| # | Item | Effort | Files | Status |
|---|------|--------|-------|--------|
| 1 | Optional: add database migration smoke test (apply → rollback → reapply) | S | `tests/integration/database-migration-smoke.test.ts` | ✅ Done |

## Verification

1. Run `npm run db:push` — confirm schema applies cleanly
2. ✅ `npx vitest run tests/integration/database-migration-smoke.test.ts` — apply → rollback → reapply cycle completes (3/3 pass, ~7s)
