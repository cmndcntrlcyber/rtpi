# v3.1.12 — Agent System

**Grade: A** | Target: A | Priority: 12 (lowest)

## Overview

Multi-pattern orchestration (sequential pipeline, template-driven, attack tree branching, autonomous tool loop). Anti-fabrication regression guard tested — stub methods that returned fictional success strings have been removed and guarded against re-introduction. Dynamic workflow orchestrator with exponential backoff retries (configurable `maxRetries`, `backoffMultiplier`). Capability-based agent matching with topological sort. Comprehensive audit trail via `workflowLogs` table. 9 agent-related test files. Per-agent AI model/provider override. 14 feature flags.

## Evidence

- **Orchestration** (`server/services/agent-workflow-orchestrator.ts`, 4,517 lines): Sequential pipelines, template-driven workflows, attack tree branching (auxiliary→exploit→post-exploitation), autonomous tool execution loops.
- **Anti-fabrication** (`tests/unit/server/agent-tool-connector-no-fabrication.test.ts`): Regression guard ensures no hardcoded success strings. Old stub methods removed. Any backend unavailable → THROW, never fake success.
- **Dynamic orchestrator** (`server/services/dynamic-workflow-orchestrator.ts`, 1,471 lines): Capability-based agent matching, topological sort, dependency conditions (`context_has`, `context_equals`, `capability_available`, `custom`), safe expression evaluation (no `eval()`), exponential backoff retries.
- **Tool connector** (`server/services/agent-tool-connector.ts`, 978 lines): Three real backends (registry, Docker, MCP). Canonical `AgentToolResult` interface. 60s timeout per tool call.
- **Audit trail**: Every phase logged to `workflowLogs` table. Levels: info, warning, error, debug. Fire-and-forget logging never blocks workflow.
- **Configuration**: Per-agent AI provider/model override, 14 feature flags, environment-driven system settings.
- **Tests**: 9 agent-related test files (1,755 lines). Anti-fabrication regression, agent loop MCP, offsec registry, message bus, operations manager, R&D team, reporter agents, REST API, memory integration.

## Repair Items

No critical repairs needed.

| # | Item | Effort | Files |
|---|------|--------|-------|
| 1 | Optional: add isolated retry/timeout unit tests for backoff calculations | S | `tests/unit/services/` |
| 2 | Optional: resolve WebSocket auth TODO (`agent-websocket-manager.ts:116`) | S | `server/services/agent-websocket-manager.ts` |

## Verification

1. If retry tests added: run and confirm backoff calculation matches `baseDelay * backoffMultiplier^attempt`
2. If WebSocket auth resolved: confirm WebSocket connections require valid session
