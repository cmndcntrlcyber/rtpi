# v3.1.3 — Testing

**Grade: C** | Target: B+ | Priority: 3

## Overview

62 test files (53 unit, 4 integration, 5 E2E Playwright). Good regression guards (anti-fabrication test for agent tool connector). But zero React component tests, zero tool executor tests, no retry/backoff unit tests, no OAuth or API key auth tests, no rate limiter tests. Coverage tooling is 3 major versions behind.

## Evidence

- **Unit tests** (53 files): Cover API routes, services, auth strategies, middleware, utilities.
- **Integration tests** (4 files): Burp activation, vulnerability investigation, memory agents, hourly workflow.
- **E2E tests** (5 Playwright specs): Basic flows, attack framework, kasm workspaces, tool migration, phase6 flow.
- **Regression guard**: `agent-tool-connector-no-fabrication.test.ts` prevents re-introduction of stub success strings.
- **Gaps identified**:
  - Zero React component tests (no React Testing Library usage)
  - Zero tool executor tests (Metasploit, BBOT, Nuclei, Nmap)
  - No retry/backoff calculation tests for dynamic workflow orchestrator
  - No OAuth flow tests, no API key auth tests
  - No rate limiter tests
- **Tooling**: `@vitest/coverage-v8` at 1.x (current is 4.x), `vitest` at 1.x (current is 3.x).

## Repair Items

| # | Item | Effort | Files |
|---|------|--------|-------|
| 1 | Add rate limiter unit tests | S | `tests/unit/server/middleware/` |
| 2 | Add retry/backoff isolated tests for dynamic workflow orchestrator | M | `tests/unit/services/` |
| 3 | Add OAuth and API key auth integration tests | M | `tests/integration/` |
| 4 | Add unit tests for tool executors (MSF, BBOT, Nuclei, Nmap) | L | `tests/unit/services/` |
| 5 | Add React Testing Library component tests for critical pages (Dashboard, Operations, Agents) | L | `tests/unit/client/` |
| 6 | Update `@vitest/coverage-v8` to 4.x, align Vitest config | M | `package.json`, `vitest.config.ts` |

## Implementation Notes

### Tool Executor Tests

Mock Docker execution layer and verify: (1) correct command construction, (2) result parsing for each output format (JSON, XML, JSONL), (3) error classification on failure, (4) database writes (discoveredAssets, vulnerabilities). Test each executor independently.

### Component Tests

Use React Testing Library with Vitest (jsdom already configured in `vitest.config.ts`). Priority pages: Dashboard (stat cards render), Operations (table filtering), Agents (workflow builder interactions). Mock API calls via MSW or manual fetch mocking.

### Retry/Backoff Tests

Test `dynamic-workflow-orchestrator.ts` retry logic in isolation: verify exponential backoff calculation (`baseDelay * backoffMultiplier^attempt`), max retry cap, task status transitions on each attempt, error message persistence.

## Verification

1. Run `npm test -- --coverage` and confirm new test files execute
2. Verify coverage report shows increased coverage for services/ and client/
3. Confirm `@vitest/coverage-v8` 4.x produces valid coverage output
