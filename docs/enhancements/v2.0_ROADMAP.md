# RTPI v2.0 Roadmap: Complete All Enhancement Categories to 100%

**Created:** January 19, 2026
**Target:** Complete all documented enhancements to 100% implementation
**Total Estimated Effort:** 6-8 weeks

---

## Executive Summary

Based on analysis of `/docs/enhancements/`, 245+ enhancements were documented. Currently ~65% are implemented. This roadmap details the specific steps needed to achieve 100% completion across all categories.

---

## Category Status Overview

| Category | Current | Target | Gap |
|----------|---------|--------|-----|
| Surface Assessment | 80% | 100% | 7 features |
| Tool Framework | 80% | 100% | 5 features |
| ATT&CK Integration | 63% | 100% | 15 features |
| Agentic Implants | 73% | 100% | 8 features |
| UI/UX Improvements | 40% | 100% | 18 features |
| Empire C2 | 100% | 100% | ✅ Complete |
| Kasm Workspaces | 80% | 100% | 2 features |
| Ollama/AI | 100% | 100% | ✅ Complete |
| Critical Bugs | 67% | 100% | 5 fixes |
| Report Generation | 33% | 100% | 10 features |

---

## Phase 1: Critical Bugs (Week 1)
**Source:** `docs/enhancements/01-CRITICAL-BUGS.md`

### 1.1 Operations Date Handling Fix
**Status:** ❌ Not Verified
**Files:**
- `client/src/pages/Operations.tsx`
- `server/api/v1/operations.ts`

**Implementation:**
```typescript
// Frontend: Send ISO strings instead of Date objects
if (formData.startDate) {
  submitData.startDate = `${formData.startDate}T00:00:00.000Z`;
}

// Backend: Parse dates with Zod validation
const operationSchema = z.object({
  startDate: z.string().datetime().optional().transform(val => val ? new Date(val) : undefined),
  endDate: z.string().datetime().optional().transform(val => val ? new Date(val) : undefined),
});
```

**Testing:** Create/update operations with various date combinations

### 1.2 CVSS Calculator Paste Feature
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/vulnerabilities/CVSSCalculator.tsx`

**Implementation:**
- Add paste handler for CVSS vector strings (e.g., `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H`)
- Parse vector string and auto-populate all metric dropdowns
- Calculate score automatically on paste

### 1.3 Scan Comparison Feature
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/surface-assessment/ScanComparisonDialog.tsx` (NEW)
- `server/api/v1/surface-assessment.ts`

**Implementation:**
- Add "Compare" button when 2 scans selected
- Side-by-side diff of assets/services/vulnerabilities
- Highlight new, removed, and changed items
- Export comparison report

### 1.4 CIDR Scanning Progress Streaming
**Status:** ❌ Not Implemented
**Files:**
- `server/services/docker-executor.ts`
- `client/src/components/surface-assessment/ScanProgress.tsx` (NEW)

**Implementation:**
- WebSocket endpoint for real-time progress
- Parse nmap progress output (% complete, hosts scanned)
- Display progress bar with ETA
- Allow scan cancellation

### 1.5 Metasploit Embedded Terminal
**Status:** ❌ Not Implemented (Executor exists, no terminal UI)
**Files:**
- `client/src/components/metasploit/EmbeddedTerminal.tsx` (NEW)
- `server/api/v1/metasploit-ws.ts` (NEW)

**Implementation:**
- xterm.js terminal component
- WebSocket connection to msfconsole container
- Bidirectional I/O streaming
- Command history persistence
- Workspace isolation per operation

---

## Phase 2: UI/UX Improvements (Week 1-2)
**Source:** `docs/enhancements/06-UI-UX-IMPROVEMENTS.md`

### 2.1 Collapsible Sidebar
**Status:** ❌ Not Implemented
**Files:**
- `client/src/hooks/useSidebarCollapse.ts` (NEW)
- `client/src/components/layout/Sidebar.tsx`
- `client/src/components/layout/MainLayout.tsx`

**Implementation:**
```typescript
// Custom hook with localStorage persistence
export function useSidebarCollapse() {
  const [isCollapsed, setIsCollapsed] = useState(() =>
    localStorage.getItem('rtpi_sidebar_collapsed') === 'true'
  );
  // ... toggle function, keyboard shortcut (Ctrl+B)
}
```

- Toggle between 256px (expanded) and 64px (collapsed)
- Radix UI Tooltip for icon labels when collapsed
- 300ms CSS transition
- localStorage persistence

### 2.2 Keyboard Shortcuts
**Status:** ❌ Not Implemented
**Files:**
- `client/src/hooks/useKeyboardShortcuts.ts` (NEW)
- `client/src/components/shared/KeyboardShortcutsHelp.tsx` (NEW)
- `client/src/components/shared/CommandPalette.tsx` (NEW)

**Implementation:**
| Shortcut | Action |
|----------|--------|
| `Ctrl/Cmd + K` | Open command palette |
| `Ctrl/Cmd + B` | Toggle sidebar |
| `Ctrl/Cmd + /` | Show shortcuts help |
| `Escape` | Close modals/dialogs |
| `Ctrl/Cmd + N` | New item (context-aware) |
| `Ctrl/Cmd + S` | Save/Submit |

- Global event listener with keyboard shortcut registry
- Command palette with fuzzy search (cmdk library)
- Help modal showing all shortcuts

### 2.3 Mobile Responsive Design
**Status:** ❌ Limited
**Files:**
- All component files
- `client/src/index.css`

**Implementation:**
- Minimum support: 768px (tablet)
- Responsive breakpoints: sm(640), md(768), lg(1024), xl(1280)
- Mobile navigation drawer
- Responsive tables with horizontal scroll
- Touch-friendly button sizes (min 44px)

### 2.4 Notification Center
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/shared/NotificationCenter.tsx` (NEW)
- `client/src/contexts/NotificationContext.tsx` (NEW)
- `server/api/v1/notifications.ts` (NEW)
- `shared/schema.ts` (add notifications table)

**Implementation:**
- Bell icon in header with unread count badge
- Slide-out notification panel
- Notification types: scan_complete, vulnerability_found, system_alert
- Mark as read, clear all, sound toggle
- WebSocket for real-time notifications
- Notification preferences per user

### 2.5 Saved Filter Presets
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/shared/FilterPresets.tsx` (NEW)
- `server/api/v1/filter-presets.ts` (NEW)
- `shared/schema.ts` (add filter_presets table)

**Implementation:**
- Save current filter state as named preset
- Quick-apply saved presets
- Share presets across team
- Delete unused presets

### 2.6 WCAG 2.1 Accessibility
**Status:** ❌ Partial
**Files:** All components

**Implementation:**
- All interactive elements keyboard-navigable
- ARIA labels on icons and buttons
- Focus indicators visible (not just outline:none)
- Color contrast 4.5:1 minimum
- Screen reader announcements for dynamic content
- Skip-to-content link

---

## Phase 3: Surface Assessment Completion (Week 2)
**Source:** `docs/enhancements/02-SURFACE-ASSESSMENT.md`

### 3.1 Network Topology View
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/surface-assessment/NetworkTopologyView.tsx` (NEW)

**Implementation:**
- React Flow or D3.js visualization
- Nodes: Assets (hosts, domains)
- Edges: Network connections, service relationships
- Cluster by subnet/network segment
- Color-code by vulnerability severity
- Interactive zoom/pan
- Export as PNG/SVG

### 3.2 Scan Scheduling (Cron)
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/surface-assessment/ScheduleConfigTab.tsx` (NEW)
- `server/services/scan-scheduler.ts` (NEW)
- `shared/schema.ts` (add scan_schedules table)

**Implementation:**
```typescript
interface ScanSchedule {
  id: string;
  operationId: string;
  name: string;
  cronExpression: string; // e.g., "0 2 * * 1" (Mondays 2am)
  toolConfig: {
    bbot?: BBOTConfig;
    nuclei?: NucleiConfig;
  };
  targets: string[];
  enabled: boolean;
  lastRun?: Date;
  nextRun?: Date;
}
```

- node-cron for job execution
- Timezone support
- Email/webhook notifications on completion
- Prevent overlapping runs

### 3.3 Tool Credentials Management
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/surface-assessment/CredentialsManager.tsx` (NEW)
- `server/api/v1/credentials.ts` (NEW)
- `shared/schema.ts` (add tool_credentials table)

**Implementation:**
- Secure storage for API keys (Shodan, VirusTotal, etc.)
- Encryption at rest (AES-256)
- Test credentials button
- Masked display with copy-to-clipboard
- Last used timestamp

### 3.4 CSV/Markdown Export
**Status:** ❌ Only JSON
**Files:**
- `client/src/components/surface-assessment/ExportDialog.tsx`
- `server/api/v1/surface-assessment.ts`

**Implementation:**
- Add format dropdown: JSON, CSV, Markdown
- CSV: Flat structure with all fields
- Markdown: Formatted tables for reporting
- Field selection checkboxes
- Filename customization

### 3.5 Webhook/Email Notifications
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/settings/NotificationSettings.tsx` (NEW)
- `server/services/notification-service.ts` (NEW)

**Implementation:**
- Webhook URL configuration with secret
- Email SMTP configuration
- Trigger settings: on scan complete, on critical vuln, on error
- Severity threshold for notifications
- Custom message templates

### 3.6 Infinite Scroll Option
**Status:** ❌ Only Pagination
**Files:**
- `client/src/components/surface-assessment/VulnerabilitiesTab.tsx`
- `client/src/components/surface-assessment/AssetsTab.tsx`

**Implementation:**
- Toggle between pagination and infinite scroll
- Intersection Observer for scroll detection
- Load 50 items at a time
- "Back to top" button
- Preserve scroll position on navigation

### 3.7 GitHub Actions Integration
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/surface-assessment/GitHubActionsPanel.tsx` (NEW)
- `server/services/github-actions-service.ts` (NEW)

**Implementation:**
- Connect GitHub repository
- List workflow runs with status
- Trigger manual workflow runs
- View run logs
- Import scan results from artifact

---

## Phase 4: ATT&CK Integration (Week 3)
**Source:** `docs/enhancements/03-ATTCK-INTEGRATION.md`

### 4.1 Embedded ATT&CK Navigator
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/attack/ATTCKNavigator.tsx` (NEW)

**Implementation:**
- Embed MITRE ATT&CK Navigator in iframe
- OR use React-based navigator library
- Techniques colored by discovered vulnerabilities
- Export layer JSON
- Create custom layers per operation

### 4.2 Visual Attack Flow Builder
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/attack/AttackFlowBuilder.tsx` (NEW)
- `server/api/v1/attack-flows.ts` (NEW)
- `shared/schema.ts` (add attack_flows table)

**Implementation:**
- React Flow canvas for drag-and-drop
- Nodes: Techniques, Objectives, Assets
- Edges: Progression paths with conditions
- Save/load flows
- Export to STIX format
- Simulate flow execution

### 4.3 ATT&CK Workbench Integration
**Status:** ❌ Not Implemented
**Files:**
- `server/services/attck-workbench-sync.ts` (NEW)

**Implementation:**
- REST API connection to ATT&CK Workbench
- Import STIX bundles
- Sync technique updates
- Collection management
- Custom technique creation

### 4.4 Technique Coloring by Vulnerabilities
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/attack/TechniqueHeatmap.tsx` (NEW)
- `server/api/v1/attack.ts`

**Implementation:**
- Query vulnerabilities mapped to techniques
- Color intensity based on count/severity
- Tooltip showing linked vulnerabilities
- Click to filter vulnerabilities by technique

### 4.5 "Send to Operation Lead" Button
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/attack/TechniqueActions.tsx` (NEW)
- `server/api/v1/attack.ts`

**Implementation:**
- Button on technique cards
- Creates task for Operation Lead agent
- Includes technique context and recommendations
- Workflow integration for planning

---

## Phase 5: Tool Framework Completion (Week 3)
**Source:** `docs/enhancements/05-TOOL-FRAMEWORK.md`

### 5.1 Visual Workflow Designer
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/tools/WorkflowDesigner.tsx` (NEW)

**Implementation:**
- React Flow canvas
- Drag tools from palette to canvas
- Connect tool outputs to inputs
- Configure each tool node
- Save/load workflow templates
- Execute workflow with progress tracking

### 5.2 Test Generation Framework
**Status:** ❌ Not Implemented
**Files:**
- `server/services/tool-test-generator.ts` (NEW)

**Implementation:**
- Generate unit tests for tool parameter validation
- Generate integration tests for tool execution
- Mock Docker executor for testing
- Coverage reporting
- CI/CD integration

### 5.3 Agent-Tool Assignment Validation
**Status:** ❌ Basic Only
**Files:**
- `server/services/agent-tool-validator.ts` (NEW)

**Implementation:**
- Define tool capabilities matrix
- Agent type requirements mapping
- Validate assignments before execution
- Suggest alternative tools if incompatible
- Capability gap analysis

### 5.4 Tool Execution Quotas
**Status:** ❌ Not Implemented
**Files:**
- `server/services/tool-quota-manager.ts` (NEW)

**Implementation:**
- Rate limits per tool per user
- Daily/weekly/monthly execution limits
- Resource allocation (CPU, memory)
- Quota alerts and enforcement

### 5.5 Tool Dependency Management
**Status:** ❌ Not Implemented
**Files:**
- `server/services/tool-dependency-manager.ts` (NEW)

**Implementation:**
- Track tool dependencies (e.g., nmap requires libpcap)
- Auto-install missing dependencies
- Version compatibility checking
- Dependency update notifications

---

## Phase 6: Agentic Implants Completion (Week 4)
**Source:** `docs/enhancements/04-AGENTIC-IMPLANTS.md`

### 6.1 Multi-Architecture Builds
**Status:** ❌ Limited
**Files:**
- `server/services/implant-builder.ts`

**Implementation:**
- Build targets: Windows (x86, x64), Linux (x64, ARM), macOS (x64, ARM)
- Cross-compilation pipeline
- Architecture-specific optimizations
- Size optimization for stealth

### 6.2 Auto-Registration
**Status:** ❌ Manual Only
**Files:**
- `server/services/rust-nexus-controller.ts`

**Implementation:**
- Implant calls registration endpoint on first connection
- Auto-generate unique ID and auth token
- Assign to operation based on deploy config
- Heartbeat registration renewal

### 6.3 Load Balancing
**Status:** ❌ Not Implemented
**Files:**
- `server/services/task-distributor.ts` (NEW)

**Implementation:**
- Round-robin task distribution
- Weighted distribution based on capability/load
- Geo-aware routing (prefer same subnet)
- Sticky sessions for related tasks
- Failure detection and redistribution

### 6.4 Autonomy Level Controls
**Status:** ❌ Basic Only
**Files:**
- `client/src/components/implants/AutonomyConfig.tsx` (NEW)

**Implementation:**
- UI for setting autonomy level (1-10)
- Level descriptions and risk warnings
- Per-implant override capability
- Audit log for autonomy changes

### 6.5 Emergency Kill Switch
**Status:** ❌ Not Implemented
**Files:**
- `server/services/implant-security.ts` (NEW)

**Implementation:**
- Global kill switch for all implants
- Per-implant shutdown command
- Automatic kill on anomaly detection
- Confirmation for destructive actions

---

## Phase 7: Report Generation (Week 4-5)
**Source:** Various enhancement docs

### 7.1 PDF Report Generation
**Status:** ❌ Not Implemented
**Files:**
- `server/services/report-generator.ts` (NEW)

**Implementation:**
- Puppeteer for HTML-to-PDF conversion
- Professional report templates
- Cover page with branding
- Table of contents generation
- Charts and graphs embedded
- Page numbers and headers/footers

### 7.2 DOCX Report Generation
**Status:** ❌ Not Implemented
**Files:**
- `server/services/docx-generator.ts` (NEW)

**Implementation:**
- docx library for Word document creation
- Template-based generation
- Styles matching PDF output
- Track changes support
- Comments for review workflow

### 7.3 Report Templates
**Status:** ❌ Basic Only
**Files:**
- `client/src/components/reports/TemplateEditor.tsx` (NEW)
- `shared/schema.ts` (add report_templates table)

**Implementation:**
- WYSIWYG template editor
- Variable placeholders ({{vulnerabilities}}, {{summary}})
- Section reordering
- Custom branding upload
- Template sharing

### 7.4 Executive Summary Generator
**Status:** ❌ Not Implemented
**Files:**
- `server/services/executive-summary-generator.ts` (NEW)

**Implementation:**
- AI-powered summary generation
- Key findings extraction
- Risk scoring overview
- Recommendations prioritization
- Customizable tone (technical vs executive)

### 7.5 Finding Screenshots
**Status:** ❌ Not Implemented
**Files:**
- `client/src/components/vulnerabilities/ScreenshotUpload.tsx` (NEW)
- `server/api/v1/vulnerabilities.ts`

**Implementation:**
- Image upload to vulnerability records
- Screenshot annotation tools
- Multiple images per finding
- Thumbnail gallery
- Full-size lightbox view

---

## Phase 8: Kasm Workspaces Completion (Week 5)
**Source:** `docs/enhancements/08-EXTERNAL-SERVICES-INTEGRATION-PHASE2.md`

### 8.1 Let's Encrypt Integration
**Status:** ❌ Manual SSL
**Files:**
- `server/services/ssl-manager.ts` (NEW)

**Implementation:**
- Certbot integration for automatic certificates
- Cloudflare DNS validation
- Certificate renewal cron job
- Certificate status monitoring

### 8.2 Burp Suite Dynamic Build
**Status:** ❌ Not Implemented
**Files:**
- `Dockerfile.burp` (NEW)
- `server/services/burp-workspace.ts` (NEW)

**Implementation:**
- Dockerfile with Burp Suite installation
- License key configuration
- Project file persistence
- Integration with RTPI findings

---

## Verification Checklist

### Per-Feature Testing
- [ ] Unit tests written and passing
- [ ] Integration tests for API endpoints
- [ ] Manual testing in development environment
- [ ] UI/UX review for frontend changes
- [ ] Security review for sensitive features
- [ ] Documentation updated

### End-to-End Testing
- [ ] Complete operation workflow
- [ ] Scan execution and result storage
- [ ] Vulnerability lifecycle management
- [ ] Report generation pipeline
- [ ] User permission enforcement
- [ ] Multi-user concurrent access

### Performance Testing
- [ ] API response times under load
- [ ] Database query optimization
- [ ] Frontend rendering performance
- [ ] WebSocket connection stability
- [ ] Large dataset handling

---

## Implementation Priority Order

### Week 1: Foundation
1. Operations Date Handling Fix
2. Collapsible Sidebar
3. Keyboard Shortcuts
4. CVSS Calculator Paste

### Week 2: Core Features
5. Notification Center
6. CSV/Markdown Export
7. Saved Filter Presets
8. Scan Comparison

### Week 3: ATT&CK & Tools
9. ATT&CK Navigator Embed
10. Visual Attack Flow Builder
11. Tool Workflow Designer
12. Agent-Tool Validation

### Week 4: Implants & Reports
13. Multi-Arch Implant Builds
14. Auto-Registration
15. PDF Report Generation
16. Executive Summary AI

### Week 5: Polish
17. Network Topology View
18. Scan Scheduling
19. Mobile Responsive
20. WCAG Accessibility

### Week 6: Final
21. All remaining features
22. Comprehensive testing
23. Documentation
24. Release preparation

---

## Files to Create

| File | Purpose |
|------|---------|
| `client/src/hooks/useSidebarCollapse.ts` | Sidebar collapse state |
| `client/src/hooks/useKeyboardShortcuts.ts` | Global keyboard shortcuts |
| `client/src/components/shared/CommandPalette.tsx` | Cmd+K palette |
| `client/src/components/shared/NotificationCenter.tsx` | Notification panel |
| `client/src/components/surface-assessment/NetworkTopologyView.tsx` | Asset visualization |
| `client/src/components/surface-assessment/ScheduleConfigTab.tsx` | Cron scheduling |
| `client/src/components/surface-assessment/ScanComparisonDialog.tsx` | Diff two scans |
| `client/src/components/attack/ATTCKNavigator.tsx` | Navigator embed |
| `client/src/components/attack/AttackFlowBuilder.tsx` | Visual flow builder |
| `client/src/components/tools/WorkflowDesigner.tsx` | Tool workflow canvas |
| `client/src/components/metasploit/EmbeddedTerminal.tsx` | MSF terminal |
| `client/src/components/reports/TemplateEditor.tsx` | Report template editor |
| `server/services/scan-scheduler.ts` | Cron job management |
| `server/services/notification-service.ts` | Email/webhook alerts |
| `server/services/report-generator.ts` | PDF generation |
| `server/services/task-distributor.ts` | Implant load balancing |
| `server/api/v1/notifications.ts` | Notification endpoints |
| `server/api/v1/attack-flows.ts` | Flow CRUD |
| `server/api/v1/metasploit-ws.ts` | MSF WebSocket |

---

## Success Metrics

- All 10 categories at 100% implementation
- Zero documented features remaining unimplemented
- All tests passing
- Documentation complete for new features
- User acceptance testing passed

---

**Last Updated:** January 19, 2026
**Maintained By:** RTPI Development Team
